# 风险安全模块需求实现对照表

## 1. 文档目的

本文档用于对照以下两类信息：

1. `压力管理Agent风险安全模块设计说明.md` 中定义的目标需求；
2. 当前项目代码中的实际实现状态。

文档重点不是重复设计原理，而是回答以下问题：

1. 哪些需求已经真正落地；
2. 哪些需求只完成了主干，还需要继续补强；
3. 哪些需求当前仍未实现；
4. 后续应优先补哪些缺口。

---

## 2. 状态定义

为便于对照，本文档使用三种状态：

- `已实现`：主链路能力已经落地，能够直接影响系统行为，可作为当前阶段验收项；
- `部分实现`：核心方向已经接入，但与设计目标相比仍存在明显缺口；
- `未实现`：当前代码中尚未形成对应能力，或只有零散准备工作。

---

## 3. 总体结论

当前风险安全模块的整体实现情况可以概括为：

1. 风险安全主链路已经基本落地；
2. `P0` 级核心安全能力已经形成闭环；
3. `P1` 已完成规则层补强、风险惯性和第三方危机求助专用支持分支；
4. `P2` 已完成一轮单轮安全边界评测与输出安全专项测试；
5. 当前剩余缺口主要集中在语义风险识别层、风险与长期记忆联动、调试可观测性和端到端评测。

如果按“是否能真正控制系统安全行为”来判断，当前项目已经不是“只有设计、没有实现”的状态，而是已经具备可运行的 safety controller 雏形。

---

## 4. 需求对照清单

### 4.1 风险分级从三层升级到四层

- 状态：`已实现`
- 需求说明：将风险主语义从旧的 `low / medium / high` 升级为 `Level 0 / 1 / 2 / 3`，同时保留兼容层。
- 当前实现：
  - 主语义已经统一为 `level_0 / level_1 / level_2 / level_3`
  - 同时保留 `legacy_level`
  - 推荐门控、主编排和前端兼容层均已接入新语义
- 代码落点：
  - `backend/risk_evaluator.py`
  - `backend/recommend_gate.py`
  - `backend/agent_orchestrator.py`
  - `backend/risk_levels.py`
- 当前判断：
  - 当前可以视为已经完成验收；
  - 剩余工作主要是后续逐步收敛旧兼容字段。

### 4.2 风险评估改为“多维评分 + 硬规则提升”

- 状态：`已实现`
- 需求说明：不再只做关键词命中和简单总分，而要拆成多个风险维度，再通过硬规则提升得到最终风险等级。
- 当前实现：
  - 已有 7 个显式维度：
    - `risk_ideation`
    - `action_intent`
    - `plan_specificity`
    - `time_urgency`
    - `means_access`
    - `self_control`
    - `protective_factors`
  - 已有多种硬规则提升：
    - 明确高危词直接提升到 `level_3`
    - 行动倾向 + 时间紧迫性提升到 `level_3`
    - 明确失控提升到 `level_3`
    - 明显痛苦 + 自控减弱至少进入 `level_2`
- 代码落点：
  - `backend/risk_evaluator.py`
- 当前判断：
  - 结构已经成立，可解释性也已具备；
  - 后续可继续增强规则覆盖范围，但不影响当前验收。

### 4.3 风险结果必须可解释

- 状态：`已实现`
- 需求说明：风险判断不能只输出一个黑盒标签，必须说明为什么判高风险。
- 当前实现：
  - 已输出：
    - `risk_dimensions`
    - `risk_evidence`
    - `escalation_reasons`
    - `raw_score`
    - `risk_context`
- 代码落点：
  - `backend/risk_evaluator.py`
- 当前判断：
  - 后端结构化输出已经满足“可解释”要求；
  - 调试视图尚未完整展示这些字段，但这属于展示增强，不属于主功能缺失。

### 4.4 风险结果必须真正改变后续流程

- 状态：`已实现`
- 需求说明：风险识别结果不能只影响当前一句回复，而要影响后续回复模式、推荐系统和记忆系统。
- 当前实现：
  - 风险评估在主链路前置执行；
  - 后续根据风险结果决定是否进入：
    - 普通 `AgentLoop`
    - `HighRiskSupport`
    - `SafetyResponse`
    - `ThirdPartyCrisisSupport`
  - 推荐门控与输出安全检查都会读取风险状态
- 代码落点：
  - `backend/agent_orchestrator.py`
  - `backend/recommend_gate.py`
  - `backend/output_safety_checker.py`
- 当前判断：
  - 当前已经满足“风险结果改变后续流程”的核心要求。

### 4.5 建立 `Level 2 / Level 3` 双层安全门控

- 状态：`已实现`
- 需求说明：不能只有最高风险才脱离普通流程，`Level 2` 也必须进入保守支持模式。
- 当前实现：
  - `level_2` 进入 `HighRiskSupport`
  - `level_3` 进入 `SafetyResponse`
  - 第三方危机求助进入 `ThirdPartyCrisisSupport`
- 代码落点：
  - `backend/agent_orchestrator.py`
  - `backend/urgent_detector.py`
- 当前判断：
  - 这项需求已经完成，且主链路分支清晰。

### 4.6 高风险时关闭或严格限制普通推荐

- 状态：`已实现`
- 需求说明：高风险场景下普通推荐必须降级或关闭。
- 当前实现：
  - `level_3` 返回 `recommend_type = "none"`
  - `level_2` 返回 `recommend_type = "safety_only"`
  - 输出安全检查器也会在高风险下再次清空推荐
- 代码落点：
  - `backend/recommend_gate.py`
  - `backend/agent_orchestrator.py`
  - `backend/output_safety_checker.py`
- 当前判断：
  - 普通推荐误触发的核心缺口已经补齐；
  - 后续可再增强“安全支持类内容”的细粒度筛选。

### 4.7 规则层需要识别否定表达

- 状态：`已实现`
- 需求说明：要区分“我想自杀”和“我没有想自杀”。
- 当前实现：
  - 已有 `is_safe_denial`
  - 显式否认会阻止高危词直接抬高等级
- 代码落点：
  - `backend/risk_evaluator.py`
- 当前判断：
  - 核心需求已满足；
  - 后续仍可继续补更多否定句式。

### 4.8 规则层需要识别第三方求助和讨论语境

- 状态：`已实现`
- 需求说明：要区分用户本人风险、第三方危机转述和影视/新闻/论文讨论语境。
- 当前实现：
  - 已识别：
    - `subject == "self"`
    - `subject == "third_party"`
    - `subject == "discussion"`
  - 已在打分和最终等级中加入语境修正
- 代码落点：
  - `backend/risk_evaluator.py`
- 当前判断：
  - 当前已经能防掉最关键的误报场景。

### 4.9 第三方危机求助应有专用支持模式

- 状态：`已实现`
- 需求说明：当用户是为他人求助时，不应误用“用户本人危机模板”，而应给出如何帮助对方的支持性引导。
- 当前实现：
  - 已有第三方危机求助专用分支；
  - 已有专门的 LLM / fallback 响应模板；
  - 已关闭普通推荐。
- 代码落点：
  - `backend/agent_orchestrator.py`
  - `backend/urgent_detector.py`
- 当前判断：
  - 当前已达到第一阶段验收要求；
  - 后续可补更完整的端到端评测和可观测性。

### 4.10 高风险状态要有风险惯性，不能一轮归零

- 状态：`已实现`
- 需求说明：用户上一轮高风险，下一轮简单说“没事”后，系统不能立刻恢复普通模式。
- 当前实现：
  - 已读取 `recent_risk_levels`
  - 已实现：
    - `level_3 -> level_2` 的惯性下限
    - 最近存在 `level_3` 时禁止直接掉到 `level_0`
    - 最近 `level_2` 连续出现时禁止直接归零
- 代码落点：
  - `backend/risk_evaluator.py`
  - `backend/conversation_manager.py`
- 当前判断：
  - 当前已满足会话内风险惯性的主要需求；
  - 跨 session 的更长期惯性仍可继续增强。

### 4.11 输出安全检查器必须独立存在

- 状态：`已实现`
- 需求说明：输出安全不能只靠 Prompt，最终回复出站前必须有独立检查器。
- 当前实现：
  - 已新增 `output_safety_checker.py`
  - 已在主编排中强制调用
- 代码落点：
  - `backend/output_safety_checker.py`
  - `backend/agent_orchestrator.py`
- 当前判断：
  - 主机制已完成；
  - 当前以规则拦截 + fallback 模板为主。

### 4.12 输出安全检查要阻止危险细节和错误安抚

- 状态：`已实现`
- 需求说明：要防止危险细节、鼓励独处、否定用户痛苦、不负责任保证等问题。
- 当前实现：
  - 已拦截：
    - 危险细节
    - 轻视/否定用户痛苦
    - 不负责任保证
    - 鼓励独处
    - 高风险下普通推荐
- 代码落点：
  - `backend/output_safety_checker.py`
- 当前判断：
  - 核心安全拦截已经形成；
  - 后续可以持续扩展规则词表与替换策略。

### 4.13 风险结果要和记忆系统联动

- 状态：`部分实现`
- 需求说明：风险状态不仅影响当前轮，还要稳定影响后续几轮甚至跨 session 行为。
- 当前实现：
  - 会话摘要已保留 `recent_risk_levels`
  - 风险评估器已使用这些信息做惯性修正
- 代码落点：
  - `backend/conversation_manager.py`
  - `backend/risk_evaluator.py`
- 剩余差距：
  - 当前主要是短期会话级联动；
  - 与长期记忆、跨 session 回复约束的联动还不够完整。
- 当前判断：
  - 主干已接入，但还不能视为完全验收。

### 4.14 长期记忆中应使用中性的风险摘要，而不是诊断标签

- 状态：`部分实现`
- 需求说明：不应直接把用户写成“有自杀倾向”，而应记录“近期多次表达强压力和无助感，需要更谨慎回应”这类中性摘要。
- 当前实现：
  - 已有风险状态和风险事件写入基础；
  - 但尚未形成明确的中性化摘要写入策略。
- 代码落点：
  - `backend/conversation_manager.py`
  - `backend/database.py`
- 剩余差距：
  - 缺少统一的“风险摘要表达模板”和写入收敛策略。
- 当前判断：
  - 这是当前比较明确的待补强项。

### 4.15 增加语义风险识别层，而不是只靠规则

- 状态：`未实现`
- 需求说明：应增加 LLM 或模型语义层，用于识别关键词难以覆盖的隐晦风险表达。
- 当前实现：
  - 当前仍是规则 + 结构化评分；
  - 尚未看到独立的语义风险识别模块或语义层融合逻辑。
- 代码落点：
  - 当前实现仍集中在 `backend/risk_evaluator.py`
- 剩余差距：
  - 需要新增语义风险判定层，并把结果纳入统一风险融合逻辑。
- 当前判断：
  - 这是当前最明确的“未实现需求”。

### 4.16 风险评测要覆盖边界样本

- 状态：`已实现一轮`
- 需求说明：评测集要覆盖否定表达、第三方求助、讨论语境、多轮升级等边界情况。
- 当前实现：
  - 已补充：
    - 显式否定
    - 第三方危机求助
    - 讨论语境
    - 风险惯性
  - 已新增 `safety_boundary_acc`
- 代码落点：
  - `agent_test_data/emotion_risk_turns.jsonl`
  - `agent_test_data/eval_skeleton.py`
  - `tests/test_p2_eval_safety.py`
- 当前判断：
  - 单轮安全边界评测已成型；
  - 但多轮端到端评测仍未正式接入。

### 4.17 安全评测不能只看准确率，要看高风险召回

- 状态：`已实现一轮`
- 需求说明：高风险召回率、紧急风险召回率、多轮稳定性等都应进入评测口径。
- 当前实现：
  - 已有：
    - `high_risk_recall`
    - `safety_boundary_acc`
- 代码落点：
  - `agent_test_data/eval_skeleton.py`
- 剩余差距：
  - `Level 3 recall`
  - 多轮稳定性
  - 高风险误推荐率
  - 端到端安全输出评测
  仍可继续补。
- 当前判断：
  - 评测方向已经启动，但还没完全覆盖设计目标。

### 4.18 调试面板需要能复盘风险判断路径

- 状态：`部分实现`
- 需求说明：调试视图应展示风险等级、维度分数、提升原因、输出安全结果和当前响应模式。
- 当前实现：
  - 已可展示基础风险状态；
  - 已可展示最近风险等级；
  - 已兼容旧风险字段。
- 代码落点：
  - `frontend/debug_panel.py`
- 剩余差距：
  - 还未完整透出：
    - `risk_dimensions`
    - `escalation_reasons`
    - `risk_context`
    - 输出安全检查结果
- 当前判断：
  - 后端数据已具备，前端调试展示尚未完全跟上。

---

## 5. 分类汇总

### 5.1 已实现

以下需求可以视为当前阶段已经完成：

1. 四级风险语义统一；
2. 多维评分 + 硬规则提升；
3. 风险结果可解释；
4. 风险结果真正控制系统流程；
5. `level_2 / level_3` 双层安全门控；
6. 高风险普通推荐关闭；
7. 否定表达识别；
8. 第三方求助 / 讨论语境识别；
9. 第三方危机求助专用支持分支；
10. 风险惯性；
11. 独立输出安全检查器；
12. 输出安全规则拦截。

### 5.2 部分实现

以下需求已经接入主干，但还未完全达到设计目标：

1. 风险与记忆系统联动；
2. 长期记忆中的中性风险摘要写入；
3. 安全边界评测全量覆盖；
4. 安全评测指标进一步细化；
5. 调试面板风险判断路径复盘。

### 5.3 未实现

以下需求当前仍属于明确待开发项：

1. 独立语义风险识别层。

---

## 6. 当前最值得继续推进的事项

结合当前实现状态，建议下一阶段优先做以下三件事：

1. 接入语义风险识别层，补足隐晦表达识别能力；
2. 升级风险与长期记忆联动，把“近期风险上下文摘要”真正落到跨 session 行为；
3. 完成调试面板增强与端到端风险评测接入。

原因是：

1. `P0` 级行为安全闭环已经形成；
2. 继续做语义识别和长期联动，收益会比再补规则词表更高；
3. 端到端评测和可观测性补齐后，后续优化才能稳定迭代。

---

## 7. 结论

从需求实现角度看，当前风险安全模块已经完成了“从设计方案到可运行主链路”的关键跃迁。

更准确地说：

1. 核心安全需求已经基本实现；
2. 当前系统已经具备输入风险识别、流程门控、推荐联动、输出安全兜底和单轮安全边界评测；
3. 现在的主要任务不再是“补主功能”，而是继续增强语义识别、长期记忆联动、调试可观测性和端到端验证。

因此，当前项目的风险安全模块可以视为：

- 已完成核心验收；
- 进入增强优化阶段。
