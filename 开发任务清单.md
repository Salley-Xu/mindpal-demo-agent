# 压力管理 Agent 开发任务清单

## 1. 文档目的

本文档基于现有项目代码和 `技术文档.md` 中定义的目标方案，整理一份可直接执行的开发任务清单。

文档重点不是重复设计原理，而是回答以下问题：

1. 当前项目离目标方案还差什么；
2. 建议先做哪些任务；
3. 每个任务要改哪些文件；
4. 每个阶段完成后如何验收。

---

## 2. 当前项目基线

当前项目已经具备以下基础能力：

1. FastAPI 后端接口；
2. Streamlit 前端聊天界面；
3. 主 Agent 编排雏形；
4. 会话持久化与用户画像基础表；
5. 简单情绪分析；
6. 关键词驱动的风险检测；
7. 基于内容库的规则推荐。

现有可复用的核心文件：

- `backend/agent_orchestrator.py`
- `backend/api_endpoints.py`
- `backend/conversation_manager.py`
- `backend/database.py`
- `backend/emotion_analyzer.py`
- `backend/urgent_detector.py`
- `backend/content_db.py`
- `backend/content_recommender.py`
- `backend/models.py`
- `frontend/frontend.py`

当前项目与目标方案的主要差距：

1. 缺少统一的结构化状态模型；
2. 情绪识别结果过于简单；
3. 风险评估没有形成独立状态层；
4. 长期记忆字段不完整；
5. 推荐门控和冷却机制缺失；
6. 检索层仍然是简单规则检索，不是混合检索；
7. Prompt 组装没有显式注入完整状态；
8. 前端缺少推荐反馈和状态闭环；
9. 测试与评估体系基本缺失。

---

## 3. 开发原则

建议遵循以下实施原则：

1. 先修当前主链路问题，再做架构升级；
2. 先统一状态模型，再做推荐和检索增强；
3. 先规则化可控实现，再逐步引入更复杂的 LLM 或检索能力；
4. 每个阶段都要能独立运行和验证；
5. 数据结构变更必须配套迁移或兼容方案。

---

## 4. 优先级划分

### P0

目标：修复当前主链路问题，建立统一数据契约，为后续改造提供稳定基线。

### P1

目标：完成状态层、记忆层和推荐门控的重构，让系统从“对话原型”升级为“状态驱动 Agent”。

### P2

目标：完成混合检索、反馈闭环、评估体系和可观测性建设。

---

## 5. P0 任务清单

### P0-1 修复现有接口契约问题

#### 目标

修复当前已经确认会影响使用的主链路问题，避免在不稳定基线上继续迭代。

#### 任务项

1. 修复 `/content/recommend` 的请求协议；
2. 修复聊天接口返回的情绪摘要缺失问题；
3. 统一前后端情绪字段命名；
4. 接通前端高风险告警展示；
5. 修复“新会话”删除逻辑；
6. 清理前端后台线程直接调用 Streamlit UI 的写法。

#### 涉及文件

- `backend/api_endpoints.py`
- `backend/agent_orchestrator.py`
- `backend/models.py`
- `backend/conversation_manager.py`
- `frontend/frontend.py`

#### 输出物

1. 稳定可用的聊天主链路；
2. 稳定可用的推荐接口；
3. 前端可显示风险告警和情绪状态；
4. 修复后的统一接口字段。

#### 验收标准

1. 聊天请求可以成功返回回复、情绪状态和风险状态；
2. 推荐接口可以成功接收 JSON 请求体；
3. 高风险输入时前端出现明确可见的告警提示；
4. 点击“新会话”不会删除错误 session；
5. 前端不再使用后台线程更新 Streamlit 组件。

---

### P0-2 统一状态模型和 API 契约

#### 目标

把当前零散字段收敛成统一的数据结构，为 P1 的状态流重构铺路。

#### 任务项

1. 在 `backend/models.py` 中新增统一状态模型；
2. 将当前 `emotion_summary` 重构为明确的结构化对象；
3. 增加 `risk_state`、`recommendation_decision` 等响应结构；
4. 统一前后端状态字段命名；
5. 为前端保留必要的中文展示映射，但后端内部建议使用稳定枚举值。

#### 建议新增模型

1. `EmotionState`
2. `RiskState`
3. `SessionSummary`
4. `LongMemoryProfile`
5. `RecommendationDecision`
6. `RecommendationItem`
7. `AgentTurnResponse`

#### 涉及文件

- `backend/models.py`
- `backend/api_endpoints.py`
- `backend/agent_orchestrator.py`
- `frontend/frontend.py`

#### 验收标准

1. 前后端只使用一套状态字段；
2. 前端不再依赖旧的临时字段名；
3. API 文档中能清晰看到新的响应结构；
4. 后续模块改造不再需要猜测字段含义。

---

## 6. P1 任务清单

### P1-1 重构情绪识别模块

#### 目标

把当前单标签情绪分析升级为结构化情绪状态。

#### 任务项

1. 升级 `emotion_analyzer` 输出结构；
2. 增加情绪强度 `emotion_intensity`；
3. 增加压力来源 `stress_source`；
4. 增加用户意图 `user_intent`；
5. 增加多轮负面趋势 `negative_trend`；
6. 保留 `confidence` 作为置信度字段。

#### 建议实现

第一版优先使用规则 + LLM 结构化输出，不先引入训练分类模型。

#### 涉及文件

- `backend/emotion_analyzer.py`
- `backend/models.py`
- `backend/agent_orchestrator.py`

#### 验收标准

1. 每轮对话都能得到结构化 `EmotionState`；
2. 情绪字段可以被推荐门控和风险评估直接复用；
3. 情绪趋势不再依赖前端或临时字段拼接。

---

### P1-2 新建风险评估模块

#### 目标

把当前关键词检测升级为独立的风险状态模块。

#### 任务项

1. 新建 `backend/risk_evaluator.py`；
2. 将风险等级统一为 `low / medium / high`；
3. 将现有关键词规则迁移到新模块；
4. 将近期情绪趋势、长期风险等级和历史风险事件接入风险判断；
5. 为高风险场景设计强制安全响应分支。

#### 涉及文件

- `backend/risk_evaluator.py`
- `backend/urgent_detector.py`
- `backend/agent_orchestrator.py`
- `backend/models.py`

#### 验收标准

1. 风险状态是独立结构，而不是附加字段；
2. 高风险时普通推荐逻辑会被跳过；
3. 风险状态能写入长期记忆或事件日志；
4. 高风险回复遵循技术文档中的安全原则。

---

### P1-3 升级短期记忆

#### 目标

把当前以轮次为主的会话摘要机制升级为更稳定的短期上下文管理。

#### 任务项

1. 引入 token 统计；
2. 设计 soft threshold 和 hard threshold；
3. 重构摘要内容结构；
4. 保留最近若干轮原始消息和压缩摘要；
5. 在 Prompt 组装时优先使用结构化摘要。

#### 建议新增模块

- `backend/short_memory.py`

也可以先在 `conversation_manager.py` 内部重构，再决定是否拆文件。

#### 涉及文件

- `backend/conversation_manager.py`
- `backend/agent_orchestrator.py`
- `backend/models.py`
- `requirements.txt`

#### 依赖建议

如采用 token 统计，可增加 `tiktoken`。

#### 验收标准

1. 长对话不会无限堆积原始历史；
2. 摘要里保留问题背景、情绪、建议接受度、风险信息；
3. 上下文压缩后仍能维持对话连续性。

---

### P1-4 升级长期记忆与数据库结构

#### 目标

让系统具备跨 session 的用户建模能力。

#### 任务项

1. 扩展用户画像结构；
2. 扩展情绪事件结构；
3. 新增推荐日志表；
4. 新增推荐反馈表或反馈字段；
5. 实现长期记忆写入门控；
6. 区分 append、merge、overwrite 三类更新方式。

#### 建议新增或扩展的数据结构

1. `preferred_support_style`
2. `avoid_style`
3. `main_stress_sources`
4. `risk_level`
5. `emotion_intensity`
6. `stress_source`
7. `user_intent`
8. `recommendation_feedback`

#### 涉及文件

- `backend/database.py`
- `backend/agent_tools.py`
- `backend/agent_orchestrator.py`
- `backend/models.py`

#### 补充任务

1. 增加数据库迁移方案；
2. 处理旧数据兼容；
3. 为长期记忆写入增加去噪策略。

#### 验收标准

1. 同一用户跨 session 能保留偏好和风险状态；
2. 情绪事件不再只有简单情绪标签；
3. 推荐反馈能回写长期记忆；
4. 数据表结构能支持文档中的状态模型。

---

### P1-5 新建推荐门控模块

#### 目标

控制“什么时候推荐、推荐什么形式、是否需要冷却”。

#### 任务项

1. 新建 `backend/recommend_gate.py`；
2. 引入推荐分数计算；
3. 增加冷却机制；
4. 增加用户明确求助场景的加权；
5. 增加用户拒绝某类推荐的惩罚；
6. 区分软推荐与硬推荐。

#### 推荐输入

1. `emotion_intensity`
2. `risk_state`
3. `user_intent`
4. `negative_trend`
5. `user_profile`
6. `recent_recommendations`
7. `conversation_state`

#### 输出建议

1. `should_recommend`
2. `recommend_type`
3. `recommend_score`
4. `reason_codes`
5. `cooldown_penalty`

#### 涉及文件

- `backend/recommend_gate.py`
- `backend/agent_orchestrator.py`
- `backend/models.py`
- `backend/database.py`

#### 验收标准

1. 系统不会每轮都推荐；
2. 高风险场景不会进入普通推荐；
3. 用户明确求助时更容易触发硬推荐；
4. 最近已推荐过时推荐分数会下降。

---

### P1-6 重构 Prompt 组装模块

#### 目标

把对话生成从“临时拼接”升级为“显式状态驱动”。

#### 任务项

1. 新建 `backend/prompt_builder.py`；
2. 将系统角色、安全规则、长期记忆、短期摘要、情绪状态、风险状态、推荐素材拆成独立输入块；
3. 定义优先级顺序；
4. 明确高风险和普通场景的 Prompt 差异；
5. 减少在主 Agent 中手工拼装 Prompt 的逻辑。

#### 涉及文件

- `backend/prompt_builder.py`
- `backend/agent_orchestrator.py`
- `backend/agent_prompts.py`

#### 验收标准

1. Prompt 输入来源清晰可追踪；
2. 高风险场景自动切换安全 Prompt；
3. 推荐内容能自然注入回复，而不是生硬拼接。

---

## 7. P2 任务清单

### P2-1 升级推荐素材库结构

#### 目标

让素材数据足以支撑门控、检索和 rerank。

#### 任务项

1. 扩展内容字段；
2. 增加适用风险等级；
3. 增加禁用场景；
4. 增加推荐形式；
5. 增加优先级；
6. 增加来源说明。

#### 涉及文件

- `backend/content_db.py`
- `backend/models.py`
- `backend/data/content_db.json`
- `data/content_db.json`

#### 验收标准

1. 素材数据能够支持门控和 rerank；
2. 高风险不适用内容能被明确过滤；
3. 同类素材具备稳定的结构化标签。

---

### P2-2 实现混合检索

#### 目标

把当前简单搜索升级为 `BM25 + 向量检索 + RRF` 的混合检索架构。

#### 任务项

1. 新建 `bm25_retriever.py`；
2. 新建 `vector_retriever.py`；
3. 新建 `hybrid_retriever.py`；
4. 实现多路查询；
5. 实现 RRF 融合；
6. 输出统一候选结果结构。

#### 推荐依赖

1. `rank-bm25`
2. `faiss-cpu`
3. `sentence-transformers` 或其他 embedding 方案

#### 涉及文件

- `backend/bm25_retriever.py`
- `backend/vector_retriever.py`
- `backend/hybrid_retriever.py`
- `backend/content_recommender.py`
- `requirements.txt`

#### 验收标准

1. 同一查询可同时利用关键词与语义召回；
2. 能输出候选排序及来源分数；
3. 查询覆盖率高于当前简单规则搜索。

---

### P2-3 新建重排模块

#### 目标

在候选内容集合上做状态感知的二次排序。

#### 任务项

1. 新建 `backend/reranker.py`；
2. 接入情绪匹配分；
3. 接入风险匹配分；
4. 接入偏好匹配分；
5. 接入可执行性分；
6. 接入重复惩罚。

#### 涉及文件

- `backend/reranker.py`
- `backend/content_recommender.py`
- `backend/models.py`

#### 验收标准

1. 候选内容排序不只依赖检索分数；
2. 高风险不适用内容被下调或剔除；
3. 用户偏好可影响最终排序。

---

### P2-4 前端反馈闭环

#### 目标

让推荐和长期记忆形成正反馈循环。

#### 任务项

1. 为推荐项增加反馈按钮；
2. 支持“有帮助 / 一般 / 不想看这类内容”；
3. 将反馈写入后端；
4. 更新长期记忆中的偏好和拒绝信号；
5. 在侧边栏展示当前用户偏好和风险状态。

#### 涉及文件

- `frontend/frontend.py`
- `backend/api_endpoints.py`
- `backend/database.py`
- `backend/agent_orchestrator.py`

#### 验收标准

1. 用户反馈能被成功记录；
2. 后续推荐能体现反馈偏好；
3. 前端能看到部分状态变化结果。

---

### P2-5 测试、评估与可观测性

#### 目标

为后续稳定迭代建立基本保障。

#### 任务项

1. 新建测试目录；
2. 为风险评估、推荐门控、推荐接口和聊天接口增加测试；
3. 建立一批固定评估样例；
4. 输出结构化日志；
5. 记录每轮的情绪、风险、推荐决策和生成结果。

#### 建议新增目录

- `tests/`
- `backend/eval/`

#### 验收标准

1. 主链路具备基本回归测试；
2. 高风险场景有专门用例；
3. 推荐误触发和重复推荐可以被检测；
4. 每轮决策状态可追踪。

---

## 8. 建议新增文件清单

建议优先新增以下文件：

- `backend/risk_evaluator.py`
- `backend/recommend_gate.py`
- `backend/prompt_builder.py`
- `backend/bm25_retriever.py`
- `backend/vector_retriever.py`
- `backend/hybrid_retriever.py`
- `backend/reranker.py`
- `backend/memory_updater.py`
- `tests/test_chat_api.py`
- `tests/test_recommend_gate.py`
- `tests/test_risk_evaluator.py`

---

## 9. 建议修改文件清单

以下文件预计需要重点修改：

- `backend/models.py`
- `backend/agent_orchestrator.py`
- `backend/api_endpoints.py`
- `backend/emotion_analyzer.py`
- `backend/urgent_detector.py`
- `backend/conversation_manager.py`
- `backend/database.py`
- `backend/agent_tools.py`
- `backend/content_db.py`
- `backend/content_recommender.py`
- `backend/config.py`
- `frontend/frontend.py`
- `requirements.txt`

---

## 10. 建议里程碑

### M1：基线稳定版

包含：

1. P0-1
2. P0-2

产出：

1. 可稳定运行的聊天与推荐主链路；
2. 统一状态字段；
3. 可继续演进的接口基线。

### M2：状态驱动版

包含：

1. P1-1
2. P1-2
3. P1-3
4. P1-4
5. P1-5
6. P1-6

产出：

1. 结构化状态层；
2. 长短期记忆升级；
3. 推荐门控与安全链路；
4. 状态驱动的 Prompt 组装。

### M3：检索增强版

包含：

1. P2-1
2. P2-2
3. P2-3

产出：

1. 混合检索；
2. 重排能力；
3. 更稳定的推荐质量。

### M4：产品闭环版

包含：

1. P2-4
2. P2-5

产出：

1. 推荐反馈闭环；
2. 基础评估体系；
3. 更可观测、更可迭代的工程基础。

---

## 11. 建议先做的最小可行版本

如果当前希望尽快把方案落到代码上，建议先做以下最小可行闭环：

1. 修复现有接口与前端问题；
2. 统一 `EmotionState` 和 `RiskState`；
3. 增强长期记忆字段；
4. 先实现规则版推荐门控；
5. 高风险场景强制安全响应；
6. 推荐先保留规则检索，不立即引入 FAISS。

这样可以先完成“状态驱动 Agent”的核心骨架，再在此基础上继续做混合检索和高级评估。

---

## 12. 执行建议

建议按以下顺序启动开发：

1. 先完成 M1；
2. M1 验收通过后再进入 M2；
3. M2 稳定后再做 M3；
4. 最后补 M4 的反馈与评估体系。

不建议一次性并行推进所有模块，否则容易出现：

1. 状态字段频繁变动；
2. 数据表重复返工；
3. 前后端接口反复变化；
4. 检索与推荐效果难以定位问题来源。

---

## 13. 备注

本文档是执行清单，不替代 `技术文档.md` 的方案设计说明。

建议后续在开发过程中继续维护两份文档：

1. `技术文档.md` 负责描述目标架构和设计原则；
2. `开发任务清单.md` 负责描述实施顺序、任务拆解和阶段验收。

---

## 14. 基于三份拆分文档的复核补充

本节基于以下三份文档重新对照当前项目现状整理：

1. `压力管理 Agent 系统架构设计文档.md`
2. `压力管理 Agent 记忆系统设计文档.md`
3. `压力管理 Agent 推荐系统设计文档.md`

这次复核的重点不是重复旧计划，而是回答两个问题：

1. 当前已经做出的能力，和新拆分文档相比还差哪几块；
2. 下一步最值得优先修的差距是什么。

### 14.1 当前实现与三份文档的对齐情况

当前项目已经基本具备以下能力：

1. 主 Agent 编排主链路；
2. 结构化 `EmotionState / RiskState / SessionSummary`；
3. `RecommendGate` 推荐门控；
4. `BM25 + 轻量向量检索 + RRF` 的混合检索骨架；
5. 推荐反馈接口与前端反馈按钮；
6. 长期画像字段扩展和情绪事件结构扩展；
7. 前端调试面板与基础回归测试。

但与三份新文档相比，仍存在几个关键缺口：

1. 高风险场景还没有真正切到独立安全响应链路；
2. 推荐日志、冷却状态和会话级反馈没有稳定持久化；
3. 长短期记忆还没有真正进入 Prompt 主链路；
4. 长期记忆写入仍然偏“每轮直写”，缺少 MemoryGate；
5. 短期记忆压缩还不是 token 驱动；
6. 语义检索仍是轻量过渡版，不是文档中的 FAISS 实现。

---

### 14.2 新优先级建议

结合当前代码现状，建议把后续任务优先级重新收敛为以下三层。

#### P0：修主行为正确性

目标：先补齐会直接影响系统行为正确性的链路缺口。

##### P0-1 高风险安全响应链路落地

目标：

让 `risk_state.level == high` 时真正进入独立安全响应，而不是继续沿用普通对话生成链路。

任务项：

1. 在 `agent_orchestrator.py` 中增加高风险专用分支；
2. 单独设计安全响应 Prompt；
3. 高风险时跳过普通推荐和一般性工具建议；
4. 优先输出稳定情绪、联系现实支持、寻求专业帮助类内容；
5. 将高风险事件稳定写入风险日志与长期记忆。

涉及文件：

- `backend/agent_orchestrator.py`
- `backend/agent_prompts.py`
- `backend/risk_evaluator.py`
- `backend/urgent_detector.py`
- `backend/models.py`

验收标准：

1. 高风险输入不再进入普通回复策略；
2. 高风险输出不包含泛化效率建议或普通练习推荐；
3. 高风险事件可以被日志和后续状态追踪复用；
4. 前端仍能正常展示风险提示。

##### P0-2 推荐日志与冷却状态持久化

目标：

让推荐门控真正能依赖“最近推荐过什么、用户接受了什么、拒绝了什么”做稳定决策，而不是只在内存里生效。

任务项：

1. 新增推荐事件持久化结构；
2. 新增推荐反馈持久化结构；
3. 会话加载时恢复 `recommendation_events`；
4. 会话摘要返回稳定的 `recent_recommendation_turns`、接受/拒绝状态；
5. 服务重启后推荐冷却逻辑仍然有效。

涉及文件：

- `backend/database.py`
- `backend/conversation_manager.py`
- `backend/api_endpoints.py`
- `backend/recommend_gate.py`
- `backend/agent_tools.py`

验收标准：

1. 推荐事件与反馈在重启后不丢失；
2. `RecommendGate` 的冷却逻辑跨进程仍有效；
3. 用户短期拒绝的内容不会立即再次出现；
4. 集成测试能覆盖“推荐 -> 反馈 -> 下轮门控”的闭环。

##### P0-3 记忆进入 Prompt 主链路

目标：

让系统真正按架构文档所述，在生成回复前使用短期摘要、长期画像和相关历史事件，而不是只把最近几轮原始消息发给模型。

任务项：

1. 将 `conversation_summary` 中的压缩摘要纳入 Prompt；
2. 将长期用户画像纳入 Prompt；
3. 明确当前输入、风险状态、情绪状态、长期记忆的优先级；
4. 避免长期记忆覆盖当前用户输入；
5. 为高风险和普通场景分别定义 Prompt 组装策略。

涉及文件：

- `backend/agent_orchestrator.py`
- `backend/agent_prompts.py`
- `backend/conversation_manager.py`
- `backend/agent_tools.py`

验收标准：

1. Prompt 组装可追踪地使用会话摘要与用户画像；
2. 长对话中重复建议明显减少；
3. 跨 session 的用户偏好能影响回复风格；
4. 高风险场景下长期记忆只作为辅助判断，不覆盖当前输入。

---

#### P1：补记忆系统设计缺口

目标：让记忆系统从“能存一些状态”升级为“符合文档定义的上下文工程模块”。

##### P1-1 引入 MemoryGate

目标：

避免把瞬时状态误写成长期偏好，减少长期记忆污染。

任务项：

1. 新增长期记忆写入门控；
2. 区分稳定偏好、压力来源、情绪事件、风险变化；
3. 增加置信度或重复出现次数作为写入条件；
4. 对 `preferred_support_style` 等字段改为审慎更新；
5. 保留 append / merge / overwrite 三类更新策略。

涉及文件：

- `backend/conversation_manager.py`
- `backend/agent_tools.py`
- `backend/database.py`
- `backend/agent_orchestrator.py`

验收标准：

1. 不会因为单轮 `seeking_help` 就立即固化长期偏好；
2. 重复出现的压力来源更容易写入长期画像；
3. 用户明确修改偏好时能覆盖旧值；
4. 长期记忆噪声显著减少。

##### P1-2 升级短期记忆压缩为 token 驱动

目标：

从“轮次触发摘要”升级为“上下文窗口可控”的短期记忆管理。

任务项：

1. 引入 token 估算；
2. 设计 soft threshold / hard threshold；
3. 保留最近原始消息 + 历史摘要；
4. 摘要中强制保留风险、已给建议、用户反馈；
5. 在 Prompt 中优先使用压缩后的结构化上下文。

涉及文件：

- `backend/conversation_manager.py`
- `backend/agent_orchestrator.py`
- `requirements.txt`

依赖建议：

1. `tiktoken`

验收标准：

1. 长对话下上下文长度可预测；
2. 摘要保留关键建议与反馈；
3. 上下文压缩后仍能保持连续性；
4. 不再只依赖“超过 5 轮就压缩”的固定规则。

##### P1-3 相关长期记忆检索

目标：

不是把全部长期记忆注入 Prompt，而是按当前问题检索相关画像和历史事件。

任务项：

1. 增加与当前输入相关的长期记忆筛选；
2. 优先命中相同压力来源、相同主题、相似历史事件；
3. 引入简单时间衰减或相关性排序；
4. 让推荐系统和 Prompt 共享这部分检索结果。

涉及文件：

- `backend/agent_tools.py`
- `backend/agent_orchestrator.py`
- `backend/database.py`

验收标准：

1. 当前是“面试焦虑”时优先取回面试相关历史；
2. 无关记忆不再机械进入 Prompt；
3. 记忆检索结果能同时服务推荐和回复生成。

---

#### P2：补推荐系统设计缺口

目标：把当前推荐骨架继续向文档目标收拢，但放在主链路正确性之后。

##### P2-1 语义检索从轻量版升级到 FAISS

目标：

把当前“稀疏向量近似”升级到文档定义的 `FAISS` 语义检索实现。

任务项：

1. 选定 embedding 方案；
2. 建立内容向量索引；
3. 引入 FAISS 检索；
4. 与 BM25 继续用 RRF 融合；
5. 保留当前轻量实现作为降级路径或开发模式。

涉及文件：

- `backend/vector_retriever.py`
- `backend/hybrid_retriever.py`
- `backend/content_recommender.py`
- `requirements.txt`

验收标准：

1. 语义近似召回能力强于当前轻量版本；
2. FAISS 结果可被 RRF 正常融合；
3. 检索结果仍能输出来源说明。

##### P2-2 推荐日志评估与可观测性

目标：

让推荐系统能够被量化评估，而不是只凭主观体验判断。

任务项：

1. 定义推荐日志结构；
2. 落盘触发原因、素材 ID、风险等级、推荐类型；
3. 统计接受率、负反馈率、重复推荐率；
4. 为高风险误推荐设置专门检查指标。

涉及文件：

- `backend/database.py`
- `backend/api_endpoints.py`
- `backend/content_recommender.py`
- `tests/`

验收标准：

1. 每次推荐有日志可追；
2. 能统计推荐触发质量；
3. 高风险误推荐可以被自动发现。

---

### 14.3 推荐执行顺序

基于当前现状，建议后续严格按以下顺序推进：

1. `P0-1 高风险安全响应链路`
2. `P0-2 推荐日志与冷却状态持久化`
3. `P0-3 记忆进入 Prompt 主链路`
4. `P1-1 MemoryGate`
5. `P1-2 token 驱动短期记忆`
6. `P1-3 相关长期记忆检索`
7. `P2-1 FAISS 升级`
8. `P2-2 推荐评估与可观测性`

不建议先做：

1. 更复杂的前端展示；
2. 更激进的 AI rerank 增强；
3. 额外的新推荐素材类型；
4. 多 Agent 扩展。

原因是这些工作都建立在主链路正确性、记忆可用性和安全边界稳定之上。

---

### 14.4 当前建议的最小下一阶段范围

如果按“先做最关键、每完成一块先测试”的方式推进，下一阶段建议只做一个最小闭环：

1. 高风险时切到独立安全响应 Prompt；
2. 普通推荐链路在 high risk 下彻底短路；
3. 补对应最小测试：
   - high risk 不触发普通推荐；
   - high risk 回复使用安全响应策略；
   - high risk 事件可记录日志。

这样做的好处是：

1. 直接补上当前最大行为缺口；
2. 对后续记忆与推荐改造影响最小；
3. 测试边界清晰，适合作为下一步开发入口。
