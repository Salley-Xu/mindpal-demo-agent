# 压力管理 Agent 推荐系统设计文档

## 1. 模块概述

推荐系统是压力管理 Agent 中的核心模块之一。它的目标是在合适的时机，为用户提供合适的压力管理内容，例如呼吸练习、任务拆分方法、情绪记录模板、认知重构建议、睡眠改善方法、面试焦虑应对方案和高风险求助建议。

该推荐系统不是传统意义上面向商品或内容消费的推荐系统，而是一个状态感知的干预推荐模块。它不只考虑用户当前说了什么，还会结合用户当前情绪、风险等级、长期偏好、多轮对话趋势和历史接受度，决定是否推荐、推荐什么以及以什么形式推荐。

推荐系统整体由以下部分组成：

1. 推荐素材库；
2. 查询构造模块；
3. FAISS 语义检索；
4. BM25 关键词检索；
5. RRF 融合排序；
6. Rerank 重排模块；
7. 推荐门控模块；
8. 软推荐/硬推荐输出策略；
9. 推荐日志与反馈更新模块。

---

## 2. 设计目标

推荐系统需要实现以下目标：

1. 在用户需要帮助时主动提供压力管理建议；
2. 避免每轮都推荐，减少打扰；
3. 根据情绪状态选择合适的推荐内容；
4. 根据风险等级控制推荐边界；
5. 根据用户历史偏好提升推荐接受率；
6. 结合语义检索和关键词检索提升召回质量；
7. 使用重排机制过滤不适合当前场景的内容；
8. 支持普通压力场景的软推荐；
9. 支持明确求助场景的硬推荐；
10. 在高风险场景下切换到安全响应，而不是普通推荐。

推荐系统的关键不是“多推荐”，而是“推荐得及时、自然、具体且安全”。

---

## 3. 推荐素材库设计

推荐素材库是 RAG 推荐的基础。素材库中的内容需要结构化存储，而不是简单保存长文本。

每条推荐素材建议包含以下字段：

```json
{
  "item_id": "breathing_001",
  "title": "3分钟呼吸放松练习",
  "category": "breathing",
  "emotion_type": ["anxiety", "stress"],
  "risk_level": ["low", "medium"],
  "tags": ["焦虑", "压力", "快速缓解", "呼吸"],
  "content": "吸气4秒，屏息2秒，呼气6秒，重复8轮。",
  "recommend_type": "hard",
  "actionability": 0.95,
  "priority": 0.8,
  "contraindications": ["high_risk_crisis"]
}
```

字段说明：

| 字段                | 含义          |
| ----------------- | ----------- |
| item_id           | 推荐素材唯一 ID   |
| title             | 素材标题        |
| category          | 推荐类别        |
| emotion_type      | 适用情绪类型      |
| risk_level        | 适用风险等级      |
| tags              | 关键词标签       |
| content           | 推荐正文        |
| recommend_type    | soft 或 hard |
| actionability     | 可执行程度       |
| priority          | 人工或系统设置的优先级 |
| contraindications | 禁用场景        |

推荐素材类型包括：

1. breathing：呼吸放松；
2. mindfulness：正念练习；
3. planning：任务拆分与计划；
4. sleep：睡眠改善；
5. cognitive_reframing：认知重构；
6. emotion_record：情绪记录；
7. interview_anxiety：面试焦虑应对；
8. study_pressure：学业压力管理；
9. relationship_stress：人际压力应对；
10. crisis_support：高风险求助建议。

---

## 4. 推荐门控机制

推荐门控用于判断当前轮是否应该触发推荐。它是推荐系统最关键的控制模块之一。

如果没有门控机制，系统容易出现两个问题：

1. 用户只是想倾诉，系统却频繁推方法，显得生硬；
2. 用户已经处于高风险状态，系统仍然给普通建议，存在安全问题。

因此推荐门控需要综合判断用户状态。

---

## 5. 推荐门控输入

推荐门控输入包括：

1. 当前用户输入；
2. 情绪类型；
3. 情绪强度；
4. 风险等级；
5. 用户意图；
6. 多轮负面趋势；
7. 长期用户偏好；
8. 历史推荐接受度；
9. 最近几轮是否已经推荐；
10. 当前对话是否适合插入建议。

结构化输入示例：

```json
{
  "emotion_type": "anxiety",
  "emotion_intensity": 0.78,
  "risk_level": "medium",
  "user_intent": "seeking_help",
  "negative_trend": true,
  "preferred_support_style": "actionable",
  "recent_recommendation_count": 1,
  "last_recommendation_turn": 2
}
```

---

## 6. 推荐评分设计

推荐门控可以使用规则打分方式实现。

推荐分数设计如下：

```text
recommend_score =
    w1 * emotion_intensity
  + w2 * risk_score
  + w3 * intent_score
  + w4 * negative_trend_score
  + w5 * preference_score
  - w6 * cooldown_penalty
```

各因子的含义如下：

| 因子                   | 含义         |
| -------------------- | ---------- |
| emotion_intensity    | 当前情绪强度     |
| risk_score           | 风险等级对应分数   |
| intent_score         | 用户是否明确求助   |
| negative_trend_score | 是否连续多轮负面   |
| preference_score     | 用户是否偏好具体建议 |
| cooldown_penalty     | 最近是否已经推荐过  |

风险等级可以映射为：

```text
low    -> 0.2
medium -> 0.6
high   -> 直接进入安全响应，不走普通推荐
```

用户意图可以映射为：

```text
venting_only     -> 0.2
seeking_support  -> 0.5
seeking_solution -> 0.9
```

触发逻辑示例：

```python
if risk_level == "high":
    route_to_safety_response()
elif recommend_score >= threshold:
    trigger_recommendation()
else:
    normal_support_response()
```

---

## 7. 冷却机制设计

冷却机制用于避免推荐过于频繁。

可以设置以下规则：

1. 最近 3 轮内已经推荐过，则降低推荐分数；
2. 用户明确拒绝某类建议，则短期内不再推荐该类型；
3. 如果用户正在明显倾诉，优先共情，不立即硬推荐；
4. 如果用户连续多轮表达无助感，可以降低触发阈值；
5. 如果用户明确说“我该怎么办”，直接提高推荐分数；
6. 高风险场景不使用普通冷却机制，而是进入安全响应。

冷却惩罚示例：

```python
if turns_since_last_recommendation <= 2:
    cooldown_penalty = 0.4
elif turns_since_last_recommendation <= 5:
    cooldown_penalty = 0.2
else:
    cooldown_penalty = 0.0
```

---

## 8. 查询构造模块

当推荐门控判断需要推荐时，系统会构造用于检索的查询。

查询不只使用用户原始输入，还可以构造多路查询。

例如，用户输入：

```text
我感觉事情太多了，完全不知道从哪里开始。
```

系统可以构造：

```text
Query 1: 我感觉事情太多了，完全不知道从哪里开始
Query 2: 焦虑 任务过载 缺少掌控感
Query 3: 学业压力 计划制定 行动建议
Query 4: 用户需要具体可执行的方法
```

多路查询的作用是提高召回覆盖率。用户的自然表达可能比较模糊，而情绪增强查询、压力来源查询和意图查询可以帮助系统召回更合适的素材。

查询构造输入包括：

1. 原始用户输入；
2. 情绪识别结果；
3. 压力来源；
4. 用户意图；
5. 长期偏好；
6. 当前风险等级。

---

## 9. FAISS 语义检索

FAISS 用于向量语义检索。

推荐素材库中的每条素材会被编码为向量，并存入 FAISS 索引。用户查询同样会被编码为向量，然后与素材向量计算相似度，召回语义相关内容。

FAISS 适合处理以下情况：

1. 用户表达和素材文本不完全匹配，但语义相近；
2. 用户使用口语化表达；
3. 用户没有说出明确关键词；
4. 需要根据上下文语义召回建议。

例如：

用户输入：

```text
我脑子里很乱，感觉完全控制不住。
```

FAISS 可能召回：

1. 情绪稳定练习；
2. 任务拆分方法；
3. 焦虑缓解建议；
4. 呼吸放松练习。

---

## 10. BM25 关键词检索

BM25 用于关键词检索，弥补纯语义检索在精确匹配上的不足。

BM25 适合处理以下情况：

1. 用户出现明确关键词；
2. 素材标签与用户输入高度匹配；
3. 需要匹配具体场景，如“面试”“睡不着”“拖延”；
4. 需要提高可解释性。

例如：

用户输入：

```text
我明天面试，今晚很焦虑睡不着。
```

BM25 可以准确召回包含“面试”“焦虑”“睡眠”等标签的素材。

---

## 11. RRF 融合排序

FAISS 和 BM25 会分别返回候选结果。由于两者分数尺度不同，不能直接相加，因此使用 RRF 进行融合排序。

RRF 公式如下：

```text
RRF(d) = Σ 1 / (k + rank_i(d))
```

其中：

1. d 表示候选文档；
2. rank_i(d) 表示文档 d 在第 i 个检索器中的排名；
3. k 是平滑参数，通常可以取 60。

RRF 的优点是：

1. 不依赖不同检索器的原始分数；
2. 实现简单；
3. 对多路召回结果稳定；
4. 能兼顾语义匹配和关键词匹配。

融合流程如下：

```text
多路查询
  ↓
FAISS TopK 召回
  ↓
BM25 TopK 召回
  ↓
候选结果合并
  ↓
RRF 融合排序
  ↓
得到初步候选列表
```

---

## 12. Rerank 重排模块

RRF 只能解决检索相关性问题，但压力管理场景还需要判断推荐内容是否适合当前用户状态。因此需要 Rerank 模块进行二次排序。

Rerank 考虑以下因子：

1. 检索相关性；
2. 情绪匹配度；
3. 风险等级匹配度；
4. 用户偏好匹配度；
5. 推荐内容可执行性；
6. 最近是否重复推荐；
7. 是否存在禁用场景；
8. 推荐类型是否适合当前轮对话。

最终分数可以设计为：

```text
final_score =
    0.35 * retrieval_score
  + 0.20 * emotion_match_score
  + 0.15 * risk_match_score
  + 0.15 * preference_match_score
  + 0.10 * actionability_score
  - 0.05 * repetition_penalty
```

如果素材存在禁用场景，例如 contraindications 包含 high_risk_crisis，而当前用户为 high risk，则该素材直接过滤。

---

## 13. 软推荐与硬推荐

推荐系统输出分为软推荐和硬推荐。

### 13.1 软推荐

软推荐是将建议自然融入对话，不以强制工具形式出现。

示例：

```text
你现在的状态更像是任务过载带来的焦虑。
可以先不用急着解决所有事情，先把任务分成三类：
今天必须完成、可以推迟、可以放弃。
先从第一类里最小的一件事开始，会更容易恢复掌控感。
```

软推荐适合：

1. 普通压力；
2. 轻中度焦虑；
3. 用户正在倾诉；
4. 用户没有明确要求工具；
5. 需要保持对话自然性。

### 13.2 硬推荐

硬推荐是明确给出一个方法、练习或步骤。

示例：

```text
我建议你现在做一个 3 分钟呼吸练习：
1. 吸气 4 秒；
2. 屏息 2 秒；
3. 呼气 6 秒；
4. 重复 8 轮。
```

硬推荐适合：

1. 用户明确说“我该怎么办”；
2. 情绪强度较高；
3. 用户需要立即缓解；
4. 多轮负面情绪持续出现；
5. 用户偏好具体行动建议。

---

## 14. 推荐日志与反馈更新

系统需要记录推荐日志，用于后续优化。

推荐日志结构如下：

```json
{
  "log_id": "rec_001",
  "user_id": "user_001",
  "session_id": "session_001",
  "item_id": "planning_003",
  "recommend_type": "soft",
  "emotion_type": "anxiety",
  "risk_level": "medium",
  "accepted": true,
  "created_at": "2026-06-01T12:10:00"
}
```

反馈来源包括：

1. 用户明确表示有用；
2. 用户继续围绕建议展开；
3. 用户拒绝建议；
4. 用户无视建议；
5. 用户表达“不要再说这个”。

反馈会反向更新长期用户画像，例如：

```json
{
  "recommendation_feedback": {
    "breathing_exercise": "accepted",
    "mindfulness": "rejected",
    "task_planning": "preferred"
  }
}
```

---

## 15. 推荐系统评估

推荐系统可以从检索质量、触发质量和用户体验三个层面评估。

### 15.1 检索质量指标

1. Recall@K；
2. Precision@K；
3. MRR；
4. NDCG；
5. 语义召回覆盖率；
6. 关键词命中率。

### 15.2 触发质量指标

1. 推荐触发准确率；
2. 过度推荐率；
3. 漏推荐率；
4. 高风险场景误推荐率；
5. 冷却机制有效性。

### 15.3 用户体验指标

1. 用户接受率；
2. 用户继续互动率；
3. 建议执行反馈；
4. 重复推荐率；
5. 用户负反馈率。

其中，高风险场景误推荐率需要重点控制。高风险输入不应该进入普通推荐链路，而应进入安全响应链路。

---

## 16. 模块亮点

推荐系统的主要亮点包括：

1. 推荐不是无条件触发，而是由门控机制控制；
2. 同时使用 FAISS 语义检索和 BM25 关键词检索，提高召回稳定性；
3. 使用 RRF 融合不同检索器结果，避免分数尺度不一致问题；
4. 使用 Rerank 结合情绪、风险、偏好和可执行性进行二次排序；
5. 区分软推荐和硬推荐，使推荐更自然；
6. 引入冷却机制，避免过度打扰；
7. 推荐反馈会反向更新长期记忆，形成个性化闭环。

---

## 17. 总结

压力管理 Agent 的推荐系统本质上是一个状态感知的 RAG 推荐模块。它不是单纯根据用户输入做文本检索，而是综合考虑用户当前情绪、风险等级、历史偏好和多轮对话趋势，决定是否推荐、推荐什么以及如何推荐。

其完整流程可以概括为：

```text
用户输入 + 情绪状态 + 长期记忆
  ↓
推荐门控判断
  ↓
多路查询构造
  ↓
FAISS 语义召回
  ↓
BM25 关键词召回
  ↓
RRF 融合排序
  ↓
状态感知 Rerank
  ↓
软推荐 / 硬推荐输出
  ↓
推荐反馈写入长期记忆
```

该模块体现了 Agent 项目中“检索增强生成”和“推荐决策控制”的核心能力。
