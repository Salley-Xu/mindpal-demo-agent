# 压力管理 Agent 系统架构设计文档

## 1. 项目概述

本项目是一个面向压力管理与情绪支持场景的对话式 Agent 系统。系统目标不是简单地调用大模型完成单轮聊天，而是构建一个具备情绪理解、风险识别、长期记忆、个性化推荐和安全响应能力的智能体。

在传统大模型对话系统中，模型主要依赖当前输入和有限上下文进行回复，容易出现长对话遗忘、个性化不足、建议重复、风险场景处理不稳定等问题。本项目通过模块化 Agent 架构，将情绪识别、记忆管理、推荐决策、RAG 检索和安全控制拆分为独立模块，由主 Agent 统一调度，从而提升系统的可控性、连续性和可解释性。

系统整体可以概括为：

```text
用户输入
  ↓
主 Agent 调度
  ↓
情绪识别 / 风险评估 / 记忆检索 / 推荐判断
  ↓
Prompt 组装
  ↓
大模型生成回复
  ↓
记忆更新
```

项目核心价值在于：让大模型从“被动聊天工具”升级为“能够持续理解用户状态、维护长期画像、适时提供干预”的个性化压力管理 Agent。

---

## 2. 系统设计目标

系统设计目标主要包括四类。

第一，提升长对话连续性。系统需要在多轮对话中持续追踪用户的问题背景、情绪状态、已给出的建议和用户反馈，避免每轮都重新理解用户。

第二，提升个性化能力。系统需要维护跨会话用户画像，包括用户常见压力来源、偏好的支持方式、历史情绪事件和对不同建议类型的接受度。

第三，提升推荐可控性。系统不能每轮都机械推荐，而是需要根据用户情绪强度、风险等级、用户意图、多轮状态和冷却机制判断是否触发推荐。

第四，提升安全性。面对自伤、自杀、极端绝望等高风险表达时，系统应优先进入安全响应流程，而不是继续执行普通建议或推荐逻辑。

---

## 3. 总体架构

系统采用“主 Agent + 多子模块”的架构。主 Agent 负责调度，子模块负责完成具体任务。

核心模块包括：

1. 主 Agent；
2. 情绪识别模块；
3. 风险评估模块；
4. 短期记忆模块；
5. 长期记忆模块；
6. 推荐门控模块；
7. RAG 推荐模块；
8. Prompt 组装模块；
9. 大模型生成模块；
10. 记忆更新模块；
11. 安全响应模块。

整体架构如下：

```text
                    ┌──────────────────┐
                    │    用户输入       │
                    └────────┬─────────┘
                             ↓
                    ┌──────────────────┐
                    │    主 Agent       │
                    └────────┬─────────┘
                             ↓
        ┌────────────────────┼────────────────────┐
        ↓                    ↓                    ↓
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ 情绪识别模块 │     │ 风险评估模块 │     │ 记忆检索模块 │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       ↓                    ↓                    ↓
       └────────────────────┼────────────────────┘
                            ↓
                  ┌──────────────────┐
                  │  推荐门控模块     │
                  └────────┬─────────┘
                           ↓
              ┌────────────────────────┐
              │ 是否触发 RAG 推荐       │
              └────────┬───────────────┘
                       ↓
              ┌────────────────────────┐
              │ Prompt 组装模块         │
              └────────┬───────────────┘
                       ↓
              ┌────────────────────────┐
              │ 大模型生成模块          │
              └────────┬───────────────┘
                       ↓
              ┌────────────────────────┐
              │ 回复输出 + 记忆更新     │
              └────────────────────────┘
```

---

## 4. 主 Agent 设计

主 Agent 是系统的调度中心，负责控制每一轮对话的信息流和模块调用顺序。

主 Agent 的职责包括：

1. 接收用户当前输入；
2. 获取当前 Session 的短期上下文；
3. 检索长期用户画像；
4. 调用情绪识别模块；
5. 调用风险评估模块；
6. 调用推荐门控模块；
7. 根据门控结果决定是否调用 RAG 推荐；
8. 整合所有状态信息并组装 Prompt；
9. 调用大模型生成回复；
10. 将本轮对话结果写入短期记忆；
11. 判断是否更新长期记忆。

主 Agent 的核心伪代码如下：

```python
def agent_turn(user_input, user_id, session_id):
    short_context = short_memory.get_context(session_id)

    long_memory = long_memory_store.retrieve(
        user_id=user_id,
        query=user_input
    )

    emotion_state = emotion_analyzer.analyze(
        user_input=user_input,
        context=short_context
    )

    risk_state = risk_evaluator.evaluate(
        user_input=user_input,
        emotion_state=emotion_state,
        long_memory=long_memory
    )

    recommend_decision = recommend_gate.decide(
        user_input=user_input,
        emotion_state=emotion_state,
        risk_state=risk_state,
        long_memory=long_memory,
        short_context=short_context
    )

    if risk_state.level == "high":
        prompt = safety_prompt_builder.build(
            user_input=user_input,
            short_context=short_context,
            long_memory=long_memory,
            risk_state=risk_state
        )
    else:
        if recommend_decision.should_recommend:
            recommend_items = recommender.retrieve(
                user_input=user_input,
                emotion_state=emotion_state,
                risk_state=risk_state,
                user_profile=long_memory
            )
        else:
            recommend_items = []

        prompt = prompt_builder.build(
            user_input=user_input,
            short_context=short_context,
            long_memory=long_memory,
            emotion_state=emotion_state,
            risk_state=risk_state,
            recommend_items=recommend_items
        )

    response = llm.generate(prompt)

    short_memory.append(session_id, user_input, response)

    memory_updater.update(
        user_id=user_id,
        user_input=user_input,
        response=response,
        emotion_state=emotion_state,
        risk_state=risk_state
    )

    return response
```

---

## 5. 情绪识别模块

情绪识别模块用于判断用户当前输入中的主要情绪、情绪强度、压力来源和用户意图。

输入包括：

1. 当前用户输入；
2. 最近几轮对话；
3. 历史情绪摘要；
4. 可选的长期用户画像。

输出为结构化结果：

```json
{
  "emotion_type": "anxiety",
  "emotion_intensity": 0.76,
  "stress_source": "学业/求职压力",
  "user_intent": "seeking_help",
  "negative_trend": true,
  "confidence": 0.84
}
```

其中，emotion_type 表示主要情绪类型，emotion_intensity 表示情绪强度，stress_source 表示压力来源，user_intent 表示用户是倾诉、求助还是寻求计划，negative_trend 表示是否存在多轮负面趋势。

情绪识别结果会影响后续两件事：

第一，影响大模型回复风格。轻度焦虑可以使用普通支持性回复，高强度焦虑需要更具体、更稳定的安抚和行动建议。

第二，影响推荐触发逻辑。情绪强度越高、负面趋势越明显，推荐触发概率越高。

---

## 6. 风险评估模块

风险评估模块用于识别用户是否处于高风险状态。

系统将风险等级分为三类：

| 风险等级   | 描述               | 系统策略          |
| ------ | ---------------- | ------------- |
| low    | 普通压力、轻度焦虑、一般倾诉   | 正常回复，可触发软推荐   |
| medium | 情绪强烈、持续负面、自我否定明显 | 加强支持，提供具体行动建议 |
| high   | 自伤、自杀、极端绝望等表达    | 进入安全响应流程      |

风险评估综合考虑：

1. 当前用户输入；
2. 情绪强度；
3. 风险关键词；
4. 多轮负面趋势；
5. 历史风险等级；
6. 用户是否表达明确危险计划。

当风险等级为 high 时，系统优先进入安全响应流程，跳过普通推荐系统。

高风险响应原则包括：

1. 先承接情绪；
2. 避免批评和说教；
3. 不提供任何可能造成伤害的细节；
4. 鼓励联系现实中的可信任对象；
5. 建议寻求专业帮助或当地紧急服务；
6. 将本次风险事件写入长期记忆或风险日志。

---

## 7. Prompt 组装模块

Prompt 组装模块负责把多个模块的输出整理成大模型可理解的上下文。

最终 Prompt 通常包括：

1. 系统角色设定；
2. 安全响应规则；
3. 当前用户输入；
4. 最近几轮对话；
5. Session 摘要；
6. 长期用户画像；
7. 当前情绪状态；
8. 当前风险等级；
9. 推荐素材；
10. 回复风格要求。

Prompt 模板示例：

```text
你是一个压力管理与情绪支持 Agent。你的目标是理解用户当前状态，
提供具体、温和、可执行的支持建议。

【安全规则】
- 如果用户存在高风险表达，优先进行安全回应。
- 不提供可能造成伤害的具体方法。
- 鼓励用户联系可信任的人或专业资源。

【用户长期画像】
{long_memory}

【当前会话摘要】
{session_summary}

【最近对话】
{recent_turns}

【当前情绪状态】
{emotion_state}

【风险等级】
{risk_state}

【推荐素材】
{recommend_items}

【当前用户输入】
{user_input}

请生成一段自然、具体、有同理心的回复。
```

Prompt 中的信息优先级设计为：

```text
安全规则 > 当前用户输入 > 当前风险状态 > 当前情绪状态 > 最近上下文 > 长期记忆 > 推荐素材
```

这样可以避免长期记忆或推荐素材覆盖当前用户真实表达。

---

## 8. 每轮对话数据流

每轮对话的数据流可以分为五层。

### 8.1 输入层

输入层包括：

1. 当前用户输入；
2. user_id；
3. session_id；
4. 最近对话历史。

### 8.2 状态层

状态层生成中间状态：

1. emotion_state；
2. risk_state；
3. user_profile；
4. session_summary；
5. recommendation_context。

### 8.3 决策层

决策层判断：

1. 是否进入安全响应；
2. 是否触发推荐；
3. 使用软推荐还是硬推荐；
4. 需要注入哪些长期记忆；
5. 是否调用 RAG 推荐模块。

### 8.4 生成层

生成层将用户输入、上下文、记忆、风险状态和推荐素材组装为 Prompt，并调用大模型生成最终回复。

### 8.5 更新层

更新层完成：

1. 追加当前对话到短期记忆；
2. 判断是否触发摘要压缩；
3. 抽取长期记忆候选；
4. 更新用户画像；
5. 写入情绪事件；
6. 更新推荐反馈记录。

---

## 9. 安全响应链路

安全响应链路是系统中的高优先级路径。

当风险评估模块判断 risk_level 为 high 时，系统直接进入安全响应模式：

```text
用户高风险输入
  ↓
风险评估模块标记 high
  ↓
跳过普通推荐
  ↓
构建安全响应 Prompt
  ↓
生成支持性、安全性回复
  ↓
写入风险事件
  ↓
提高后续对话监控等级
```

安全响应链路与普通回复链路的区别在于：

1. 不再使用普通压力管理建议作为主要内容；
2. 不做一般性效率建议或情绪练习推荐；
3. 优先稳定用户情绪；
4. 引导用户获得现实世界支持；
5. 避免输出任何危险细节。

---

## 10. 工程解耦设计

系统采用模块化设计，每个模块只负责一个明确任务。

| 模块              | 输入              | 输出        |
| --------------- | --------------- | --------- |
| EmotionAnalyzer | 用户输入、短期上下文      | 情绪状态      |
| RiskEvaluator   | 用户输入、情绪状态、长期记忆  | 风险等级      |
| ShortMemory     | session_id、对话历史 | 最近上下文、摘要  |
| LongMemory      | user_id、query   | 用户画像、历史事件 |
| RecommendGate   | 情绪、风险、意图、偏好     | 是否推荐      |
| Recommender     | query、用户状态      | 推荐素材      |
| PromptBuilder   | 所有状态信息          | 最终 Prompt |
| MemoryUpdater   | 本轮对话和状态         | 记忆更新结果    |

模块化设计带来的好处是：

1. 便于单独测试；
2. 便于替换具体实现；
3. 便于调试每轮对话的信息流；
4. 便于定位错误来源；
5. 便于后续引入多 Agent 协作。

---

## 11. 系统评估

系统评估包括模块级评估和端到端评估。

模块级评估包括：

1. 情绪识别准确率；
2. 情绪分类 Macro-F1；
3. 风险识别召回率；
4. 推荐触发准确率；
5. RAG 检索 Recall@K；
6. 长期记忆命中率。

端到端评估包括：

1. 回复是否理解用户情绪；
2. 建议是否具体可执行；
3. 是否符合用户偏好；
4. 是否避免重复；
5. 是否在高风险场景下安全；
6. 是否保持长对话连续性。

其中，高风险识别召回率是最重要的安全指标，宁可适度提高敏感性，也要避免漏判严重风险。

---

## 12. 项目亮点

本系统的主要亮点包括：

1. 使用主 Agent 调度多个功能模块，而不是完全依赖单次 LLM 调用；
2. 将用户情绪、风险、偏好等信息结构化，提升系统可解释性；
3. 通过短期记忆和长期记忆解决长对话连续性和跨会话个性化问题；
4. 通过推荐门控控制推荐触发时机，避免过度推荐；
5. 通过 RAG 推荐模块为压力管理建议提供稳定素材来源；
6. 通过安全响应链路处理高风险用户输入；
7. 系统具有良好的模块扩展性，可继续引入 Reflection、多 Agent 协作和 Learning to Rank 推荐排序。

---

## 13. 总结

压力管理 Agent 系统的核心设计思想是：先理解用户状态，再进行回复生成。系统通过情绪识别、风险评估、记忆检索和推荐门控，将原本隐式依赖大模型完成的任务拆分为多个可控模块，使对话生成过程更加稳定、可解释和可优化。

整体上，该系统可以被定位为一个具备长期记忆、个性化推荐和安全响应能力的情绪支持 Agent 原型，适合用于展示大模型 Agent 架构设计、上下文工程、RAG 推荐系统和安全策略设计能力。
