# 压力管理 Agent 项目现状审查报告

## 1. 审查范围

本报告基于以下信息进行整理：

1. 统一后的设计目标，来源于：
   - `压力管理 Agent 系统架构设计文档.md`
   - `压力管理 Agent 推荐系统设计文档.md`
   - `压力管理 Agent 记忆系统设计文档.md`
   - `技术文档.md`
2. 当前项目代码实现：
   - `backend/`
   - `frontend/`
   - `tests/`
3. 已有自动化测试与最小复现结果。

本报告关注两类内容：

1. 当前项目已经落地了哪些关键能力；
2. 当前项目与设计目标相比还存在哪些真实缺口或风险。

---

## 2. 总体结论

当前项目已经从“聊天原型”升级为“具备状态驱动骨架的压力管理 Agent”，核心主链路能力已经基本成型，主要体现在：

1. 已有主 Agent 编排；
2. 已有结构化情绪状态、风险状态、会话摘要和推荐决策；
3. 已有高风险短路安全响应；
4. 已有推荐门控、冷却状态和推荐反馈闭环；
5. 已有短期记忆压缩、长期画像、相关长期记忆注入 Prompt；
6. 已有混合检索与重排骨架；
7. 已有一批围绕主链路的自动化回归测试。

基于本轮对照审查与后续修复，前面识别出的 4 个高优先级问题已经全部完成收口：

1. 风险评估已接入长期风险画像和历史高风险事件；
2. 会话摘要接口已恢复只读语义，不再创建“幻影会话”；
3. 调试视图已兼容并优先展示当前风险结构；
4. 直接推荐接口已补齐长期画像透传，和聊天链路保持一致。

当前项目已从“补主链路缺口”进入“继续清理兼容层、更新文档和增强验证边界”的阶段。

---

## 3. 已落地能力对齐情况

### 3.1 系统架构

当前系统已经实现以下架构能力：

1. `FastAPI + Streamlit` 的前后端分离结构；
2. `agent_orchestrator` 负责主链路调度；
3. 情绪识别、风险评估、推荐门控、推荐检索、记忆更新都已模块化；
4. 高风险响应有独立分支，不再继续普通 Agent loop。

### 3.2 记忆系统

当前记忆系统与设计文档对齐较好的部分：

1. Session 级短期记忆已存在；
2. 历史上下文压缩已经改为 token 驱动；
3. 长期画像支持 `preferred_support_style / avoid_style / main_stress_sources / recommendation_feedback`；
4. 已实现 MemoryGate 风格的“重复出现或显式表达”写入逻辑；
5. Prompt 中已注入会话摘要、压缩上下文、长期画像和相关长期记忆。

### 3.3 推荐系统

推荐系统已落地的核心能力：

1. 推荐门控支持 `none / soft / hard`；
2. 高风险场景会短路普通推荐；
3. 推荐日志和会话级反馈已持久化；
4. 推荐反馈可以回写长期画像；
5. 推荐检索支持多路查询、混合检索和二次重排；
6. 前端支持推荐反馈按钮与检索元数据展示。

### 3.4 测试体系

当前已有较完整的主链路回归测试，包括：

1. 高风险安全路由测试；
2. 推荐持久化测试；
3. Prompt 记忆注入测试；
4. MemoryGate 测试；
5. token 驱动短期记忆测试；
6. 推荐门控测试；
7. 集成回归测试；
8. 调试视图测试。

---

## 4. 关键问题处理状态

以下问题为本轮审查中识别出的关键缺口及其当前处理状态。

### 4.1 已修复：风险评估接入长期风险画像与历史高风险

#### 修复结果

`agent_orchestrator` 已在风险评估前前移加载用户画像和近期情绪事件，并将以下上下文真正传入 `risk_evaluator.evaluate(...)`：

1. `long_term_risk_level`
2. `historical_high_risk_count`

#### 当前状态

这意味着设计文档中“风险评估应综合长期风险等级和历史高风险事件”的目标已经落实到主链路，当前表达较隐晦但历史风险较高的情况会被抬升风险分数。

#### 验证

已补回归测试，覆盖：

1. 长期风险上下文会显著抬升风险分数；
2. `run_agent` 预检确实透传长期风险等级和历史高风险次数。

---

### 4.2 已修复：会话摘要接口不再创建幻影会话

#### 修复结果

`GET /session/{user_id}/{session_id}/summary` 现在会先做只读存在性检查，不存在时直接返回 `404`，不会再因为查询动作隐式创建空会话。

#### 当前状态

摘要接口的语义已经和“只读查询”保持一致，会话统计和后续删除逻辑也不再受到幻影数据污染。

#### 验证

已补接口测试，锁定“不存在会话摘要返回 `404` 且不创建会话”的行为。

---

### 4.3 已修复：调试视图兼容并优先读取当前风险结构

#### 修复结果

调试视图现在会优先读取：

1. `risk_score`
2. `message`
3. `triggers`

同时继续兼容旧字段：

1. `score`
2. `reason`
3. `signals`

#### 当前状态

真实风险元数据已经能够在调试面板中完整展示，同时旧测试和旧数据不会立刻失效。

#### 验证

已补回归测试，覆盖：

1. 使用当前 `RiskState` 结构输入时调试快照正确展示；
2. 使用旧字段输入时兼容映射仍然有效。

---

### 4.4 已修复：直接推荐接口已带入完整长期画像

#### 修复结果

`/content/recommend` 现在会把以下长期画像字段完整传给推荐器：

1. `preferred_support_style`
2. `avoid_style`
3. `main_stress_sources`
4. `recommendation_feedback`

以及原本已传入的基础偏好字段。

#### 当前状态

直接推荐接口与聊天主链路的画像输入结构已经基本对齐，个性化推荐能力不再因调用入口不同而明显降级。

#### 验证

已补接口级回归测试，锁定推荐器可以收到完整长期画像透传。

---

## 5. 次要一致性问题

以下问题不是当前阻塞主链路的问题，但建议纳入后续整理：

1. 文档和零散测试里仍保留少量旧风险等级示例，主要用于兼容性说明，建议逐步收敛表达；
2. 数据库映射和日志统计中仍保留旧等级到新等级的兼容转换，当前属于合理保底层；
3. 前端健康检查提示语和个别版本文案仍略偏旧，可在下一轮做统一清理。

---

## 6. 测试与验证现状

当前项目的验证基础总体较好，已覆盖以下方向：

1. 高风险安全短路；
2. 推荐门控与冷却；
3. 推荐反馈闭环；
4. 记忆写入门控；
5. token 驱动短期记忆；
6. Prompt 中的状态与记忆注入；
7. 集成回归链路。

但从更高标准看，仍可继续加强以下验证：

1. 基于真实数据库持久化的端到端联调；
2. 风险日志统计与旧等级兼容映射的跨版本回归；
3. 前端页面级风险提示与推荐展示的端到端自动化验证；
4. 更长会话、多轮推荐冷却和记忆压缩同时触发时的组合场景测试。

---

## 7. 按设计文档视角的现状判断

### 7.1 已基本达成

以下目标可以认为已经基本达成：

1. 状态驱动的主 Agent 骨架；
2. 结构化情绪、风险、记忆、推荐状态；
3. 高风险独立安全响应；
4. 记忆进入 Prompt 主链路；
5. 推荐反馈闭环；
6. 混合检索与重排的第一版实现；
7. 一批面向主链路的回归测试。

### 7.2 已完成本轮关键收口

以下目标已在本轮完成关键收口：

1. 风险评估中的历史风险建模已进入主链路；
2. 调试视图已和后端真实字段对齐，并保留旧字段兼容；
3. 直接推荐接口与聊天链路的画像输入已基本一致；
4. 只读接口与会话持久化语义已恢复一致。

### 7.3 尚可继续增强

以下方向属于下一阶段增强项：

1. 更正式的向量索引与检索评估；
2. 更细粒度的推荐日志分析；
3. 更丰富的风险事件记忆；
4. 更完整的端到端评测数据。

---

## 8. 建议优先级

建议下一轮工作按以下顺序推进：

1. 更新统一技术文档和审查报告中的“当前状态”，避免继续引用过期问题描述；
2. 收敛文档、测试和注释中的旧风险等级示例；
3. 增加基于真实持久化的端到端回归；
4. 继续补前端页面级自动化验证；
5. 视需要再清理兼容层中的旧命名。

---

## 9. 最终结论

当前项目已经具备较强的 Agent 主链路能力，整体状态明显优于早期原型，尤其在以下方面已具备展示价值：

1. 状态驱动对话；
2. 风险控制；
3. 长短期记忆；
4. 推荐门控；
5. 检索增强推荐；
6. 推荐反馈闭环；
7. 自动化回归测试。

从设计文档对照角度看，项目当前不再是“缺少大框架”，而是进入了“做一致性收口和细节补强”的阶段。后续工作的重点应从“继续堆模块”转向“修复关键语义不一致、补强测试边界、提升主链路一致性和可观测性”。
