# 压力管理 Agent 统一技术文档

## 1. 文档说明

本文档用于统一收敛以下四份文档中的设计信息：

1. `压力管理 Agent 系统架构设计文档.md`
2. `压力管理 Agent 推荐系统设计文档.md`
3. `压力管理 Agent 记忆系统设计文档.md`
4. `技术文档.md`

目标不是机械拼接，而是在保留原设计意图的基础上，整理出一份可直接用于项目沟通、实现对照和后续演进的统一版技术文档。

---

## 2. 项目定位

本项目是一个面向压力管理与情绪支持场景的对话式 Agent 系统。系统目标不是只完成单轮聊天，而是构建一个具备以下能力的状态驱动智能体：

1. 理解用户当前情绪与压力来源；
2. 识别中高风险表达并在高风险场景下优先进入安全响应；
3. 维护 Session 内短期上下文；
4. 维护跨 Session 的长期用户画像和情绪事件；
5. 根据用户状态进行推荐门控；
6. 使用检索增强推荐为用户提供更稳定的干预素材；
7. 将状态信息显式注入 Prompt，提高回复的连续性、个性化和可控性。

系统核心价值在于：让大模型从“被动聊天工具”升级为“能够持续理解用户状态、维护长期记忆、适时提供安全支持和个性化建议的压力管理 Agent”。

---

## 3. 设计目标

### 3.1 业务目标

系统设计围绕以下目标展开：

1. 提升长对话连续性；
2. 提升跨会话个性化能力；
3. 提升推荐触发的可控性；
4. 提升高风险场景下的安全性；
5. 形成推荐反馈和长期记忆的闭环。

### 3.2 工程目标

工程上强调以下能力：

1. 模块解耦；
2. 状态显式化；
3. 检索增强；
4. 可观测性；
5. 可测试性；
6. 对旧数据和旧字段的兼容性。

---

## 4. 总体架构

系统采用“主 Agent + 多子模块”的架构，主 Agent 负责调度，子模块负责产出结构化状态或候选结果。

```text
用户输入
  ↓
主 Agent 调度
  ↓
短期记忆读取 / 长期记忆检索
  ↓
情绪识别 / 风险评估 / 推荐门控
  ↓
是否触发检索增强推荐
  ↓
Prompt 组装
  ↓
大模型生成回复
  ↓
短期记忆更新 / 长期记忆更新 / 推荐反馈写回
```

核心模块包括：

1. 主 Agent
2. 情绪识别模块
3. 风险评估模块
4. 短期记忆模块
5. 长期记忆模块
6. MemoryGate 记忆写入门控
7. 推荐门控模块
8. RAG 推荐模块
9. Prompt 组装模块
10. 大模型生成模块
11. 安全响应模块

---

## 5. 主 Agent 设计

主 Agent 是系统的调度中心，负责控制每轮对话的信息流顺序。

### 5.1 主职责

每轮对话中，主 Agent 负责：

1. 接收 `user_input / user_id / session_id`；
2. 读取当前 Session 短期上下文；
3. 检索长期用户画像和相关历史记忆；
4. 调用情绪识别模块；
5. 调用风险评估模块；
6. 调用推荐门控模块；
7. 根据结果决定是否触发推荐检索；
8. 组装 Prompt；
9. 调用大模型生成回复；
10. 更新短期记忆、长期记忆与推荐反馈状态。

### 5.2 设计原则

主 Agent 的关键设计原则如下：

1. 安全优先，高风险响应高于普通推荐和普通回复；
2. 当前用户输入优先于历史记忆；
3. 当前风险状态和当前情绪状态优先于长期画像；
4. 推荐素材只能作为辅助上下文，不能盖过用户真实表达；
5. 生成前的显式状态建模优先于“全部交给 LLM 隐式判断”。

---

## 6. 状态模型

系统中的关键中间状态应保持结构化，便于模块之间复用。

### 6.1 EmotionState

用于表达当前轮的情绪状态，建议包含：

```json
{
  "current_emotion": "焦虑",
  "emotion_type": "anxiety",
  "context_emotion": "自我怀疑",
  "emotion_intensity": 0.76,
  "stress_source": "学业/求职压力",
  "user_intent": "seeking_help",
  "negative_trend": true,
  "confidence": 0.84
}
```

### 6.2 RiskState

用于表达风险评估结果，建议包含：

```json
{
  "level": "level_2",
  "legacy_level": "medium",
  "level_label": "Level 2",
  "message": "检测到高风险倾向，请先停止普通问题分析，优先确认当前安全并寻求现实支持。",
  "suggestions": ["先做一次缓慢深呼吸", "尽快联系可信任的人"],
  "triggers": ["撑不住", "绝望"],
  "risk_score": 5.0,
  "raw_score": 5.0,
  "risk_dimensions": {
    "risk_ideation": 1,
    "action_intent": 1,
    "self_control": 1
  },
  "risk_evidence": {
    "risk_ideation": ["撑不住"]
  },
  "escalation_reasons": ["score_threshold:level_2"],
  "risk_context": {
    "subject": "self"
  }
}
```

### 6.3 SessionSummary

用于表达当前 Session 的聚合状态，建议包含：

1. `conversation_stage`
2. `turn_count`
3. `primary_emotion`
4. `emotion_trend`
5. `key_concerns`
6. `current_topic`
7. `recent_intents`
8. `stress_sources`
9. `recent_risk_levels`
10. `recent_recommendation_turns`
11. `accepted_recommendations`
12. `rejected_recommendations`
13. `compressed_context`

### 6.4 RecommendationDecision

用于表达当前轮是否推荐、推荐方式和触发原因，建议包含：

1. `should_recommend`
2. `recommend_type`
3. `score`
4. `threshold`
5. `reason_codes`
6. `cooldown_remaining`

---

## 7. 情绪识别模块

情绪识别模块用于从当前输入和最近上下文中产出结构化情绪状态。

### 7.1 输入

情绪识别输入包括：

1. 当前用户输入；
2. 最近若干轮对话；
3. 会话摘要；
4. 可选的长期画像信息。

### 7.2 输出

模块输出应至少支持：

1. 情绪类别；
2. 情绪强度；
3. 压力来源；
4. 用户意图；
5. 多轮负面趋势；
6. 置信度。

### 7.3 设计要求

1. 第一阶段可以使用规则 + LLM 结构化输出；
2. 后续可引入轻量分类器优化延迟和成本；
3. 输出字段必须能直接供风险评估和推荐门控复用。

---

## 8. 风险评估与安全响应

风险评估模块优先级高于普通推荐模块。

### 8.1 风险等级

系统当前主语义采用四层风险，并保留到旧三层 `low / medium / high` 的兼容映射：

| 风险等级 | 兼容语义 | 描述 | 系统策略 |
| --- | --- | --- | --- |
| level_0 | low | 普通压力、轻度焦虑、一般倾诉 | 正常支持性回复，可触发软推荐 |
| level_1 | medium | 轻中度情绪风险、持续负面或明显崩溃 | 更谨慎安抚，优先低负担支持 |
| level_2 | medium | 高风险倾向、模糊自伤/失控风险 | 进入高风险支持模式，关闭普通推荐 |
| level_3 | high | 紧急危机风险、明确行动/计划/失控 | 强制进入危机响应链路 |

### 8.2 风险评估输入

风险评估应综合：

1. 当前用户输入；
2. 当前情绪强度；
3. 风险关键词或语义信号；
4. 多轮负面趋势；
5. 长期风险等级；
6. 历史高风险事件；
7. 是否存在明确时间性或计划性表达。

### 8.3 高风险链路

当前系统已实现双层安全门控与第三方危机求助专用分支：

1. 当 `risk_state.level == "level_2"` 时：
   - 跳过普通 Agent loop；
   - 进入高风险支持模式；
   - 关闭普通推荐，仅保留安全支持类内容兜底；
   - 优先确认当前安全、联系现实支持。
2. 当 `risk_state.level == "level_3"` 时：
   - 进入独立危机响应；
   - 关闭普通推荐与普通分析；
   - 输出短、直接、低认知负担的危机干预文本；
   - 优先引导紧急资源和立即行动。
3. 当 `risk_context.subject == "third_party"` 且 `is_help_request == true` 时：
   - 进入第三方危机求助专用支持分支；
   - 不误用用户本人危机模板；
   - 重点指导如何联系对方身边现实支持者与紧急资源。
4. 所有最终回复在返回前都必须经过独立 `output_safety_checker`。

---

## 9. 记忆系统设计

记忆系统分为短期记忆和长期记忆两层。

### 9.1 短期记忆

短期记忆解决当前 Session 内的连续性问题。

应包含：

1. 最近 N 轮原始对话；
2. 历史对话摘要；
3. 当前轮用户输入。

### 9.2 Token 监控与压缩

短期记忆应使用 token 监控机制，而不是只依赖固定轮次。

建议采用：

1. `soft threshold`：上下文达到窗口 60% 到 70% 时开始压缩较早对话；
2. `hard threshold`：达到 80% 到 90% 时强制压缩，只保留摘要和最近原始轮次。

摘要需要优先保留：

1. 当前问题背景；
2. 主要压力来源；
3. 当前情绪与趋势；
4. 已给出的关键建议；
5. 用户接受和拒绝过的建议；
6. 风险等级变化；
7. 下一轮回复必须延续的信息。

### 9.3 长期记忆

长期记忆解决跨 Session 个性化问题。

建议至少维护以下结构：

1. `preferred_support_style`
2. `avoid_style`
3. `main_stress_sources`
4. `risk_level`
5. `mood_events`
6. `recommendation_feedback`

示例：

```json
{
  "user_profile": {
    "preferred_support_style": "direct_actionable",
    "avoid_style": ["empty_comfort"],
    "main_stress_sources": ["academic_pressure", "job_interview"],
    "risk_level": "medium"
  },
  "mood_events": [
    {
      "emotion_type": "anxiety",
      "emotion_intensity": 0.76,
      "stress_source": "interview",
      "summary": "用户担心面试中项目讲不清楚。"
    }
  ],
  "recommendation_feedback": {
    "breathing_exercise": "accepted",
    "task_planning": "preferred"
  }
}
```

### 9.4 MemoryGate

长期记忆不能每轮无条件写入，必须通过记忆写入门控控制噪声。

适合写入的信息：

1. 用户明确表达的长期偏好；
2. 多次重复出现的压力来源；
3. 明显情绪事件；
4. 用户对建议的明确反馈；
5. 风险等级变化。

不适合写入的信息：

1. 临时性闲聊；
2. 无长期价值的细节；
3. 低置信度推断；
4. 与压力管理无关的噪声；
5. 不必要的敏感细节。

### 9.5 相关长期记忆检索

每轮对话不应把全部长期记忆都注入 Prompt，而应按当前问题检索“相关”长期记忆。

优先检索：

1. 相同压力来源的历史事件；
2. 相同主题或相似问题；
3. 用户偏好的支持方式；
4. 之前已接受或拒绝过的相关建议；
5. 最近的同类情绪事件。

---

## 10. 推荐系统设计

推荐系统是一个状态感知的干预推荐模块，而不是传统内容消费推荐。

### 10.1 设计目标

推荐系统需要做到：

1. 在合适的时机给出合适的建议；
2. 不要每轮都推荐；
3. 推荐触发与情绪、风险、意图、多轮趋势相关；
4. 推荐排序与长期偏好、反馈闭环相关；
5. 高风险场景下不进入普通推荐。

### 10.2 推荐素材库

每条素材建议结构化存储，建议字段包括：

1. `item_id`
2. `title`
3. `category`
4. `emotion_type`
5. `risk_level`
6. `tags`
7. `content`
8. `recommend_type`
9. `actionability`
10. `priority`
11. `contraindications`

### 10.3 推荐门控

推荐门控综合以下因素：

1. 情绪强度；
2. 风险等级；
3. 用户意图；
4. 多轮负面趋势；
5. 用户长期偏好；
6. 历史推荐接受度；
7. 最近是否已经推荐过；
8. 当前对话是否适合插入建议。

推荐分数可建模为：

```text
recommend_score =
    w1 * emotion_intensity
  + w2 * risk_score
  + w3 * intent_score
  + w4 * negative_trend_score
  + w5 * preference_score
  - w6 * cooldown_penalty
```

### 10.4 冷却机制

冷却机制用于减少频繁推荐，建议包括：

1. 最近 2 到 3 轮已推荐则显著降权；
2. 用户明确拒绝某类建议时短期内不再推荐；
3. 用户明确求助时可抬高推荐分数；
4. 高风险场景直接短路普通推荐。

### 10.5 软推荐与硬推荐

软推荐适合：

1. 普通压力；
2. 轻中度焦虑；
3. 用户仍在倾诉；
4. 需要保持对话自然性。

硬推荐适合：

1. 用户明确问“我该怎么办”；
2. 情绪强度较高；
3. 用户需要立即可执行的方法；
4. 多轮负面状态持续。

---

## 11. 检索增强推荐

### 11.1 查询构造

推荐触发后，系统不只使用原始用户输入做检索，而应构造多路查询：

1. 原始输入查询；
2. 情绪增强查询；
3. 压力来源查询；
4. 用户意图查询；
5. 长期记忆增强查询。

### 11.2 混合检索

系统采用以下检索策略：

1. BM25：处理关键词精确匹配；
2. 向量语义检索：处理语义近似召回；
3. RRF：融合不同检索器结果。

RRF 公式如下：

```text
RRF(d) = Σ 1 / (k + rank_i(d))
```

### 11.3 Rerank

融合后的候选结果还需要结合当前状态二次排序，重排因子包括：

1. 检索相关性；
2. 情绪匹配；
3. 风险适配；
4. 偏好匹配；
5. 可执行性；
6. 重复惩罚；
7. 禁用场景过滤。

---

## 12. Prompt 组装策略

Prompt 组装模块负责把显式状态整理成大模型可利用的上下文。

### 12.1 建议输入块

最终 Prompt 建议包含：

1. 系统角色设定；
2. 安全规则；
3. 当前用户输入；
4. 会话摘要；
5. 压缩上下文；
6. 用户长期画像；
7. 相关长期记忆；
8. 当前情绪状态；
9. 当前风险状态；
10. 推荐素材或推荐门控结果；
11. 回复风格约束。

### 12.2 信息优先级

建议优先级如下：

```text
安全规则 > 当前用户输入 > 当前风险状态 > 当前情绪状态 > 最近上下文 > 长期记忆 > 推荐素材
```

这样可以避免长期记忆和推荐内容覆盖当前用户真实表达。

---

## 13. API 与前端交互

### 13.1 核心接口

系统核心接口建议包括：

1. `/chat/intelligent`
2. `/agent/run`
3. `/session/{user_id}/{session_id}/summary`
4. `/content/recommend`
5. `/content/feedback`
6. `/session/{user_id}/list`

### 13.2 前端职责

前端应承担以下职责：

1. 展示聊天历史；
2. 展示风险提示；
3. 展示推荐及推荐反馈按钮；
4. 展示推荐门控摘要；
5. 展示调试视图；
6. 展示会话摘要、情绪状态和风险状态。

### 13.3 调试视图

调试视图建议集中展示：

1. 当前情绪状态；
2. 当前风险状态；
3. 推荐门控结果；
4. 会话摘要；
5. 推荐和检索元数据；
6. 当前反馈状态。

---

## 14. 数据更新闭环

每轮对话之后，系统应完成以下更新动作：

1. 将本轮加入短期历史；
2. 必要时触发上下文压缩；
3. 记录情绪事件；
4. 更新长期用户画像；
5. 记录推荐事件；
6. 记录推荐反馈；
7. 将反馈写回长期画像。

完整闭环可以概括为：

```text
聊天
  ↓
状态生成
  ↓
风险判断 / 推荐门控
  ↓
回复生成
  ↓
推荐反馈
  ↓
长期画像与会话状态更新
```

---

## 15. 工程约束与实现建议

### 15.1 工程约束

1. 高风险场景必须短路普通推荐；
2. 推荐冷却和反馈状态应持久化；
3. 长期记忆写入必须经过 MemoryGate；
4. 上下文压缩应基于 token，而不是仅按轮次；
5. Prompt 应显式注入摘要、画像、状态和相关记忆；
6. 前端调试视图应与后端结构化状态字段保持一致。

### 15.2 实施建议

建议按以下顺序演进：

1. 先稳定主链路；
2. 再优化状态与记忆；
3. 再升级检索与重排；
4. 最后补足评估、观测和更复杂的优化策略。

---

## 16. 当前实现状态与兼容策略

### 16.1 当前实现状态

截至当前版本，设计文档中的核心主链路能力已经基本落地，主要包括：

1. 风险状态主语义已统一为 `level_0 / level_1 / level_2 / level_3`，并保留旧字段兼容；
2. 高风险场景已从单一路由升级为 `level_2 / level_3` 双层安全门控，且补齐第三方危机求助专用分支；
3. 风险评估已接入长期风险等级、历史高风险事件、风险惯性和主体/否定/讨论语境识别；
4. 风险评估输出已包含 `risk_dimensions / risk_evidence / escalation_reasons / risk_context` 等结构化信息；
5. 已有独立输出安全检查器，最终回复出站前统一做安全校验；
6. 会话摘要接口已恢复只读语义，不会因查询动作创建幻影会话；
7. 长短期记忆已经进入 Prompt 主链路，包括会话摘要、压缩上下文、长期画像和相关长期记忆；
8. 推荐门控、推荐冷却、推荐反馈回写和推荐事件持久化已经形成闭环；
9. `/content/recommend` 已和聊天链路对齐，能够透传完整长期画像；
10. 调试视图已兼容并优先展示当前 `RiskState` 与推荐门控结构；
11. 离线评测已补充显式否定、第三方危机求助、讨论语境和风险惯性边界样本，并增加 `safety_boundary_acc` 指标。

从工程阶段看，项目已经从“补主链路缺口”进入“继续做兼容层收口、端到端联调和验证增强”的阶段。

### 16.2 当前兼容策略

为了保证旧数据、旧前端读取方式和当前结构化主链路可以平滑共存，项目当前采用以下兼容策略：

1. 风险等级主语义统一为 `level_0 / level_1 / level_2 / level_3`，但兼容层仍接受旧值如 `low / medium / high / urgent / warning_high / warning / normal`；
2. 聊天响应以 `risk_state` 为主，同时保留 `urgent_issue` 作为旧前端读取兜底；
3. 情绪相关返回以 `emotion_state` 为主，同时保留 `emotion_summary` 兼容旧调用方；
4. 调试视图优先读取 `risk_score / raw_score / risk_dimensions / escalation_reasons / risk_context`，同时兼容旧字段 `score / reason / signals`；
5. 数据库和风险日志统计层保留旧等级到新等级的映射，避免历史数据回放和跨版本统计失真；
6. 前端展示层会先做风险等级归一化，再决定是否展示高风险、中风险或第三方危机提示；
7. 评测脚本会把四级主语义统一映射为离线评测使用的 `low / medium / high` 标签。

### 16.3 后续收口建议

当前兼容策略是必要的过渡方案，但不应长期无限扩散。下一阶段建议按以下顺序继续收口：

1. 逐步更新文档、注释和测试中的旧风险等级示例；
2. 增加基于真实数据库持久化的端到端回归；
3. 补充前端页面级自动化验证，覆盖风险提示、推荐展示和反馈闭环；
4. 在确认无旧调用方依赖后，再逐步清理冗余兼容字段和旧命名。

---

## 17. 评估方案

### 17.1 模块级评估

建议评估以下指标：

1. 情绪识别 Accuracy / Macro-F1；
2. 情绪强度 MAE；
3. 高风险识别召回率；
4. `Level 3 recall` 与高风险误报率；
5. 安全边界样本准确率，例如 `safe_denial / third_party_crisis / discussion_context / risk_inertia`；
6. 推荐触发准确率；
7. 检索 Recall@K / Precision@K / NDCG；
8. 长期记忆命中率；
9. 噪声写入率。

### 17.2 端到端评估

端到端评估重点包括：

1. 回复是否理解用户情绪；
2. 建议是否具体可执行；
3. 是否符合用户长期偏好；
4. 是否避免重复建议；
5. 是否在高风险场景下安全；
6. 是否保持长对话连续性。

高风险识别召回率和高风险场景误推荐率是安全相关的核心指标。

---

## 18. 可扩展方向

后续可继续演进的方向包括：

1. 使用真实对话数据训练轻量情绪分类器；
2. 升级向量检索为更正式的 FAISS 索引；
3. 引入 Learning to Rank 或更细粒度 rerank；
4. 增强风险评估中的历史风险建模；
5. 加强长期记忆中的敏感信息过滤；
6. 引入更完整的推荐日志与分析看板；
7. 扩展为多 Agent 协作。

---

## 19. 总结

压力管理 Agent 的核心设计思想是：

1. 先理解用户状态；
2. 再做风险判断和推荐判断；
3. 最后生成回复并更新记忆。

系统通过显式状态建模、长短期记忆、推荐门控、检索增强推荐和安全响应链路，把原本隐式依赖大模型完成的任务拆分为多个可控模块，使系统在连续性、个性化、可解释性和安全性上都优于普通单轮聊天原型。
