# 压力管理 Agent 系统技术文档

## 1. 项目概述

本项目是一个面向压力管理与情绪支持场景的智能体系统，核心目标是构建一个具备情绪理解、长期记忆、个性化推荐和安全响应能力的对话式 Agent。系统不仅关注单轮对话中的文本生成效果，还强调在多轮连续交互中对用户状态的持续建模，使 Agent 能够根据用户当前情绪、历史压力来源、长期偏好和风险等级，生成更稳定、更个性化、更安全的支持性回复。

传统大模型聊天系统通常以当前输入和有限上下文作为主要依据，容易出现以下问题：

1. 长对话中上下文遗忘，无法稳定追踪用户状态；
2. 对用户长期压力来源和偏好缺乏建模；
3. 建议内容随机性较强，缺少可控的推荐机制；
4. 面对高风险表达时缺少明确的安全响应流程；
5. 每轮对话都依赖大模型隐式判断，系统行为不够可解释。

本项目围绕上述问题，设计了一个由主 Agent 调度的模块化系统，包括情绪识别模块、风险评估模块、短期记忆模块、长期记忆模块、RAG 推荐模块、推荐门控模块和安全响应模块。系统通过结构化状态建模和检索增强生成，使 Agent 从“被动聊天”转变为“能持续理解用户、适时提供干预”的个性化压力管理助手。

---

## 2. 系统目标

### 2.1 功能目标

系统需要实现以下功能：

1. 识别用户当前输入中的情绪状态，包括焦虑、低落、愤怒、压力、迷茫等；
2. 评估用户当前风险等级，区分普通压力、中度风险和高风险场景；
3. 维护 Session 内短期上下文，支持长对话连续性；
4. 维护跨会话长期记忆，记录用户偏好、压力来源、历史情绪事件和风险状态；
5. 基于 FAISS 和 BM25 构建混合检索推荐系统；
6. 根据情绪强度、用户意图、多轮状态和冷却策略判断是否触发推荐；
7. 在普通压力场景下提供自然融入对话的软推荐；
8. 在高风险场景下切换到更谨慎的安全回应策略；
9. 将情绪状态、长期记忆和推荐素材共同注入 Prompt，提高大模型回复的稳定性和个性化程度。

### 2.2 技术目标

系统设计重点包括：

1. **模块解耦**：将情绪识别、记忆管理、推荐检索、安全控制从主对话生成中拆分出来；
2. **状态显式化**：将用户情绪、风险、偏好等信息结构化存储，减少完全依赖大模型隐式推理；
3. **检索增强**：通过 RAG 机制为压力管理建议提供稳定素材来源；
4. **推荐可控**：通过门控机制控制推荐触发频率和触发场景；
5. **长对话稳定性**：通过 token 监控与摘要压缩解决上下文窗口限制；
6. **安全优先**：对高风险场景采用专门的安全响应链路。

---

## 3. 总体架构

系统整体采用“主 Agent 调度 + 多模块协作”的架构。

核心模块包括：

1. 主 Agent；
2. 情绪识别模块；
3. 风险评估模块；
4. 短期记忆模块；
5. 长期记忆模块；
6. 推荐门控模块；
7. RAG 推荐模块；
8. Prompt 组装模块；
9. 大模型生成模块；
10. 记忆更新模块；
11. 安全响应模块。

整体流程如下：

```text
用户输入
  ↓
主 Agent 接收请求
  ↓
读取短期上下文
  ↓
检索长期记忆
  ↓
情绪识别与风险评估
  ↓
推荐门控判断
  ↓
是否触发 RAG 推荐
  ↓
构造最终 Prompt
  ↓
调用大模型生成回复
  ↓
输出给用户
  ↓
更新短期记忆与长期记忆
```

系统不是让大模型一次性完成所有任务，而是将关键判断拆分为多个中间状态。例如，情绪类型、情绪强度、风险等级、推荐意图和用户偏好都会被显式计算或抽取，再作为结构化上下文提供给大模型。这种设计提高了系统的可解释性和可控性。

---

## 4. 主 Agent 设计

主 Agent 是系统的调度中心，负责组织每一轮对话的信息流和模块调用顺序。

### 4.1 核心职责

主 Agent 主要承担以下职责：

1. 接收用户当前输入；
2. 获取当前 Session 的短期上下文；
3. 从长期记忆中检索与当前输入相关的用户画像信息；
4. 调用情绪识别模块，得到当前情绪状态；
5. 调用风险评估模块，判断是否存在高风险表达；
6. 调用推荐门控模块，判断是否需要触发推荐；
7. 如需推荐，则调用 RAG 推荐模块获取候选素材；
8. 组装最终 Prompt；
9. 调用大模型生成回复；
10. 根据当前对话结果更新短期记忆和长期记忆。

### 4.2 主 Agent 执行流程

伪代码如下：

```python
def agent_turn(user_input, session_id, user_id):
    # 1. 读取短期上下文
    short_context = short_memory.get_context(session_id)

    # 2. 检索长期记忆
    long_memory = memory_store.retrieve(user_id, query=user_input)

    # 3. 情绪识别
    emotion_state = emotion_analyzer.analyze(
        user_input=user_input,
        context=short_context
    )

    # 4. 风险评估
    risk_state = risk_evaluator.evaluate(
        user_input=user_input,
        emotion_state=emotion_state,
        long_memory=long_memory
    )

    # 5. 推荐门控
    recommend_decision = recommend_gate.decide(
        user_input=user_input,
        emotion_state=emotion_state,
        risk_state=risk_state,
        long_memory=long_memory,
        short_context=short_context
    )

    # 6. RAG 推荐
    if recommend_decision.should_recommend:
        recommend_items = recommender.retrieve(
            user_input=user_input,
            emotion_state=emotion_state,
            risk_state=risk_state,
            user_profile=long_memory
        )
    else:
        recommend_items = []

    # 7. Prompt 组装
    prompt = prompt_builder.build(
        user_input=user_input,
        short_context=short_context,
        long_memory=long_memory,
        emotion_state=emotion_state,
        risk_state=risk_state,
        recommend_items=recommend_items
    )

    # 8. 调用大模型生成回复
    response = llm.generate(prompt)

    # 9. 更新记忆
    short_memory.append(session_id, user_input, response)
    memory_updater.update(user_id, user_input, response, emotion_state, risk_state)

    return response
```

---

## 5. 情绪识别模块

情绪识别模块用于判断用户当前输入中的情绪状态，为后续推荐和安全响应提供依据。

### 5.1 输入

模块输入包括：

1. 当前用户输入；
2. 最近若干轮对话上下文；
3. 可选的长期用户画像；
4. 历史情绪事件摘要。

### 5.2 输出

模块输出为结构化结果：

```json
{
  "emotion_type": "anxiety",
  "emotion_intensity": 0.76,
  "stress_source": "学业/求职压力",
  "user_intent": "seeking_help",
  "negative_trend": true,
  "confidence": 0.84
}
```

字段说明：

| 字段                | 含义                  |
| ----------------- | ------------------- |
| emotion_type      | 当前主要情绪类型            |
| emotion_intensity | 情绪强度，范围 0 到 1       |
| stress_source     | 主要压力来源              |
| user_intent       | 用户当前意图，例如倾诉、求助、计划制定 |
| negative_trend    | 是否存在多轮负面趋势          |
| confidence        | 模型判断置信度             |

### 5.3 情绪类型设计

常见情绪标签包括：

1. anxiety：焦虑；
2. sadness：低落；
3. anger：愤怒；
4. stress：压力；
5. confusion：迷茫；
6. helplessness：无助；
7. neutral：中性；
8. positive：积极。

### 5.4 情绪识别方式

情绪识别可以采用两种方式：

1. **规则 + LLM 分类**
   使用关键词、强度词、否定词和风险词进行初步判断，再由大模型输出结构化分类结果。

2. **轻量分类模型**
   使用标注数据训练文本分类模型，输出情绪类别和强度分数。

在项目早期，优先采用 LLM 结构化输出方式，原因是压力管理场景的数据量有限，LLM 对复杂语义、隐含情绪和上下文依赖的识别能力更强。后续如果积累足够标注数据，可以进一步训练轻量分类器，提高成本和延迟上的可控性。

---

## 6. 风险评估模块

风险评估模块用于识别用户是否处于高风险状态。该模块在系统中优先级高于普通推荐模块。

### 6.1 风险等级

系统将风险分为三类：

| 风险等级   | 描述               | 系统策略             |
| ------ | ---------------- | ---------------- |
| low    | 普通压力、轻度焦虑、一般倾诉   | 正常支持性回复，可触发软推荐   |
| medium | 情绪强烈、持续负面、自我否定明显 | 加强安抚，提供具体行动建议    |
| high   | 出现自伤、自杀、极端绝望等表达  | 进入安全响应流程，优先稳定和求助 |

### 6.2 风险评估输入

风险评估综合以下信息：

1. 当前用户输入；
2. 情绪识别结果；
3. 最近多轮情绪变化；
4. 长期风险等级；
5. 历史高风险事件；
6. 用户表达中的高风险关键词或语义。

### 6.3 高风险响应原则

当风险等级为 high 时，系统不再优先进行普通压力管理推荐，而是进入安全响应模式：

1. 表达理解和陪伴；
2. 避免批评、否定或过度理性分析；
3. 引导用户远离即时危险环境；
4. 鼓励联系可信任的人；
5. 建议寻求专业帮助或当地紧急资源；
6. 避免给出可能造成伤害的具体方法或步骤。

---

## 7. 记忆系统设计

记忆系统是本项目的核心模块之一，分为短期记忆和长期记忆。

---

### 7.1 短期记忆

短期记忆负责维护当前 Session 内的对话上下文。

#### 7.1.1 设计目标

短期记忆需要解决两个问题：

1. 保证当前对话的连续性；
2. 避免上下文超过模型 token 限制。

#### 7.1.2 实现方式

系统使用 tiktoken 对上下文 token 数进行实时监控。当上下文长度接近设定阈值时，触发摘要压缩机制。

短期记忆可以分为三部分：

1. 最近 N 轮原始对话；
2. 历史对话摘要；
3. 当前轮用户输入。

上下文拼接方式如下：

```text
Session Summary:
- 用户最近主要压力来源是……
- 用户对空泛安慰接受度较低，更偏好具体行动建议……
- 前面已经建议过任务拆分和呼吸练习……

Recent Turns:
User: ...
Assistant: ...
User: ...

Current User Input:
...
```

#### 7.1.3 Token 触发策略

可以设置两个阈值：

1. soft threshold：达到上下文窗口的 60% 到 70% 时，开始压缩较早对话；
2. hard threshold：达到上下文窗口的 80% 到 90% 时，强制压缩，只保留摘要和最近若干轮。

示例策略：

```python
if current_tokens > max_context_tokens * 0.7:
    compress_old_messages()

if current_tokens > max_context_tokens * 0.85:
    keep_recent_turns_only()
```

#### 7.1.4 摘要内容

摘要需要保留以下信息：

1. 用户当前问题背景；
2. 用户主要情绪；
3. 压力来源；
4. 已经给过的建议；
5. 用户明确接受或拒绝的建议；
6. 是否出现过风险表达；
7. 下一轮回复需要延续的上下文。

---

### 7.2 长期记忆

长期记忆用于维护跨 Session 的用户状态。

#### 7.2.1 设计目标

长期记忆的目标是让系统具备持续学习能力，使 Agent 能够记住用户的稳定偏好、长期压力模式和历史情绪事件。

#### 7.2.2 存储内容

长期记忆主要包括：

1. 用户画像；
2. 压力来源；
3. 情绪事件；
4. 风险等级；
5. 推荐偏好；
6. 建议接受度；
7. 不喜欢的回应方式。

示例结构如下：

```json
{
  "user_profile": {
    "preferred_support_style": "直接、具体、有行动建议",
    "avoid_style": ["空泛安慰", "过度说教"],
    "main_stress_sources": ["学业压力", "求职焦虑"],
    "risk_level": "medium"
  },
  "mood_events": [
    {
      "date": "2026-06-01",
      "emotion": "anxiety",
      "intensity": 0.72,
      "trigger": "面试准备",
      "summary": "用户担心项目表达不清楚，希望准备面试回答。"
    }
  ],
  "recommendation_feedback": {
    "breathing_exercise": "accepted",
    "mindfulness": "neutral",
    "task_planning": "preferred"
  }
}
```

#### 7.2.3 记忆写入机制

长期记忆不是每轮都无条件写入，而是通过记忆门控判断是否值得保存。

适合写入长期记忆的信息包括：

1. 用户明确表达的长期偏好；
2. 多次重复出现的压力来源；
3. 明显的情绪事件；
4. 用户对某类建议的接受或拒绝；
5. 风险等级变化；
6. 对后续个性化有帮助的信息。

不适合写入的信息包括：

1. 临时性闲聊；
2. 无长期价值的细节；
3. 低置信度推断；
4. 可能造成隐私风险的敏感信息；
5. 与压力管理无关的噪声。

#### 7.2.4 记忆更新策略

记忆更新可以采用 append、merge、overwrite 三种方式：

| 更新方式      | 场景              |
| --------- | --------------- |
| append    | 新增一次情绪事件        |
| merge     | 将相似压力来源合并       |
| overwrite | 用户明确修改偏好或风险状态变化 |

示例：

```python
def update_memory(user_id, new_event):
    old_memory = memory_store.get(user_id)

    if is_new_mood_event(new_event):
        old_memory["mood_events"].append(new_event)

    if is_stable_preference(new_event):
        old_memory["user_profile"]["preferred_support_style"] = new_event["preference"]

    if risk_level_changed(new_event):
        old_memory["user_profile"]["risk_level"] = new_event["risk_level"]

    memory_store.save(user_id, old_memory)
```

---

## 8. 推荐系统设计

推荐系统用于在合适的时机向用户提供压力管理内容。系统并不是每轮都推荐，而是通过推荐门控判断是否触发推荐。

---

### 8.1 推荐内容类型

推荐素材库可以包含以下内容：

1. 呼吸放松练习；
2. 正念练习；
3. 睡眠改善建议；
4. 情绪记录模板；
5. 认知重构方法；
6. 学习/工作计划模板；
7. 面试焦虑应对方法；
8. 人际压力应对策略；
9. 高风险求助建议；
10. 专业资源引导话术。

每条推荐素材可以结构化存储：

```json
{
  "item_id": "breathing_001",
  "title": "3分钟呼吸放松练习",
  "category": "breathing",
  "risk_level": ["low", "medium"],
  "emotion_type": ["anxiety", "stress"],
  "content": "吸气4秒，屏息2秒，呼气6秒，重复8轮。",
  "tags": ["焦虑", "压力", "快速缓解", "呼吸"],
  "contraindications": ["high_risk_crisis"]
}
```

---

### 8.2 推荐门控机制

推荐门控用于判断当前轮是否需要触发推荐。

#### 8.2.1 门控输入

推荐门控综合以下因素：

1. 情绪强度；
2. 风险等级；
3. 用户当前意图；
4. 多轮负面趋势；
5. 用户历史偏好；
6. 最近是否已经推荐过；
7. 当前对话是否适合打断；
8. 用户是否明确请求方法或建议。

#### 8.2.2 推荐评分

可以设计如下推荐分数：

```text
recommend_score =
    w1 * emotion_intensity
  + w2 * risk_score
  + w3 * intent_score
  + w4 * negative_trend_score
  + w5 * preference_score
  - w6 * cooldown_penalty
```

其中：

| 因子                   | 含义         |
| -------------------- | ---------- |
| emotion_intensity    | 当前情绪强度     |
| risk_score           | 风险等级对应分数   |
| intent_score         | 用户是否明确求助   |
| negative_trend_score | 是否连续多轮负面   |
| preference_score     | 用户是否偏好此类建议 |
| cooldown_penalty     | 避免频繁推荐     |

触发逻辑示例：

```python
if risk_level == "high":
    route_to_safety_response()
elif recommend_score >= threshold:
    trigger_recommendation()
else:
    normal_support_response()
```

#### 8.2.3 冷却机制

冷却机制用于避免系统频繁推荐，造成用户被打扰。

可以设置：

1. 最近 3 轮内已经推荐过，则降低推荐分数；
2. 用户明确拒绝某类推荐，则短期内不再推荐；
3. 对普通压力场景优先使用软推荐；
4. 只有用户明确求助或情绪强度较高时，才使用硬推荐。

---

### 8.3 软推荐与硬推荐

#### 8.3.1 软推荐

软推荐是将建议自然融入回复中，不以“推荐卡片”形式强行插入。

示例：

```text
你现在的问题更像是任务堆积带来的焦虑，不一定需要马上解决所有事情。
可以先把任务分成三类：今天必须完成、可以推迟、可以放弃。
先从第一类中最小的一件事开始，这样会更容易恢复掌控感。
```

软推荐适用于：

1. 普通压力；
2. 轻中度焦虑；
3. 用户正在倾诉但没有明确要求工具；
4. 需要保持对话自然性的场景。

#### 8.3.2 硬推荐

硬推荐是明确给出一个工具、练习或步骤。

示例：

```text
我建议你现在做一个 3 分钟呼吸练习：
1. 吸气 4 秒；
2. 屏息 2 秒；
3. 呼气 6 秒；
4. 重复 8 轮。
```

硬推荐适用于：

1. 用户明确说“我该怎么办”；
2. 情绪强度较高；
3. 多轮负面表达持续出现；
4. 用户需要立即可执行的缓解方法。

---

## 9. RAG 推荐模块

RAG 推荐模块用于从推荐素材库中检索与当前用户状态最匹配的内容。

### 9.1 素材库构建

推荐素材库中的每条数据包含：

1. 标题；
2. 正文内容；
3. 适用情绪；
4. 适用风险等级；
5. 标签；
6. 禁用场景；
7. 推荐形式；
8. 优先级；
9. 来源说明。

示例：

```json
{
  "id": "planning_003",
  "title": "任务拆分法",
  "category": "planning",
  "emotion": ["anxiety", "stress", "confusion"],
  "risk_level": ["low", "medium"],
  "tags": ["任务过载", "拖延", "计划", "掌控感"],
  "content": "将当前任务拆分为必须做、可以推迟、可以放弃三类，并从最小任务开始执行。",
  "recommend_type": "soft",
  "priority": 0.82
}
```

---

### 9.2 混合检索

系统采用 FAISS 向量检索和 BM25 关键词检索的混合方案。

#### 9.2.1 FAISS 语义检索

FAISS 用于语义召回，适合处理用户表达和素材文本之间的语义相似关系。

例如：

用户输入：

```text
我感觉事情太多了，完全不知道从哪里开始。
```

语义检索可以召回：

1. 任务拆分；
2. 优先级排序；
3. 恢复掌控感；
4. 焦虑缓解。

#### 9.2.2 BM25 关键词检索

BM25 用于关键词精确匹配，适合处理明确词汇和领域标签。

例如：

用户输入中出现“睡不着”“面试”“拖延”等关键词时，BM25 可以准确召回对应主题素材。

#### 9.2.3 RRF 融合排序

FAISS 和 BM25 各自产生候选结果后，使用 RRF 进行融合排序。

RRF 公式如下：

```text
RRF(d) = Σ 1 / (k + rank_i(d))
```

其中：

1. d 表示候选文档；
2. rank_i(d) 表示文档 d 在第 i 个检索器中的排名；
3. k 是平滑参数，通常取 60。

RRF 的优点是实现简单、稳定性好，不要求不同检索器分数处于同一尺度。

---

### 9.3 多路查询

为了提升召回质量，可以构造多路查询：

1. 原始用户输入；
2. 情绪增强查询；
3. 压力来源查询；
4. 用户意图查询；
5. 长期记忆增强查询。

示例：

```text
Query 1: 我感觉事情太多了，完全不知道从哪里开始
Query 2: 焦虑 任务过载 缺少掌控感
Query 3: 学业压力 计划制定 行动建议
Query 4: 用户需要具体可执行的方法
```

多路查询的作用是提升召回覆盖率，避免只依赖用户原始表达导致检索不稳定。

---

### 9.4 Rerank 重排

RRF 得到候选列表后，还需要结合用户状态进行二次排序。

重排因子包括：

1. 当前情绪类型是否匹配；
2. 当前风险等级是否适用；
3. 用户历史偏好是否匹配；
4. 是否近期已经推荐过；
5. 是否存在禁用条件；
6. 推荐内容是否足够具体；
7. 推荐形式是否适合当前场景。

重排分数示例：

```text
final_score =
    0.35 * retrieval_score
  + 0.20 * emotion_match_score
  + 0.15 * risk_match_score
  + 0.15 * preference_match_score
  + 0.10 * actionability_score
  - 0.05 * repetition_penalty
```

---

## 10. Prompt 组装策略

Prompt 组装模块负责将用户输入、短期上下文、长期记忆、情绪状态、风险等级和推荐素材整理成大模型可理解的上下文。

### 10.1 Prompt 输入内容

最终 Prompt 通常包含以下部分：

1. 系统角色设定；
2. 安全响应规则；
3. 当前用户输入；
4. 最近几轮对话；
5. Session 摘要；
6. 长期用户画像；
7. 当前情绪识别结果；
8. 当前风险评估结果；
9. 推荐素材；
10. 回复风格约束。

### 10.2 Prompt 模板示例

```text
你是一个压力管理与情绪支持 Agent。你的目标是理解用户当前情绪，
提供具体、温和、可执行的支持建议。

【安全规则】
- 如果用户存在高风险表达，优先进行安全回应。
- 不提供可能造成伤害的具体方法。
- 鼓励用户联系可信任的人或专业资源。

【用户长期画像】
{long_memory}

【当前会话摘要】
{session_summary}

【最近对话】
{recent_turns}

【当前情绪状态】
{emotion_state}

【风险等级】
{risk_state}

【推荐素材】
{recommend_items}

【当前用户输入】
{user_input}

请生成一段自然、具体、有同理心的回复。
```

### 10.3 Prompt 控制重点

Prompt 设计中需要控制以下问题：

1. 不要机械复述情绪标签；
2. 不要每轮都给建议；
3. 不要空泛安慰；
4. 普通压力场景优先给具体行动建议；
5. 高风险场景优先安全回应；
6. 推荐内容要自然融入对话；
7. 避免暴露系统内部判断过程。

---

## 11. 每轮对话的信息流

每轮对话的信息流可以分为输入层、状态层、决策层、生成层和更新层。

### 11.1 输入层

输入层包括：

1. 当前用户消息；
2. 当前 Session ID；
3. 用户 ID；
4. 最近对话历史。

### 11.2 状态层

状态层生成结构化中间状态：

1. emotion_state；
2. risk_state；
3. user_profile；
4. session_summary；
5. recommendation_context。

### 11.3 决策层

决策层主要完成：

1. 是否触发推荐；
2. 推荐软推荐还是硬推荐；
3. 是否进入安全响应；
4. 需要检索哪些素材；
5. 哪些长期记忆需要注入 Prompt。

### 11.4 生成层

生成层将所有上下文组装为 Prompt，并调用大模型生成最终回复。

### 11.5 更新层

更新层完成：

1. 追加本轮对话到短期记忆；
2. 判断是否需要压缩历史上下文；
3. 抽取长期记忆候选；
4. 更新用户画像；
5. 写入情绪事件；
6. 更新推荐反馈。

---

## 12. 数据结构设计

### 12.1 用户画像表

```json
{
  "user_id": "user_001",
  "preferred_support_style": "direct_actionable",
  "avoid_style": ["empty_comfort", "preaching"],
  "main_stress_sources": ["academic_pressure", "job_interview"],
  "risk_level": "medium",
  "updated_at": "2026-06-01T12:00:00"
}
```

### 12.2 情绪事件表

```json
{
  "event_id": "event_001",
  "user_id": "user_001",
  "session_id": "session_001",
  "emotion_type": "anxiety",
  "emotion_intensity": 0.76,
  "stress_source": "interview",
  "summary": "用户担心面试中项目讲不清楚。",
  "created_at": "2026-06-01T12:05:00"
}
```

### 12.3 推荐素材表

```json
{
  "item_id": "item_001",
  "title": "任务拆分法",
  "category": "planning",
  "emotion_type": ["anxiety", "stress"],
  "risk_level": ["low", "medium"],
  "tags": ["任务过载", "拖延", "计划"],
  "content": "将任务拆分为必须做、可以推迟、可以放弃三类。",
  "recommend_type": "soft"
}
```

### 12.4 推荐日志表

```json
{
  "log_id": "rec_001",
  "user_id": "user_001",
  "item_id": "item_001",
  "recommend_type": "soft",
  "accepted": true,
  "created_at": "2026-06-01T12:10:00"
}
```

---

## 13. 安全响应设计

压力管理 Agent 面对的用户输入可能包含心理危机场景，因此系统需要独立的安全响应模块。

### 13.1 安全响应触发条件

以下情况需要触发安全响应：

1. 用户表达自伤或自杀意图；
2. 用户表达极端绝望；
3. 用户出现明确危险计划；
4. 用户持续表达无法控制自己；
5. 用户提到即将伤害自己或他人。

### 13.2 安全响应流程

```text
高风险输入
  ↓
风险评估模块标记 high
  ↓
跳过普通推荐
  ↓
进入安全响应 Prompt
  ↓
生成支持性、安全性回复
  ↓
记录风险事件
  ↓
提高后续对话监控等级
```

### 13.3 安全响应策略

高风险场景下，回复应遵循以下原则：

1. 先承接情绪，不直接讲大道理；
2. 明确表达用户不是一个人；
3. 鼓励用户联系身边可信任的人；
4. 鼓励寻求专业支持；
5. 对即时危险表达，要建议联系当地紧急服务；
6. 避免提供任何伤害方法或细节。

---

## 14. 系统评估方案

系统评估可以从模块级评估和端到端评估两个层面进行。

### 14.1 情绪识别评估

评估指标包括：

1. Accuracy；
2. Macro-F1；
3. 情绪强度 MAE；
4. 高风险召回率；
5. 高风险误判率。

其中，高风险召回率优先级最高，因为漏判高风险输入的代价较大。

### 14.2 推荐系统评估

推荐系统可以使用以下指标：

1. Recall@K；
2. Precision@K；
3. MRR；
4. NDCG；
5. 推荐触发准确率；
6. 用户接受率；
7. 重复推荐率；
8. 高风险场景误推荐率。

其中，高风险场景误推荐率需要重点控制，避免在危机场景下仍然输出普通建议。

### 14.3 长对话评估

长对话评估关注：

1. 是否记住用户前文背景；
2. 是否避免重复建议；
3. 是否能延续上一轮计划；
4. 摘要压缩后是否保留关键信息；
5. 长期记忆是否被正确调用。

### 14.4 端到端回复质量评估

端到端评估可以从以下维度打分：

1. 情绪理解是否准确；
2. 回复是否有同理心；
3. 建议是否具体可执行；
4. 是否符合用户偏好；
5. 是否过度推荐；
6. 是否存在安全风险；
7. 是否保持对话自然性。

---

## 15. 项目难点

### 15.1 长期记忆的写入边界

长期记忆不能无条件写入。写入过多会导致噪声积累，写入过少又会影响个性化效果。因此需要设计记忆门控，判断哪些信息具有长期价值。

### 15.2 推荐触发时机

压力管理场景中，推荐过早会显得打断用户，推荐过晚又可能错过干预时机。因此推荐门控需要同时考虑情绪强度、用户意图、多轮趋势和冷却机制。

### 15.3 安全风险识别

高风险表达可能是显性的，也可能是隐含的。系统需要避免漏判，同时也要避免对普通负面情绪过度升级。

### 15.4 RAG 素材质量控制

推荐系统效果不仅取决于检索算法，也取决于素材库质量。素材需要有明确标签、适用场景、禁用场景和推荐形式，否则容易出现检索到了但不适合当前用户状态的问题。

### 15.5 Prompt 上下文冲突

当短期上下文、长期记忆、情绪识别结果和推荐素材同时注入 Prompt 时，可能出现信息冲突。因此需要设计优先级：

```text
安全规则 > 当前用户输入 > 当前情绪状态 > 最近上下文 > 长期记忆 > 推荐素材
```

---

## 16. 项目亮点

### 16.1 从单轮问答升级为状态驱动 Agent

系统不只是根据当前输入生成回复，而是先构建用户状态，再基于状态做决策和生成。

### 16.2 记忆系统提升长对话连续性

短期记忆通过 token 监控和摘要压缩解决上下文窗口限制，长期记忆通过用户画像和情绪事件实现跨会话持续学习。

### 16.3 推荐系统具有门控机制

系统不是每轮都推荐，而是通过情绪强度、风险等级、用户意图、多轮趋势和冷却机制决定是否推荐，提高了推荐的自然性和可控性。

### 16.4 RAG 推荐结合语义检索和关键词检索

使用 FAISS 进行语义召回，BM25 进行关键词召回，再通过 RRF 融合和 Rerank 提升推荐准确性。

### 16.5 安全响应模块独立设计

系统对高风险输入设置专门链路，优先保障安全性，而不是继续执行普通推荐逻辑。

---

## 17. 可扩展方向

后续可以从以下方向继续优化：

1. 使用真实对话数据训练情绪分类模型；
2. 对推荐素材进行人工标注和质量分级；
3. 引入用户反馈机制，优化推荐接受率；
4. 使用 Learning to Rank 模型替代规则 Rerank；
5. 建立多轮对话评测集；
6. 引入 Reflection 机制，对低质量回复进行自我修正；
7. 支持多 Agent 协作，例如情绪分析 Agent、推荐 Agent、安全 Agent、总结 Agent；
8. 引入 A/B 测试，比较不同门控阈值和推荐策略；
9. 加强隐私保护，对长期记忆做敏感信息过滤；
10. 建立可观测性日志，追踪每轮对话的情绪、风险、推荐和生成结果。

---

## 18. 总结

本项目构建了一个面向压力管理场景的智能体系统，核心技术路线是：

```text
多 Agent 调度
+ 情绪识别
+ 风险评估
+ 短期/长期记忆
+ FAISS + BM25 混合检索
+ RRF 融合排序
+ 推荐门控
+ 安全响应
+ Prompt 组装
```

系统通过显式状态建模和模块化决策，使大模型不再只是进行单轮文本生成，而是能够在连续对话中理解用户状态、维护长期画像、适时触发推荐，并在高风险场景下进入安全响应流程。

从工程角度看，项目体现了大模型 Agent 系统设计中的几个关键能力：上下文管理、长期记忆、检索增强生成、推荐决策控制、安全策略和端到端评估。整体上，该项目可以被定位为一个具备个性化、连续性和安全性的压力管理 Agent 原型系统。
