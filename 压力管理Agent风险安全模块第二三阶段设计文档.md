# 压力管理 Agent 风险安全模块第二、三阶段设计文档

## 1. 文档定位

本文档用于说明压力管理 Agent 风险安全模块在单轮风险识别模型之后，如何继续扩展到多轮会话级风险管理和长期风险记忆管理。

当前 v4.2 单轮模型已经能够对当前用户输入进行风险语义识别，采用四分类头、high-risk binary 辅助头和规则兜底进行三级融合。它的定位是 utterance-level risk detector，即“单轮文本级风险识别器”。它回答的问题是：当前这一句话是否包含风险语义、风险严重度大概在哪一级。

但是完整的 Agent 风险安全系统不能只依赖单句话判断，还需要处理两个更高层的问题：

1. 最近几轮对话中，用户风险是否在持续升高或缓解？
2. 用户在长期使用中是否存在近期风险基线、反复触发因素、保护因素和支持偏好？

因此，后续设计拆成两个阶段：

- 第二阶段：会话级风险聚合器，负责多轮上下文内的风险趋势、风险惯性和安全确认。
- 第三阶段：长期风险记忆层，负责跨会话维护近期风险基线、触发因素、保护因素和支持偏好。

完整架构如下：

```text
当前用户输入
  ↓
v4.2 单轮风险识别器
  ↓
会话级风险聚合器
  ↓
长期风险记忆层
  ↓
Safety Gate 最终安全门控
  ↓
回复策略 / 推荐策略 / 记忆写入策略
```

---

## 2. 第二阶段：会话级风险聚合器

### 2.1 模块目标

会话级风险聚合器的目标不是重新判断单句话风险，而是把最近多轮对话中的风险信号聚合成一个稳定的会话风险状态。

它主要解决以下问题：

1. 用户风险是否在连续几轮中逐步升高？
2. 用户刚刚出现高风险表达后，是否不能因为一句“没事”就立即降级？
3. 多个轻中度风险表达连续出现时，是否应触发更谨慎的安全策略？
4. 用户是否明确确认安全、是否已经联系他人、是否愿意继续获得帮助？
5. 当前 Agent 应该使用普通支持、高风险支持，还是危机响应？

### 2.2 模块边界

会话级风险聚合器不直接替代 v4.2 单轮模型。

它的输入来自每轮用户输入经过单轮风险识别器后的结构化信号，而不是直接对完整对话重新做黑盒分类。

推荐边界如下：

| 模块 | 负责内容 | 不负责内容 |
|---|---|---|
| v4.2 单轮模型 | 当前文本风险语义 | 多轮趋势、长期状态 |
| 会话级风险聚合器 | 最近几轮风险趋势与风险惯性 | 长期画像、跨会话记忆 |
| 长期风险记忆层 | 近期风险基线、触发因素、支持偏好 | 单轮分类、即时危机判断 |
| Safety Gate | 最终回复模式与推荐策略 | 原始文本语义建模 |

---

## 3. 会话级风险聚合器的输入与输出

### 3.1 输入字段

每一轮用户输入经过 v4.2 单轮识别器后，建议生成一条 `utterance_risk` 记录。

| 字段 | 含义 |
|---|---|
| `turn_id` | 当前轮次编号 |
| `user_text` | 当前用户输入 |
| `model_level` | 四分类头输出的语义风险等级，0/1/2/3 |
| `binary_risk` | high-risk binary 头是否判断为 level_2+ 风险通道 |
| `binary_prob` | binary 风险概率 |
| `rule_hit` | 是否命中高危规则 |
| `rule_level` | 规则建议的最低风险等级，通常为 2 或 3 |
| `evidence_tags` | 当前文本中的风险证据标签 |
| `subject_context` | 主体与语境，例如本人、第三方、否定、假设 |
| `risk_gate_candidate` | 单轮层得到的候选门控等级 |

这里的 `risk_gate_candidate` 仍然只代表当前这一轮，不等于整个会话的最终风险等级。

### 3.2 输出字段

会话聚合器输出 `session_risk_state`。

| 字段 | 含义 |
|---|---|
| `session_risk_level` | 当前会话级风险等级，0/1/2/3 |
| `recent_max_level` | 最近 N 轮最高风险等级 |
| `last_user_level` | 上一轮用户输入的候选风险等级 |
| `consecutive_risk_turns` | 连续出现 level_1+ 风险信号的轮数 |
| `last_high_risk_turn` | 最近一次 level_2/3 出现的轮次 |
| `risk_trend` | 风险趋势，rising / stable / falling |
| `safety_confirmed` | 用户是否明确确认当前安全 |
| `support_engaged` | 用户是否已经联系或愿意联系支持对象 |
| `escalation_reason` | 本轮升级或保持风险等级的原因 |
| `recommended_response_mode` | normal / cautious / high_risk_support / crisis |

---

## 4. 会话级聚合核心机制

### 4.1 滑动窗口

建议先使用最近 3 到 5 轮用户输入作为风险聚合窗口。

窗口太短会漏掉逐步升级的风险；窗口太长则会导致旧风险过度影响当前状态。对于压力管理 Agent，最近 5 轮通常是比较合适的初始选择。

需要统计的窗口特征包括：

| 特征 | 说明 |
|---|---|
| `recent_max_level` | 最近 N 轮最高风险等级 |
| `recent_high_risk_count` | 最近 N 轮中 level_2/3 出现次数 |
| `recent_level1_count` | 最近 N 轮中 level_1 出现次数 |
| `rule_hit_count` | 最近 N 轮规则命中次数 |
| `risk_trend` | 最近几轮风险是否持续上升 |

### 4.2 当前轮优先原则

如果当前轮已经明确达到高风险，则优先使用当前轮结果。

| 当前轮候选等级 | 会话处理 |
|---|---|
| level_3 | 会话风险直接进入 level_3 |
| level_2 | 会话风险至少保持 level_2 |
| level_1 | 结合最近窗口和历史状态判断是否保持 level_1 或升级 |
| level_0 | 不能单独决定降级，需要结合风险惯性和安全确认 |

### 4.3 风险惯性

风险安全系统不能让风险等级断崖式下降。

典型场景：

```text
上一轮：我已经准备好了。
当前轮：算了，没事。
```

当前轮虽然可能被单轮模型判为 level_0，但会话层不能立即降为 normal。更合理的处理是先降到 level_2 或 level_1，并继续做安全确认。

建议规则：

| 上一会话等级 | 当前候选等级 | 会话层处理 |
|---|---|---|
| level_3 | level_0/1 | 最多降到 level_2 |
| level_2 | level_0 | 最多降到 level_1 |
| level_1 | level_0 | 可以降级，但需观察是否连续低风险 |

### 4.4 连续风险升级

如果最近多轮没有单独出现 level_3，但风险表达持续升高，也应该提高会话级风险。

建议规则：

| 条件 | 会话层处理 |
|---|---|
| 最近 5 轮 level_1 出现 3 次以上 | 会话风险至少 level_1 |
| 最近 5 轮 level_2 出现 2 次以上 | 会话风险至少 level_2 |
| 最近几轮风险等级连续上升 | 标记 `risk_trend = rising`，回复更谨慎 |
| 最近 5 轮同时出现 high-risk binary 和规则弱信号 | 至少进入谨慎支持 |

### 4.5 安全确认参与降级

用户明确确认安全，可以参与降级，但不能直接覆盖高危信号。

可识别的安全确认包括：

| 信号 | 含义 |
|---|---|
| 明确说当前不会伤害自己 | `safety_confirmed = true` |
| 已经离开危险环境 | 降低紧急性 |
| 已经联系朋友、家人、老师或医生 | `support_engaged = true` |
| 愿意继续对话或接受帮助 | 作为保护因素 |

降级必须满足多个条件：

1. 最近若干轮没有新的 level_2/3 信号；
2. 当前轮没有规则命中；
3. 用户明确确认安全；
4. 用户已有支持对象或愿意继续获得帮助。

### 4.6 主体和语境处理

会话级聚合必须尊重 `subject_context`。

| 语境 | 会话层处理 |
|---|---|
| 本人当前风险 | 正常参与会话风险聚合 |
| 第三方求助 | 不直接提升用户自身 baseline，但进入帮助他人的安全支持模式 |
| 明确否定风险 | 可作为降级证据，但不能覆盖最近 level_3 |
| 假设、引用、新闻、科普 | 不应直接提升用户自身风险 |
| 过去经历 | 需要结合时间距离和当前情绪判断 |

---

## 5. 会话级响应策略

| `session_risk_level` | 响应模式 | 推荐系统策略 |
|---|---|---|
| 0 | normal_support | 正常压力管理推荐 |
| 1 | cautious_support | 低认知负担支持，可做轻量确认 |
| 2 | high_risk_support | 普通推荐降级，优先安全确认和支持资源 |
| 3 | crisis_response | 关闭普通推荐，进入危机响应和紧急支持路径 |

会话层的作用不仅是输出等级，还要控制回复方式和推荐系统：

- level_0：允许普通陪伴、压力管理建议、内容推荐；
- level_1：回复更谨慎，避免过度建议，推荐低负担内容；
- level_2：优先关注安全，普通推荐降级；
- level_3：不再走普通推荐路径，优先危机支持。

---

## 6. 第二阶段实现路线

建议第二阶段实现为 v5.0。

### 6.1 v5.0 目标

v5.0 的目标是把单轮模型输出扩展成稳定的会话级风险状态。

核心功能：

1. 每轮记录风险日志；
2. 最近 5 轮滑动窗口聚合；
3. 风险惯性机制；
4. 连续风险升级机制；
5. 安全确认和保护因素参与降级；
6. 输出 `session_risk_level` 和 `recommended_response_mode`。

### 6.2 实现步骤

| 步骤 | 任务 | 产物 |
|---|---|---|
| Step 1 | 接入 v4.2 单轮输出 | `utterance_risk` 日志 |
| Step 2 | 维护最近 N 轮窗口 | `recent_risk_window` |
| Step 3 | 实现风险惯性规则 | `risk_decay_policy` |
| Step 4 | 实现连续升级规则 | `trend_detector` |
| Step 5 | 接入安全确认解析 | `safety_confirmed`, `support_engaged` |
| Step 6 | 输出会话风险状态 | `session_risk_state` |
| Step 7 | 离线回放测试 | 多轮测试集和期望状态 |

### 6.3 测试场景

建议构造 5 类多轮测试集：

| 场景 | 目标 |
|---|---|
| 低风险持续压力 | 验证不会过度升级 |
| 风险逐步升高 | 验证趋势识别 |
| 高风险后突然否认 | 验证风险惯性 |
| 第三方求助 | 验证主体区分 |
| 风险缓解并确认安全 | 验证降级机制 |

### 6.4 第二阶段评估指标

| 指标 | 说明 |
|---|---|
| 危机漏报率 | 出现 level_3 后会话层是否漏掉 |
| 风险惯性正确率 | 高风险后是否正确保持谨慎状态 |
| 趋势识别准确率 | 连续升级场景是否被识别 |
| 误升级率 | 低风险对话是否被过度升级 |
| 降级合理性 | 用户确认安全后是否逐步降级 |

---

## 7. 第三阶段：长期风险记忆层

### 7.1 模块目标

长期风险记忆层负责跨会话维护用户近期风险基线、反复触发因素、保护因素和支持偏好。

它不负责直接判断当前这一句话是否危险，而是为 Safety Gate 和回复生成提供背景信息。

它回答的问题包括：

1. 用户近期是否多次出现 level_2/3 风险表达？
2. 用户是否存在反复触发风险的主题，例如实验失败、求职压力、睡眠问题？
3. 用户是否有可用的保护因素，例如愿意联系朋友、家人、导师？
4. 用户更接受哪种支持方式，是直接建议、温和陪伴，还是具体行动清单？
5. 当前对话是否应该因为近期风险基线而提高敏感度？

### 7.2 设计原则

长期风险记忆要遵循四个原则：

| 原则 | 说明 |
|---|---|
| 不诊断 | 不给用户贴“有自杀倾向”等长期标签 |
| 近期化 | 记录近期风险状态，而不是永久身份 |
| 最小化 | 只存系统安全和支持必要的信息 |
| 可衰减 | 风险基线随时间和低风险互动逐步降低 |

建议记录事实和系统策略，而不是诊断结论。

不建议写：

```text
用户有自杀倾向。
```

建议写：

```text
用户近期多次表达强烈压力和无助，后续回复需要更谨慎，必要时优先做安全确认。
```

---

## 8. 长期记忆的数据结构

建议维护 `risk_memory_profile`。

| 字段 | 含义 |
|---|---|
| `recent_risk_baseline` | low / medium / high，表示近期风险基线 |
| `risk_events` | 近期重要风险事件列表 |
| `common_triggers` | 反复触发压力或风险的主题 |
| `protective_factors` | 用户可用的保护因素 |
| `support_preferences` | 用户偏好的支持方式 |
| `last_high_risk_time` | 最近一次 level_2/3 时间 |
| `last_safety_confirmation` | 最近一次安全确认记录 |
| `decay_status` | 当前风险基线衰减状态 |

### 8.1 risk_events

`risk_events` 记录重要风险事件，而不是保存大量原文。

建议字段：

| 字段 | 含义 |
|---|---|
| `event_time` | 事件时间 |
| `max_session_level` | 本次会话最高风险等级 |
| `topic` | 触发主题，例如实验、求职、睡眠 |
| `evidence_summary` | 简短证据摘要 |
| `safety_confirmed` | 是否确认安全 |
| `support_engaged` | 是否联系支持对象 |
| `resolved` | 是否已经缓解 |

### 8.2 common_triggers

`common_triggers` 用于记录反复触发用户压力或风险的主题。

例子：

| 触发因素 | 说明 |
|---|---|
| 实验失败 | 多次在实验不顺后表达强压力 |
| 求职压力 | 秋招、面试、算法岗准备导致焦虑 |
| 睡眠问题 | 睡眠不足与情绪低落同时出现 |
| 人际冲突 | 与朋友、伴侣或导师相关压力 |

### 8.3 protective_factors

保护因素不是用来直接降级高危风险，而是帮助系统选择更有效的支持方式。

例子：

| 保护因素 | 用法 |
|---|---|
| 愿意联系朋友 | level_2 时优先建议联系该支持对象 |
| 愿意继续对话 | 危机支持中保持连接 |
| 有导师或家人支持 | 建议具体联系对象 |
| 接受过某种稳定技巧 | 优先推荐有效方式 |

---

## 9. 长期记忆写入策略

### 9.1 什么时候写入

长期记忆不应每轮都写，只在具有长期价值时写。

| 触发条件 | 是否写入 |
|---|---|
| 会话最高风险达到 level_2/3 | 写入 risk_event |
| 多轮连续 level_1，且同一主题反复出现 | 写入 common_trigger |
| 用户明确说某种支持方式有效 | 写入 support_preferences |
| 用户提到愿意联系某个人 | 写入 protective_factors |
| 普通一次性抱怨 | 不写 |
| 第三方求助 | 通常不写入用户自身风险基线 |
| 明确否定风险 | 可写入安全确认，但不写成长期低风险结论 |

### 9.2 写入内容控制

每条长期记忆建议包含：

1. 风险等级或会话最高风险；
2. 触发主题；
3. 简短证据摘要；
4. 是否已确认安全；
5. 是否有支持对象；
6. 是否需要未来回复更谨慎。

不要保存过多敏感原文，除非系统确实需要。

---

## 10. 长期风险基线更新

### 10.1 基线定义

| baseline | 定义 | 系统影响 |
|---|---|---|
| low | 近期无 level_2/3，主要是普通压力 | 正常敏感度 |
| medium | 近期出现 level_2，或多次 level_1 | 回复更谨慎，必要时轻量安全确认 |
| high | 近期出现 level_3，或短期多次 level_2/3 | 提高安全敏感度，优先稳定和确认安全 |

### 10.2 更新规则

建议使用时间窗口更新：

| 条件 | 更新 |
|---|---|
| 7 天内出现 level_3 | baseline 升为 high |
| 7 天内出现 2 次以上 level_2 | baseline 升为 medium 或 high |
| 14 天内只有 level_0/1 | baseline 逐步下降 |
| 用户连续多次确认安全且无新风险 | baseline 可降级 |
| 只有第三方求助 | 不提升用户自身 baseline |

### 10.3 衰减机制

长期风险状态必须衰减，避免永久贴标签。

建议：

| 记忆类型 | 建议保留或衰减 |
|---|---|
| risk_event | 30 到 90 天内参与 baseline 计算 |
| common_trigger | 如果长期不再出现，可降低权重 |
| protective_factor | 可长期保留，但需定期验证 |
| support_preference | 可长期保留，除非用户表达变化 |

---

## 11. 长期记忆如何影响当前对话

长期记忆不直接替代当前单轮风险识别，它主要影响阈值、回复方式和推荐策略。

### 11.1 调整安全敏感度

如果长期 baseline 为 high，而当前单轮模型判断 level_1，系统可以选择更谨慎的回复方式。

例子：

```text
当前输入：今天实验又失败了，我有点绷不住。
单轮模型：level_1
长期记忆：实验失败是反复触发因素，近期 baseline=medium
系统策略：保持谨慎支持，可做轻量安全确认。
```

### 11.2 个性化支持方式

如果长期记忆中记录用户更喜欢直接、实用的建议，则回复可以更偏行动清单，而不是长篇共情。

例子：

| 记忆 | 回复影响 |
|---|---|
| 用户偏好直接建议 | 给出短步骤 |
| 用户不喜欢被说教 | 避免强行建议 |
| 用户接受呼吸练习 | 优先推荐短练习 |
| 用户愿意联系朋友 | 高风险时优先引导联系该支持对象 |

### 11.3 推荐系统联动

长期风险记忆还会影响推荐系统。

| baseline | 推荐策略 |
|---|---|
| low | 正常推荐压力管理内容 |
| medium | 推荐低认知负担内容，减少复杂长文 |
| high | 普通推荐降级，优先安全资源和即时支持 |

---

## 12. 第三阶段实现路线

建议第三阶段实现为 v6.0。

### 12.1 v6.0 目标

v6.0 的目标是把会话级风险状态沉淀为跨会话的近期风险基线和支持偏好。

核心功能：

1. 风险事件写入；
2. 触发因素提取；
3. 保护因素维护；
4. 支持偏好维护；
5. 风险基线更新和衰减；
6. Safety Gate 使用长期状态进行轻量修正。

### 12.2 实现步骤

| 步骤 | 任务 | 产物 |
|---|---|---|
| Step 1 | 定义 risk_memory_profile schema | 长期记忆数据结构 |
| Step 2 | 设计写入触发条件 | memory_write_policy |
| Step 3 | 会话结束时生成 risk_event | risk_events |
| Step 4 | 提取 common_triggers 和 protective_factors | 用户风险画像片段 |
| Step 5 | 实现 baseline 更新规则 | recent_risk_baseline |
| Step 6 | 实现衰减和过期机制 | decay_policy |
| Step 7 | 接入 Safety Gate | final_risk_level 修正 |

### 12.3 第三阶段评估指标

| 指标 | 说明 |
|---|---|
| 记忆写入准确率 | 写入的 risk_event 是否确实有长期价值 |
| 误写率 | 普通抱怨是否被错误写成风险记忆 |
| 触发因素准确率 | common_triggers 是否合理 |
| baseline 稳定性 | 是否频繁过度升降 |
| 个性化有效性 | 回复是否更贴合用户偏好 |
| 安全收益 | 高风险相关话题中是否更早进入谨慎模式 |

---

## 13. Safety Gate 最终融合

最终安全门控不应只等于任何单一模块输出。

建议融合以下信号：

| 信号来源 | 作用 |
|---|---|
| v4.2 单轮模型 | 当前文本风险语义 |
| 规则兜底 | 短文本高危、明确计划、正在实施等强风险 |
| 会话级聚合 | 最近几轮风险趋势和风险惯性 |
| 长期风险记忆 | 近期风险基线、触发因素、保护因素 |
| 安全确认 | 是否可以逐步降级 |

融合思路：

1. 规则命中强 level_3 时，优先进入危机模式；
2. 当前单轮 level_2/3 时，会话风险至少保持对应等级；
3. 最近窗口出现高风险时，不允许立即降级；
4. 长期 baseline 只能轻量提高敏感度，不直接把普通句子判成危机；
5. 明确保护因素可以帮助降级，但不能覆盖高危规则。

---

## 14. 完整示例

### 14.1 会话内风险升级

```text
第 1 轮：最近实验一直失败，我好累。
单轮：level_0/1
会话：level_1

第 2 轮：感觉自己真的很没用。
单轮：level_1
会话：level_1，趋势 rising

第 3 轮：我有时候真的想消失。
单轮：level_1/2
会话：level_2，因为连续风险升高

第 4 轮：算了，没事。
单轮：level_0
会话：仍保持 level_1/2，因为风险惯性
```

### 14.2 跨会话长期记忆影响

会话结束后写入：

```text
近期因实验压力出现连续无助和消失表达，后续相关话题需更谨慎。
```

下一次用户输入：

```text
实验又失败了，我现在有点绷不住。
```

单轮模型可能只判为 level_1，但长期记忆显示“实验失败”是反复触发因素，因此 Safety Gate 可以选择谨慎支持，并做轻量安全确认。

---

## 15. 开发路线总结

| 版本 | 目标 | 核心产物 |
|---|---|---|
| v4.2 | 单轮风险识别器 | MultiTaskBERT + 规则兜底 + binary 融合 |
| v5.0 | 会话级风险聚合器 | 滑动窗口、风险惯性、趋势识别、安全确认 |
| v6.0 | 长期风险记忆层 | 风险基线、触发因素、保护因素、支持偏好 |
| v7.0 | 完整 Safety Gate | 单轮、会话、长期记忆统一融合 |

---

## 16. 一句话总结

第二阶段要做的是“多轮上下文风险状态机”，解决风险趋势、风险惯性和安全确认问题；第三阶段要做的是“长期风险记忆层”，记录近期风险基线、触发因素、保护因素和支持偏好。v4.2 单轮模型继续作为风险信号源，不需要被替代，而是被接入更高层的 Safety Gate 中。
