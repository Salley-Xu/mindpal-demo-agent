# M1 实施方案：基线稳定与状态契约统一

## 1. 文档目标

本文档用于指导第一阶段开发，也就是 `M1：基线稳定版` 的具体落地。

本阶段只做两件事：

1. 修复当前主链路中已经确认的问题；
2. 统一前后端状态字段和接口契约。

本阶段不做以下事情：

1. 不引入 FAISS、BM25、RRF；
2. 不重构长期记忆数据库结构；
3. 不做推荐门控模块；
4. 不做复杂 Prompt Builder；
5. 不做大规模前端交互升级。

这样做的目的，是先把项目从“能跑但不稳定”提升到“主链路稳定、字段统一、可继续演进”的状态。

---

## 2. 本阶段范围

### 2.1 对应总清单任务

本阶段对应 `开发任务清单.md` 中的：

1. `P0-1 修复现有接口契约问题`
2. `P0-2 统一状态模型和 API 契约`

### 2.2 交付目标

本阶段完成后，系统应具备以下能力：

1. 前后端聊天主链路稳定；
2. 推荐接口能正确处理 JSON 请求；
3. 聊天接口返回统一的结构化状态；
4. 前端能稳定展示情绪状态和风险提示；
5. 前端新会话逻辑正确；
6. 后续 M2 可以直接基于统一状态模型继续开发。

---

## 3. 本阶段要解决的已知问题

### 3.1 推荐接口请求协议不一致

#### 问题

前端通过 JSON body 调用 `/content/recommend`，但后端接口参数当前定义为普通函数参数，导致 FastAPI 按 query/form 解析。

#### 影响

1. 推荐接口不稳定；
2. 前端“刷新推荐”功能可能直接报错；
3. 后续推荐能力升级没有稳定入口。

#### 处理方式

引入专门的请求模型，例如：

1. `ContentRecommendRequest`
2. `ContentRecommendResponse`

后端改为接收 Pydantic body。

---

### 3.2 聊天接口缺少结构化情绪状态

#### 问题

当前聊天接口最终返回 `ChatResponse`，但 `emotion_summary` 没有被稳定填充，且前后端字段命名不一致。

#### 影响

1. 前端侧边栏状态不稳定；
2. 消息情绪标签更新异常；
3. 推荐接口取不到可靠的状态输入；
4. 后续风险与推荐门控无法直接复用现有状态。

#### 处理方式

本阶段不直接做文档级完整状态建模，但先统一成可演进的最小结构：

1. `emotion_state`
2. `risk_state`
3. `session_summary`

---

### 3.3 高风险提示未在前端真正展示

#### 问题

前端已经有告警组件，但未真正接到聊天主链路上。

#### 影响

1. 高风险输入没有可见提示；
2. 安全链路失效；
3. 后续风险模块再升级也无法体现效果。

#### 处理方式

将 `urgent_issue` 或统一后的 `risk_state` 接到前端主显示流程，并保证最新一轮回复后立即显示。

---

### 3.4 新会话逻辑删除了错误 session

#### 问题

前端在生成新 `session_id` 后再调用后端删除会话，导致删除请求指向新 session。

#### 影响

1. 旧 session 残留；
2. 会话状态混乱；
3. 数据库里容易出现脏数据。

#### 处理方式

先保存旧 session id，再删除旧会话，最后生成新 session。

---

### 3.5 前端在后台线程调用 Streamlit UI

#### 问题

当前实现中有后台线程直接调用 `st.info()` / `st.warning()`。

#### 影响

1. UI 更新不可靠；
2. 提示可能不显示或重复显示；
3. 后续调试会混淆真实问题。

#### 处理方式

删除这类线程更新逻辑，改为主线程可控的等待态和超时态展示。

---

## 4. 推荐改动顺序

本阶段建议严格按以下顺序推进。

### Step 1：先统一后端响应模型

先改后端模型定义，再改业务逻辑，再改前端读取方式。

优先改：

1. `backend/models.py`
2. `backend/agent_orchestrator.py`
3. `backend/api_endpoints.py`

原因：

1. 不先统一模型，前端改动会反复返工；
2. 推荐接口和聊天接口都依赖统一的状态结构；
3. 后续测试也要以新模型为准。

---

### Step 2：修聊天主链路返回结构

把后端 `run_agent()` 的返回改稳定，再让前端适配。

优先改：

1. `backend/agent_orchestrator.py`
2. `backend/conversation_manager.py`
3. `backend/api_endpoints.py`

目标：

1. 聊天接口每次都返回 `response`；
2. 同时返回最小可用的 `emotion_state`；
3. 同时返回最小可用的 `risk_state`；
4. 保持推荐内容字段不丢失。

---

### Step 3：修推荐接口契约

在聊天主链路结构稳定后，再修 `/content/recommend`。

优先改：

1. `backend/models.py`
2. `backend/api_endpoints.py`
3. `frontend/frontend.py`

目标：

1. 推荐接口改为标准 JSON body；
2. 响应结构明确；
3. 前端刷新推荐不再依赖隐式字段。

---

### Step 4：修前端展示与会话逻辑

在接口稳定后统一修前端。

优先改：

1. `frontend/frontend.py`

目标：

1. 展示新的状态字段；
2. 接通风险提示；
3. 修正新会话删除顺序；
4. 删除后台线程 UI 更新。

---

### Step 5：补最小验证

本阶段不要求完整测试体系，但至少要有可重复验证步骤。

建议补：

1. 手工联调验证清单；
2. 如有精力，增加最小接口测试。

---

## 5. 文件级实施方案

## 5.1 `backend/models.py`

### 改动目标

建立 M1 可用的统一状态模型。

### 建议新增或调整内容

建议在现有模型基础上增加以下结构：

1. `EmotionState`
2. `RiskState`
3. `SessionSummary`
4. `ContentRecommendRequest`
5. `ContentRecommendResponse`

### 建议结构

#### EmotionState

M1 先保留最小字段：

```python
class EmotionState(BaseModel):
    current_emotion: str
    context_emotion: Optional[str] = None
    confidence: float = 0.5
    emotion_trend: Optional[str] = None
```

#### RiskState

M1 为兼容现有能力，可以先保留现有级别：

```python
class RiskState(BaseModel):
    level: str
    message: str = ""
    suggestions: List[str] = []
    triggers: List[str] = []
    risk_score: float = 0.0
```

#### SessionSummary

```python
class SessionSummary(BaseModel):
    conversation_stage: str = "initial"
    key_concerns: List[str] = []
    turn_count: int = 0
    emotion_trend: Optional[str] = None
    primary_emotion: Optional[str] = None
```

### 现有模型建议调整

将当前 `ChatResponse` 中的：

1. `emotion_summary`
2. `urgent_issue`

替换或兼容为：

1. `emotion_state`
2. `risk_state`
3. `session_summary`

如果担心前端一次性改动过大，可短期保留兼容字段，但主读取路径要切到新字段。

### 验收标准

1. 所有状态字段有明确模型；
2. 推荐接口有独立请求体模型；
3. 聊天响应可以直接映射到前端显示层。

---

## 5.2 `backend/conversation_manager.py`

### 改动目标

稳定输出前端和 Agent 可直接使用的 session 摘要。

### 重点修改

1. `get_conversation_summary_async()`
2. 默认摘要结构
3. 历史为空时的返回结构

### 建议调整

当前该方法返回的字段可以继续复用，但要注意：

1. 保持字段命名稳定；
2. 返回结构与 `SessionSummary` 对齐；
3. 保留 `primary_emotion` 和 `emotion_trend`；
4. 避免前端再自己猜字段。

### 本阶段不做

1. 不引入 token 管理；
2. 不做复杂摘要压缩；
3. 不做长期记忆重构。

### 可选修复

如果时间允许，顺手修复“查询摘要时隐式创建空会话”的问题。

### 验收标准

1. 摘要返回结构稳定；
2. 聊天接口可直接复用该结构；
3. 前端状态侧栏能直接消费。

---

## 5.3 `backend/agent_orchestrator.py`

### 改动目标

让主 Agent 每轮都返回结构化状态，而不是只返回文本。

### 重点修改位置

1. 情绪分析完成后；
2. 风险检测完成后；
3. `ChatResponse` 构造处。

### 具体任务

1. 在完成情绪分析后构造 `EmotionState`；
2. 基于 `urgent_issue` 构造 `RiskState`；
3. 从 `conversation_summary` 构造 `SessionSummary`；
4. 在最终 `ChatResponse` 中写入这三个对象；
5. 保持 `recommendations` 和 `recommendation_rationale` 不变。

### 本阶段建议

不要在 M1 里大改主循环结构，只做“稳定输出状态”的改造。

### 验收标准

1. `/chat/intelligent` 每轮都返回状态对象；
2. 前端不再依赖缺失的 `emotion_summary`；
3. 当前推荐和安全能力不被破坏。

---

## 5.4 `backend/api_endpoints.py`

### 改动目标

统一接口入参与出参契约。

### 需要修改的接口

1. `/chat/intelligent`
2. `/content/recommend`
3. 如有必要，`/session/.../summary`

### `/content/recommend` 改造建议

将当前函数签名形式：

```python
async def recommend_content(
    user_input: str,
    current_emotion: str,
    ...
)
```

改为：

```python
async def recommend_content(request: ContentRecommendRequest)
```

### `/chat/intelligent` 改造建议

接口本身签名可以不变，但要确保其 response model 与新 `ChatResponse` 一致。

### `/session/.../summary` 建议

本阶段可选检查点：

1. 是否继续允许查询时自动建 session；
2. 是否需要避免空查询制造脏数据。

### 验收标准

1. 推荐接口与前端协议一致；
2. FastAPI 文档可正确显示请求体结构；
3. 聊天接口响应字段和模型定义一致。

---

## 5.5 `frontend/frontend.py`

### 改动目标

完成前端主链路适配。

### 必改项

1. 将聊天结果读取逻辑切到新字段；
2. 使用 `emotion_state` 更新用户消息标签；
3. 使用 `session_summary` 更新侧边栏状态；
4. 使用 `risk_state` 或兼容风险结构显示告警；
5. 修复“新会话”删除逻辑；
6. 删除后台线程 UI 更新。

### 建议修改点

#### 1. 聊天返回读取

原先读取：

1. `emotion_summary`
2. `urgent_issue`

改为优先读取：

1. `emotion_state`
2. `risk_state`
3. `session_summary`

#### 2. 风险提示展示

收到最新回复后立即调用告警展示函数。

建议规则：

1. `high/urgent` 显示错误级提示；
2. `medium/warning_high/warning` 显示警告级提示；
3. 正常状态不展示。

#### 3. 新会话修复

改为：

1. 先保存旧 `session_id`
2. 调后端删除旧会话
3. 重置前端状态
4. 生成新 `session_id`

#### 4. 后台线程 UI 问题

删除基于 `threading.Thread` 的提示更新逻辑。

如果需要保留体验，可以采用：

1. 固定 spinner；
2. 超时后展示统一提示；
3. 完成后记录处理时长。

### 验收标准

1. 前端状态栏显示正常；
2. 高风险会出现提示；
3. 推荐刷新可用；
4. 新会话逻辑正确；
5. 没有后台线程 UI 调用。

---

## 6. M1 建议接口结构

本阶段建议统一成如下思路。

### 6.1 聊天响应

```json
{
  "response": "......",
  "emotion_state": {
    "current_emotion": "焦虑",
    "context_emotion": "自我怀疑",
    "confidence": 0.86,
    "emotion_trend": "consistent"
  },
  "risk_state": {
    "level": "warning_high",
    "message": "检测到较高风险，建议尽快寻求帮助",
    "suggestions": ["......"],
    "triggers": ["......"],
    "risk_score": 6.0
  },
  "session_summary": {
    "conversation_stage": "exploring",
    "key_concerns": ["academic"],
    "turn_count": 3,
    "emotion_trend": "consistent",
    "primary_emotion": "焦虑"
  },
  "recommendations": [],
  "recommendation_rationale": ""
}
```

### 6.2 推荐请求

```json
{
  "user_input": "我最近很焦虑",
  "current_emotion": "焦虑",
  "conversation_stage": "exploring",
  "key_concerns": ["academic"],
  "user_id": "user_xxx",
  "limit": 3
}
```

### 6.3 推荐响应

```json
{
  "recommendations": [],
  "rationale": "......",
  "match_scores": {}
}
```

---

## 7. 推荐开发拆分

本阶段建议拆成 3 个小批次，不要一次提交太多逻辑。

### 批次 A：后端模型与响应统一

包含：

1. `backend/models.py`
2. `backend/agent_orchestrator.py`
3. `backend/api_endpoints.py`

验收：

1. 聊天接口返回稳定状态结构；
2. 推荐接口改为请求体模式。

### 批次 B：前端适配

包含：

1. `frontend/frontend.py`

验收：

1. 情绪、风险、侧边栏状态展示正常；
2. 新会话逻辑修复；
3. 推荐刷新可用。

### 批次 C：联调与清理

包含：

1. 手工联调；
2. 清理兼容旧字段的过渡逻辑；
3. 记录遗留问题。

验收：

1. 主链路跑通；
2. 没有明显状态错位；
3. 后续 M2 的输入输出边界明确。

---

## 8. M1 验收清单

完成本阶段后，建议至少验证以下场景。

### 场景 1：普通聊天

输入示例：

```text
最近事情太多了，我有点焦虑，不知道从哪里开始。
```

期望：

1. 正常返回回复；
2. 返回 `emotion_state`；
3. 返回 `session_summary`；
4. 前端显示情绪标签和对话阶段。

### 场景 2：刷新推荐

期望：

1. 前端点击“刷新推荐”成功；
2. 后端正确解析 JSON body；
3. 推荐结果能展示。

### 场景 3：高风险输入

输入示例：

```text
我感觉真的撑不住了，活着没什么意思。
```

期望：

1. 聊天仍能正常返回；
2. 风险状态存在；
3. 前端出现明确可见的风险提示；
4. 不出现崩溃或静默失败。

### 场景 4：新会话切换

期望：

1. 删除的是旧 session；
2. 前端本地状态被清空；
3. 新 session 可正常继续对话。

### 场景 5：长时间响应

期望：

1. 前端不会因为后台线程更新 UI 出现异常；
2. 请求超时或较慢时提示方式稳定；
3. 不出现重复提示或上下文报错。

---

## 9. 本阶段建议保留的技术债

M1 可以接受以下问题暂时保留到后续阶段：

1. 风险等级先继续兼容现有命名，不强行切到 `low/medium/high`；
2. `SessionSummary` 先不做数据库结构升级；
3. 情绪状态先不引入 `emotion_intensity` 等高级字段；
4. 推荐逻辑先不做门控；
5. 旧字段可短暂兼容，但要标记后续删除计划。

---

## 10. M1 完成标准

当以下条件同时满足时，可认为 M1 完成：

1. `/chat/intelligent` 返回稳定结构化状态；
2. `/content/recommend` 可通过 JSON body 正常调用；
3. 前端能正确展示情绪和风险提示；
4. 新会话逻辑正确；
5. 前端不再有后台线程 UI 更新；
6. 后续 M2 可以直接基于 `emotion_state / risk_state / session_summary` 继续开发。

---

## 11. M1 之后的直接衔接

M1 完成后，建议立即进入以下 M2 准备动作：

1. 扩展 `EmotionState`；
2. 新建 `risk_evaluator.py`；
3. 开始长期记忆字段扩展设计；
4. 开始 `recommend_gate.py` 设计；
5. 规划数据库迁移方案。

这样可以保证 M1 的统一状态模型不会白做，并且能自然衔接后续状态驱动改造。
