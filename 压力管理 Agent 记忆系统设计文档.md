# 压力管理 Agent 记忆系统设计文档

## 1. 模块概述

记忆系统是压力管理 Agent 的核心模块之一。它的目标是让系统在多轮对话和跨会话交互中持续理解用户，而不是每次都从零开始生成回复。

在压力管理场景中，用户的问题通常具有连续性和长期性。例如，用户可能多次提到学业压力、面试焦虑、睡眠问题或人际关系困扰。如果系统不能记住这些信息，就会出现回复重复、缺少上下文、无法个性化支持的问题。

因此，本项目将记忆系统分为两层：

1. Session 内短期记忆；
2. 跨 Session 长期记忆。

短期记忆解决当前对话连续性问题，长期记忆解决跨会话个性化问题。

---

## 2. 设计目标

记忆系统需要实现以下目标：

1. 保留当前会话中的关键上下文；
2. 在长对话中避免超过大模型上下文窗口；
3. 通过摘要压缩保留历史关键信息；
4. 维护用户长期画像；
5. 记录用户历史情绪事件；
6. 记录用户常见压力来源；
7. 记录用户对不同建议类型的接受度；
8. 支持后续推荐系统和 Prompt 组装；
9. 避免无价值信息和敏感噪声进入长期记忆；
10. 在高风险场景下保留必要的风险状态信息。

---

## 3. 记忆系统总体架构

记忆系统由以下模块组成：

1. ShortMemory：短期记忆模块；
2. LongMemoryStore：长期记忆存储；
3. MemoryRetriever：记忆检索模块；
4. MemoryGate：记忆写入门控；
5. MemoryCompressor：上下文压缩模块；
6. MemoryUpdater：记忆更新模块。

整体流程如下：

```text
当前用户输入
  ↓
读取短期记忆
  ↓
检索长期记忆
  ↓
参与情绪识别 / 风险评估 / 推荐判断
  ↓
生成回复
  ↓
判断是否更新记忆
  ↓
更新短期记忆
  ↓
必要时更新长期记忆
```

---

## 4. 短期记忆设计

短期记忆用于维护当前 Session 内的上下文。

### 4.1 短期记忆目标

短期记忆主要解决两个问题：

第一，保证当前对话连续性。系统需要知道前面用户说过什么、自己回复过什么、已经给过哪些建议。

第二，控制上下文长度。大模型上下文窗口有限，不能无限保留所有历史对话。

### 4.2 短期记忆内容

短期记忆通常包括三部分：

1. 最近 N 轮原始对话；
2. 历史对话摘要；
3. 当前轮输入。

示例：

```text
【Session Summary】
用户最近主要压力来源是面试准备和项目表达。
用户偏好直接、具体、可执行的建议，不喜欢空泛安慰。
前面已经建议过任务拆分法和项目 STAR 表达法。

【Recent Turns】
User: 我感觉面试的时候讲项目讲不清楚。
Assistant: 可以先把项目拆成背景、问题、方法、结果四部分……
User: 但我怕面试官追问细节。

【Current User Input】
那我该怎么准备追问？
```

这种结构既保留了早期对话重点，又保留了最近对话细节。

---

## 5. Token 监控与摘要压缩

短期记忆需要使用 token 监控机制，避免上下文超过模型窗口。

系统可以使用 tiktoken 实时估算当前上下文 token 数。

### 5.1 Token 阈值设计

可以设置两个阈值：

1. soft threshold：达到上下文窗口 60% 到 70% 时，开始压缩较早对话；
2. hard threshold：达到上下文窗口 80% 到 90% 时，强制压缩，只保留摘要和最近若干轮。

示例：

```python
if current_tokens > max_context_tokens * 0.7:
    compress_old_messages()

if current_tokens > max_context_tokens * 0.85:
    keep_summary_and_recent_turns()
```

### 5.2 压缩对象

优先压缩：

1. 较早的对话轮次；
2. 已经被总结过的重复信息；
3. 与当前任务弱相关的闲聊；
4. 大模型之前的长回复。

优先保留：

1. 最近几轮原始对话；
2. 用户当前问题；
3. 用户明确偏好；
4. 高风险表达；
5. 已经给过的关键建议；
6. 用户拒绝或接受过的建议。

### 5.3 摘要内容

摘要需要保留：

1. 用户问题背景；
2. 当前主要压力来源；
3. 用户情绪状态；
4. 已给出的建议；
5. 用户反馈；
6. 后续回复需要延续的信息；
7. 风险等级变化；
8. 不应重复的建议。

摘要示例：

```text
用户近期主要焦虑来自面试准备，担心项目表达不清楚。
用户更偏好具体、结构化、能直接用于面试的建议。
已经建议过使用“背景-问题-方法-结果”框架介绍项目。
用户目前进一步关注面试官追问时如何回答细节。
未出现高风险表达。
```

---

## 6. 长期记忆设计

长期记忆用于保存跨 Session 的用户稳定信息。

### 6.1 长期记忆目标

长期记忆主要解决个性化问题。系统需要在不同会话中记住用户长期偏好和反复出现的问题，例如：

1. 用户常见压力来源；
2. 用户偏好的支持方式；
3. 用户不喜欢的建议风格；
4. 用户历史情绪事件；
5. 用户对推荐内容的接受度；
6. 用户风险等级变化。

### 6.2 长期记忆结构

长期记忆可以使用 JSON 或数据库表结构存储。

示例：

```json
{
  "user_id": "user_001",
  "user_profile": {
    "preferred_support_style": "direct_actionable",
    "avoid_style": ["empty_comfort", "preaching"],
    "main_stress_sources": ["academic_pressure", "job_interview"],
    "risk_level": "medium"
  },
  "mood_events": [
    {
      "event_id": "event_001",
      "date": "2026-06-01",
      "emotion_type": "anxiety",
      "emotion_intensity": 0.72,
      "trigger": "interview_preparation",
      "summary": "用户担心面试中项目讲不清楚。"
    }
  ],
  "recommendation_feedback": {
    "breathing_exercise": "accepted",
    "mindfulness": "neutral",
    "task_planning": "preferred"
  }
}
```

---

## 7. 长期记忆字段设计

### 7.1 用户画像

用户画像记录稳定偏好和长期状态。

```json
{
  "preferred_support_style": "direct_actionable",
  "avoid_style": ["empty_comfort"],
  "main_stress_sources": ["academic_pressure", "job_interview"],
  "risk_level": "medium",
  "updated_at": "2026-06-01T12:00:00"
}
```

字段说明：

| 字段                      | 含义         |
| ----------------------- | ---------- |
| preferred_support_style | 用户偏好的支持方式  |
| avoid_style             | 用户不喜欢的回应风格 |
| main_stress_sources     | 常见压力来源     |
| risk_level              | 长期风险等级     |
| updated_at              | 最近更新时间     |

### 7.2 情绪事件

情绪事件用于记录重要的情绪波动。

```json
{
  "event_id": "event_001",
  "emotion_type": "anxiety",
  "emotion_intensity": 0.76,
  "stress_source": "interview",
  "summary": "用户担心面试表现。",
  "created_at": "2026-06-01T12:05:00"
}
```

### 7.3 推荐反馈

推荐反馈用于记录用户对不同建议类型的接受度。

```json
{
  "breathing_exercise": "accepted",
  "mindfulness": "rejected",
  "task_planning": "preferred"
}
```

这部分信息会被推荐系统用于个性化排序。

---

## 8. 记忆写入门控

长期记忆不能每轮都写入，否则会产生大量噪声。因此需要 MemoryGate 判断哪些信息值得保存。

### 8.1 适合写入的信息

适合写入长期记忆的信息包括：

1. 用户明确表达的长期偏好；
2. 多次重复出现的压力来源；
3. 明显的情绪事件；
4. 用户对建议的明确反馈；
5. 风险等级变化；
6. 对后续个性化有帮助的信息。

示例：

```text
“我不喜欢那种空泛安慰，最好直接告诉我怎么做。”
```

这类信息适合写入 preferred_support_style 或 avoid_style。

### 8.2 不适合写入的信息

不适合写入长期记忆的信息包括：

1. 临时性闲聊；
2. 无长期价值的细节；
3. 低置信度推断；
4. 与压力管理无关的噪声；
5. 可能造成隐私风险的敏感信息。

### 8.3 写入判断伪代码

```python
def should_write_memory(memory_candidate):
    if memory_candidate.confidence < 0.75:
        return False

    if memory_candidate.type in ["stable_preference", "stress_source", "mood_event", "risk_change"]:
        return True

    if memory_candidate.type == "temporary_chat":
        return False

    if memory_candidate.is_sensitive and not memory_candidate.is_necessary:
        return False

    return False
```

---

## 9. 记忆更新策略

长期记忆更新可以分为三类：

1. append；
2. merge；
3. overwrite。

### 9.1 Append

append 用于新增事件，例如新增一次情绪事件。

```python
memory["mood_events"].append(new_event)
```

### 9.2 Merge

merge 用于合并相似信息，例如用户多次提到“面试焦虑”和“求职焦虑”，可以合并到 job_interview 或 career_pressure。

```python
memory["main_stress_sources"] = merge_similar_sources(
    old_sources,
    new_source
)
```

### 9.3 Overwrite

overwrite 用于用户明确修改偏好。

例如，用户之前喜欢详细建议，但后来明确说“以后简单说重点就行”，则更新 preferred_support_style。

```python
memory["preferred_support_style"] = "concise_actionable"
```

---

## 10. 记忆检索机制

每轮对话不应该把全部长期记忆都塞进 Prompt，而是需要根据当前输入检索相关记忆。

检索方式可以包括：

1. 关键词匹配；
2. 向量检索；
3. 结构化字段过滤；
4. 时间衰减排序；
5. 风险优先级排序。

例如，当前用户输入是：

```text
我明天面试，突然很紧张。
```

应该优先检索：

1. 用户历史中的面试焦虑事件；
2. 用户偏好的支持方式；
3. 之前对面试建议的反馈；
4. 最近的求职压力状态。

不需要检索：

1. 与睡眠无关的历史事件；
2. 很久以前且不相关的情绪记录；
3. 与当前问题无关的闲聊摘要。

---

## 11. 记忆参与 Prompt 组装

记忆检索结果会被注入 Prompt，但需要控制长度和优先级。

Prompt 中可以这样组织：

```text
【用户长期画像】
用户偏好直接、具体、可执行的建议。
用户不喜欢空泛安慰。
用户近期常见压力来源是面试和项目表达。

【相关历史事件】
用户曾多次提到担心面试中项目讲不清楚。
此前对“结构化项目表达”和“模拟追问清单”反馈较好。
```

需要注意的是，长期记忆不能覆盖当前用户输入。当前输入永远具有更高优先级。

---

## 12. 与推荐系统的关系

记忆系统会直接影响推荐系统。

长期记忆可以为推荐系统提供：

1. 用户偏好；
2. 历史压力来源；
3. 推荐接受度；
4. 最近推荐记录；
5. 不喜欢的推荐类型；
6. 风险等级。

例如，如果用户长期偏好“具体行动计划”，那么推荐系统在 Rerank 时可以提高 planning 类素材的分数。

如果用户多次拒绝 mindfulness 类建议，则推荐系统应降低正念类素材的排序。

---

## 13. 与安全系统的关系

记忆系统也会影响风险评估和安全响应。

如果用户历史上出现过高风险表达，那么后续对话中，即使当前表达比较隐晦，系统也应该提高风险评估敏感度。

但高风险记忆的使用需要谨慎：

1. 不应在普通回复中直接暴露；
2. 不应过度标签化用户；
3. 只在风险判断和安全策略中作为辅助信息；
4. 需要避免把短期负面情绪永久固化为用户标签。

---

## 14. 记忆系统评估

记忆系统可以从以下几个维度评估。

### 14.1 记忆写入准确性

评估系统是否正确判断哪些信息值得写入长期记忆。

指标包括：

1. 写入 Precision；
2. 写入 Recall；
3. 噪声写入率；
4. 重要信息漏写率。

### 14.2 记忆检索准确性

评估当前对话是否检索到了相关记忆。

指标包括：

1. Memory Recall@K；
2. Memory Precision@K；
3. 相关记忆命中率；
4. 无关记忆注入率。

### 14.3 摘要压缩质量

评估摘要是否保留了关键信息。

指标包括：

1. 关键信息保留率；
2. 摘要压缩率；
3. 已给建议保留率；
4. 风险信息保留率；
5. 用户偏好保留率。

### 14.4 端到端效果

评估记忆是否真正提升回复质量。

指标包括：

1. 回复是否延续上下文；
2. 是否减少重复建议；
3. 是否符合用户长期偏好；
4. 是否正确调用历史信息；
5. 是否提升用户满意度。

---

## 15. 难点与解决方案

### 15.1 难点一：记忆写入边界难判断

如果写入太多，会造成长期记忆污染；如果写入太少，会影响个性化。

解决方案：

1. 使用 MemoryGate；
2. 只写入高置信度信息；
3. 区分短期摘要和长期画像；
4. 对稳定偏好进行多次确认或多轮累积。

### 15.2 难点二：摘要可能丢失关键信息

长对话压缩时，模型可能遗漏用户偏好、风险状态或已给建议。

解决方案：

1. 使用固定摘要模板；
2. 强制保留风险信息；
3. 强制保留用户反馈；
4. 保留最近 N 轮原始对话。

### 15.3 难点三：长期记忆可能与当前输入冲突

用户偏好可能发生变化，历史记忆不能覆盖当前表达。

解决方案：

1. 当前输入优先级最高；
2. 用户明确修改偏好时 overwrite；
3. 长期记忆只作为辅助上下文；
4. Prompt 中明确约束不要机械套用历史记忆。

### 15.4 难点四：敏感信息处理

压力管理场景可能涉及敏感信息，不能无控制地长期保存。

解决方案：

1. 对敏感信息进行过滤；
2. 只保存必要摘要；
3. 避免保存过度细节；
4. 对高风险信息单独标记和谨慎调用。

---

## 16. 模块亮点

记忆系统的主要亮点包括：

1. 区分短期记忆和长期记忆；
2. 使用 tiktoken 实时监控上下文 token 消耗；
3. 超限时自动摘要压缩；
4. 长期记忆结构化存储用户画像、压力来源和情绪事件；
5. 使用记忆门控控制写入质量；
6. 记忆检索结果参与情绪识别、推荐和 Prompt 组装；
7. 推荐反馈会反向更新长期记忆；
8. 风险状态可以影响后续安全响应策略。

---

## 17. 总结

压力管理 Agent 的记忆系统是实现长对话连续性和跨会话个性化的核心基础。短期记忆负责维护当前 Session 内的上下文，通过 token 监控和摘要压缩解决上下文窗口限制；长期记忆负责维护用户画像、情绪事件、压力来源和推荐偏好，使系统能够持续学习用户状态。

完整流程可以概括为：

```text
当前对话
  ↓
短期记忆维护上下文
  ↓
长期记忆检索用户画像
  ↓
参与情绪识别、风险评估和推荐决策
  ↓
生成个性化回复
  ↓
记忆门控判断是否写入
  ↓
更新短期摘要和长期画像
```

该模块体现了 Agent 系统中的上下文工程、用户建模和个性化能力，是整个压力管理 Agent 能够区别于普通大模型聊天机器人的关键部分。
