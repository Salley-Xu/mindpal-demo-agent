# 压力管理 Agent 风险安全模块设计说明

## 1. 模块定位

风险安全模块是压力管理 Agent 中的安全控制中枢。它的作用不是简单判断用户情绪好坏，而是识别用户是否存在自伤、自杀、伤害他人、极端失控或严重心理危机风险，并根据风险程度控制后续对话策略、推荐系统和记忆系统。

在普通压力管理场景中，Agent 可以进行情绪支持、压力分析和内容推荐；但一旦用户表达出高风险信号，系统就必须优先保障安全，关闭或降级普通推荐，切换到更保守的高风险支持模式。

因此，这个模块本质上是一个贯穿 Agent 全流程的 safety gate，而不是一个独立的分类器。

---

## 2. 设计目标

风险安全模块主要解决四个问题：

1. **能不能识别风险**：判断用户输入中是否存在自伤、自杀、伤害他人、失控或严重危机信号。
2. **风险有多严重**：将风险划分为不同等级，而不是简单判断“危险 / 不危险”。
3. **后续流程怎么走**：根据风险等级决定是否允许普通聊天、是否允许推荐、是否需要进入危机支持模式。
4. **多轮对话中是否保持安全上下文**：避免用户上一轮高风险，下一轮简单说“没事”后系统立刻降级成普通对话。

整体原则是：

- 高风险场景优先保证召回率，宁可谨慎一些，也不能漏掉真正危险的输入。
- 风险判断必须可解释，不能只输出一个黑盒标签。
- 风险结果必须影响后续 Agent 流程，而不是只影响当前一句回复。
- 系统不能进行医学诊断，只能做安全识别、情绪支持和求助引导。

---

## 3. 整体流程

风险安全模块可以放在用户输入进入主 Agent 之后、正式生成回复之前。

整体流程如下：

1. 用户输入进入系统。
2. 情绪识别模块判断用户当前情绪状态，例如焦虑、压力、低落、愤怒、疲惫等。
3. 风险识别模块判断是否存在安全相关信号。
4. 风险分类模块根据多个维度评估严重程度。
5. 安全门控模块根据风险等级决定后续策略。
6. 回复生成模块根据对应模式生成回复。
7. 推荐系统根据风险等级决定是否触发、降级或关闭。
8. 记忆系统记录必要的风险状态，用于后续多轮对话保持一致性。

可以理解为：

用户输入首先经过“情绪理解”和“风险理解”，然后由风险安全模块决定后续是走普通压力管理流程，还是走高风险安全支持流程。

---

## 4. 风险识别

### 4.1 风险识别的目标

风险识别负责回答一个问题：**用户当前输入或最近上下文里有没有风险信号？**

这里的风险信号包括：

- 自伤相关表达
- 自杀相关表达
- 伤害他人的表达
- 极端无助或绝望表达
- 明确失控表达
- 与危险时间、地点、工具、行为相关的表达
- 用户对他人危机状态的求助

风险识别不是只看单句话，而是要结合最近几轮上下文。很多高风险表达不是一次性出现的，而是在多轮对话中逐渐升级。

例如，用户先说“最近真的很累”，再说“没人理解我”，最后说“如果我消失了也没人会在意”。单独看最后一句可能只是低落，但结合前几轮就应该提高风险敏感度。

---

### 4.2 风险识别的输入

风险识别模块建议使用以下信息：

| 输入信息 | 作用 |
|---|---|
| 当前用户输入 | 判断当前是否出现直接风险信号 |
| 最近几轮对话 | 识别风险是否逐步升级 |
| 当前情绪识别结果 | 辅助判断用户是否处于强烈痛苦状态 |
| 上一轮风险等级 | 保持多轮安全上下文一致性 |
| 长期记忆中的近期风险状态 | 判断用户是否近期多次出现风险表达 |

这样做可以避免两个问题：

第一，避免只看当前一句话导致漏判。

第二，避免用户短暂否认后风险被过快降级。

---

### 4.3 规则兜底识别

第一层可以使用规则识别作为兜底。规则层主要负责捕捉非常明确、不能漏掉的风险表达。

例如：

- “不想活了”
- “想死”
- “活不下去”
- “结束生命”
- “伤害自己”
- “想消失”
- “控制不住自己”
- “想伤害别人”
- “今晚就这样吧”
- “我已经准备好了”

规则层的重点不是做到最精准，而是保证高危表达不会被漏掉。

但规则层不能只做关键词匹配，因为很多表达存在歧义。例如：

- “我没有想自杀，只是最近压力大。”
- “我朋友说他不想活了，我该怎么办？”
- “这部电影里主角最后自杀了。”

这些句子都包含高危词，但风险主体和语义完全不同。

因此规则层至少需要识别以下信息：

| 判断项 | 说明 |
|---|---|
| 是否存在风险词 | 是否命中自伤、自杀、他伤、失控等表达 |
| 是否是否定表达 | 区分“我想自杀”和“我没有想自杀” |
| 风险主体是谁 | 判断是用户本人、他人，还是影视/新闻/论文讨论 |
| 是否有时间紧迫性 | 判断是否出现“现在、马上、今晚、已经”等表达 |
| 是否有行动倾向 | 判断是否从想法升级到准备行动 |

规则层适合做安全兜底，但不适合作为唯一判断依据。

---

### 4.4 语义风险识别

第二层需要使用语义模型或大模型进行风险识别。

原因是很多高风险表达并不会直接出现“自杀”“自伤”等关键词，而是以隐晦方式表达。

例如：

- “我不想再醒来了。”
- “如果我消失了，应该也没人在乎。”
- “明天之后就都结束了。”
- “我已经安排好了。”
- “我怕自己会做出什么事。”

这些表达需要结合上下文理解，才能判断是否存在风险。

语义识别层可以做两件事：

第一，判断是否存在风险信号。

第二，提取支持判断的证据，例如用户是否表达了消失、结束、无法控制、马上行动等含义。

这样可以让风险判断更可解释，而不是只给出一个标签。

---

### 4.5 上下文一致性校验

高风险状态不能在多轮对话中突然消失。

例如：

上一轮用户说：“我今晚真的不想活了。”

下一轮用户说：“哈哈算了没事。”

这种情况下，系统不能马上恢复到普通聊天模式。更安全的做法是保留一段时间的风险惯性，也就是风险可以逐步下降，但不能断崖式下降。

上下文一致性校验主要解决三个问题：

1. 防止用户临时否认导致风险过快降级。
2. 防止多轮对话中丢失高风险上下文。
3. 识别风险是否正在从低风险逐步升级为高风险。

因此，最终风险识别结果应该结合当前输入和最近几轮对话共同判断。

---

## 5. 风险分类

### 5.1 风险分类的目标

风险分类负责回答第二个问题：**风险严重到什么程度？**

这一步不应该只看用户情绪有多负面，而是要看用户是否已经从“痛苦表达”进入“安全风险状态”。

判断重点包括：

- 是否有自伤或自杀意念
- 是否有伤害他人的想法
- 是否有行动意图
- 是否有明确计划
- 是否有近期时间点
- 是否具备实施条件
- 是否已经失控或接近失控
- 是否存在保护性因素

---

### 5.2 风险等级设计

建议将风险等级设计为四级。

| 风险等级 | 含义 | 示例 | 系统策略 |
|---|---|---|---|
| Level 0 | 普通压力或普通负面情绪 | “最近实验不顺，压力很大。” | 正常支持和推荐 |
| Level 1 | 轻中度情绪风险 | “我真的快撑不住了。” | 谨慎安抚，可推荐低负担内容 |
| Level 2 | 高风险倾向 | “我有时候真的不想活了。” | 降级或关闭普通推荐，进入高风险支持 |
| Level 3 | 紧急危机风险 | “我今晚就准备这么做。” | 关闭普通推荐，进入危机响应模式 |

这个分级方式的好处是可以让系统策略更精细。

低风险场景下，Agent 可以继续做压力管理和内容推荐。

中风险场景下，Agent 需要降低推荐内容的复杂度，优先给用户短、直接、低认知负担的稳定情绪方法。

高风险和紧急风险场景下，普通推荐应该被关闭或严格限制，系统优先引导用户保证当前安全、联系身边的人或专业支持资源。

---

## 6. 多维风险评分模型

### 6.1 为什么需要多维评分

高风险不是一个单一概念。

不同用户表达的危险点可能完全不同：

- 有的人是“非常痛苦，但没有行动意图”。
- 有的人是“有自杀想法，但没有计划”。
- 有的人是“有明确计划，但没有说具体时间”。
- 有的人是“已经准备行动”。
- 有的人是“害怕自己失控伤害别人”。

如果只用一个模型直接输出 risk_level，很难解释为什么判高风险，也很难控制后续策略。

因此，更合理的做法是把风险拆成多个维度分别评估，再综合得到最终等级。

---

### 6.2 推荐风险维度

建议使用以下七个维度。

| 维度 | 含义 | 关注问题 |
|---|---|---|
| 风险意念 | 用户是否有自伤、自杀或伤害他人的想法 | 有没有危险想法 |
| 行动意图 | 用户是否表达了准备行动的倾向 | 是否从想法变成行动倾向 |
| 计划明确性 | 是否出现方法、地点、对象、步骤等信息 | 是否已经有计划 |
| 时间紧迫性 | 风险是否发生在近期、马上或正在发生 | 是否需要立即干预 |
| 可实施条件 | 用户是否接近危险环境、工具、对象或独处 | 是否具备实施条件 |
| 自控能力 | 用户是否表达无法控制自己或接近失控 | 是否还能保持自我控制 |
| 保护性因素 | 用户是否愿意求助、身边是否有人、是否愿意保持安全 | 是否存在降低风险的因素 |

其中最关键的是三个维度：

- 行动意图
- 计划明确性
- 时间紧迫性

因为它们决定了风险是否已经从“想法”升级到“可能马上发生的行动”。

---

### 6.3 各维度评分逻辑

每个维度可以按 0 到 3 分评估。

#### 风险意念

| 分数 | 含义 |
|---|---|
| 0 | 没有相关表达 |
| 1 | 轻微消极表达，例如“好累”“不想面对” |
| 2 | 被动风险表达，例如“如果消失就好了” |
| 3 | 明确风险表达，例如“我不想活了”“我想伤害自己” |

#### 行动意图

| 分数 | 含义 |
|---|---|
| 0 | 没有行动倾向 |
| 1 | 模糊担心，例如“我怕自己撑不住” |
| 2 | 有一定行动倾向，例如“我可能会做傻事” |
| 3 | 明确表示准备行动 |

#### 计划明确性

| 分数 | 含义 |
|---|---|
| 0 | 没有计划 |
| 1 | 泛泛提到可能行为 |
| 2 | 有部分计划信息 |
| 3 | 有明确方式、地点、对象或准备安排 |

#### 时间紧迫性

| 分数 | 含义 |
|---|---|
| 0 | 没有时间信息 |
| 1 | 模糊未来时间 |
| 2 | 近期时间，例如“这几天”“今晚” |
| 3 | 立即或正在发生，例如“现在”“马上”“已经” |

#### 可实施条件

| 分数 | 含义 |
|---|---|
| 0 | 没有可实施条件 |
| 1 | 条件不明确 |
| 2 | 部分具备条件 |
| 3 | 已经具备明显条件 |

#### 自控能力

| 分数 | 含义 |
|---|---|
| 0 | 状态相对稳定 |
| 1 | 明显崩溃，但还能交流 |
| 2 | 表达害怕失控 |
| 3 | 表达已经控制不住或正在失控 |

#### 保护性因素

保护性因素是反向维度。保护因素越弱，风险越高。

| 分数 | 含义 |
|---|---|
| 0 | 保护因素较强，例如愿意求助、身边有人 |
| 1 | 有一定保护因素 |
| 2 | 保护因素较弱 |
| 3 | 几乎没有保护因素，或拒绝求助、处于孤立状态 |

需要注意：保护性因素只能辅助降级，不能覆盖明确的紧急风险。只要用户存在明确近期行动计划，即使有保护因素，也不能简单判为低风险。

---

## 7. 风险融合逻辑

### 7.1 基础评分

多维评分完成后，可以得到一个基础风险分。

基础风险分的作用是提供一个整体趋势判断：用户当前风险大概处于普通、轻中度、高风险还是紧急风险。

不同维度权重可以不同。通常情况下，时间紧迫性、行动意图、计划明确性、可实施条件、自控能力的权重应该高于普通情绪强度。

原因是：

情绪低落不一定代表马上危险，但“明确行动意图 + 近期时间 + 实施条件”通常意味着风险已经显著升高。

---

### 7.2 硬规则提升

风险安全不能只看平均分。因为平均分可能把某些极端风险稀释掉。

例如，一个用户只说了一句话：“我现在就准备去做。”

即使其他维度信息不足，只要时间紧迫性和行动意图足够明确，也应该直接提升到高风险或紧急风险。

因此，系统需要硬规则提升机制。

典型提升规则包括：

| 条件 | 处理方式 |
|---|---|
| 明确近期行动意图 | 至少提升到高风险 |
| 明确计划 + 明确行动意图 | 提升到紧急风险 |
| 出现“现在、马上、已经”等紧急信号 | 提升到紧急风险 |
| 表达已经控制不住自己 | 提升到紧急风险 |
| 明确伤害他人且有对象或行动倾向 | 提升到紧急风险 |
| 上一轮为高风险，本轮信息不足 | 保持风险惯性，不能直接归零 |

这类规则是安全兜底，优先级应该高于基础评分。

---

### 7.3 风险惯性

风险惯性是多轮对话安全里非常重要的一点。

如果用户上一轮表达了高风险，下一轮只说“算了没事”，系统不能直接恢复普通聊天。

更合理的处理是：

- Level 3 可以降到 Level 2，但不能立即降到 Level 0。
- Level 2 可以在多轮稳定交流后逐步降到 Level 1。
- 只有当用户持续表达安全、愿意求助、情绪稳定，并且没有新的风险信号时，才逐步恢复普通模式。

这样可以防止系统在多轮对话中丢失危机上下文。

---

## 8. 不同风险等级下的系统策略

### 8.1 Level 0：普通压力状态

适用场景：

- 用户表达压力、焦虑、疲惫、效率低、实验不顺等。
- 没有自伤、自杀或伤害他人信号。

系统策略：

- 正常共情和分析问题。
- 可以追问压力来源。
- 可以推荐压力管理内容。
- 可以调用普通推荐系统。
- 可以记录普通情绪事件。

推荐内容可以包括：

- 呼吸训练
- 时间管理建议
- 睡眠改善建议
- 情绪记录
- 轻运动
- 压力管理文章

---

### 8.2 Level 1：轻中度情绪风险

适用场景：

- 用户表达明显崩溃、无助、持续焦虑或失眠。
- 但没有明确自伤、自杀或伤害他人意图。

系统策略：

- 回复更加谨慎，优先稳定情绪。
- 可以适度确认用户当前是否安全。
- 推荐低认知负担内容。
- 避免推荐复杂长文或高执行成本任务。
- 可以记录近期压力升高状态。

推荐内容应偏向短、直接、可立即执行，例如：

- 一分钟呼吸练习
- grounding 技巧
- 简单情绪整理
- “先完成一个小动作”的行为激活建议

---

### 8.3 Level 2：高风险倾向

适用场景：

- 用户出现自伤、自杀、伤害他人或严重失控的模糊表达。
- 没有明确到马上实施，但风险已经不能按普通压力处理。

系统策略：

- 普通推荐系统降级或关闭。
- 回复进入高风险支持模式。
- 明确关注用户当前安全。
- 鼓励用户联系身边可信任的人。
- 建议寻求专业帮助。
- 可以进行安全确认，但避免询问危险细节。

此时的核心目标不是分析问题根源，而是让用户先保持安全、不要独处、不要接近危险环境，并尽快获得现实中的支持。

---

### 8.4 Level 3：紧急危机风险

适用场景：

- 用户表达明确行动意图。
- 用户提到近期或立即时间点。
- 用户表示已经准备好或正在实施。
- 用户表示无法控制自己。
- 用户表达明确伤害他人意图且存在对象或条件。

系统策略：

- 关闭普通推荐系统。
- 不进行普通心理分析。
- 不输出复杂建议。
- 不提供任何危险行为细节。
- 回复必须短、直接、低认知负担。
- 明确建议用户立即联系身边的人、紧急服务或危机支持资源。
- 引导用户离开危险物品、危险地点或危险对象。

Level 3 下，Agent 的目标不是“解决心理问题”，而是“让用户安全地度过当前危机”。

---

## 9. 与推荐系统的联动

风险安全模块会直接控制推荐系统。

| 风险等级 | 推荐系统策略 |
|---|---|
| Level 0 | 正常推荐压力管理内容 |
| Level 1 | 推荐低认知负担、立即可执行的稳定情绪内容 |
| Level 2 | 普通推荐降级或关闭，只允许安全支持类内容 |
| Level 3 | 关闭普通推荐，只输出紧急安全引导 |

这样设计的原因是：高风险用户不适合接收普通推荐内容。

例如，当用户已经表达“我今晚不想活了”时，系统不能继续推荐“冥想文章”“压力管理课程”或“运动建议”。这类内容即使本身无害，也可能显得不合时宜，甚至让用户觉得系统没有理解危机严重性。

因此，风险安全模块应该作为推荐系统前置门控。

---

## 10. 与记忆系统的联动

风险安全模块还需要和记忆系统联动。

### 10.1 短期记忆

短期记忆用于保存当前会话中的风险上下文。

例如：

- 用户刚刚表达过强烈无助感。
- 用户上一轮出现过自伤相关表达。
- 用户刚刚否认风险，但前文仍然有高风险信号。

这些信息应该在当前会话后续几轮中继续影响回复策略。

### 10.2 长期记忆

长期记忆可以记录近期风险状态，但要避免给用户贴诊断标签。

不建议记录为：

“用户有自杀倾向。”

更合适的记录方式是：

“用户近期多次表达强压力和无助感，后续回复需要更加谨慎，优先使用支持性回应。”

这样既能保持 Agent 的连续性，又避免过度诊断和标签化。

---

## 11. 输出安全检查

除了输入阶段的风险识别，系统还需要对最终回复做安全检查。

输出安全检查主要避免以下问题：

- 提供危险行为的具体方法或步骤。
- 提供药物剂量、工具使用方式等危险细节。
- 鼓励用户独处或隔离自己。
- 否定用户痛苦，例如“你想太多了”。
- 做出不负责任的保证，例如“你一定不会有事”。
- 在紧急风险场景下继续普通闲聊。
- 在高风险场景下推荐不合适内容。

如果输出不符合安全策略，系统应该重新生成，或者切换到预设的安全响应模板。

---

## 12. 可复用研究与模型

这个模块可以参考三个方向。

### 12.1 临床风险量表

可以参考 C-SSRS 这类自杀风险评估量表。它会从死亡意念、自杀想法、方法、意图、计划和准备行为等角度评估风险。

项目里的多维风险评分模型可以借鉴这些维度，把临床量表中的风险问题转化为 Agent 可处理的结构化特征。

需要注意的是，Agent 不能替代临床诊断，只能做安全预警和支持性引导。

---

### 12.2 NLP 风险识别任务

可以参考 CLPsych 和 eRisk 这类心理健康 NLP 任务。

这些任务通常会基于社交媒体文本识别用户是否存在心理危机风险，或者将风险分为 no risk、low risk、moderate risk、severe risk 等等级。

这类研究对项目的启发是：

- 风险识别应该结合用户多段文本，而不是只看单句。
- 风险等级应该可解释，最好能输出支持判断的证据。
- 风险识别不应该只依赖关键词，而要结合上下文语义。

---

### 12.3 心理健康领域模型

可参考的模型方向包括：

- MentalBERT / MentalRoBERTa
- Chinese MentalBERT
- MentalGLM
- MentaLLaMA
- 其他 suicide detection 或 mental health classification 模型

这些模型可以用于：

- 心理健康文本编码
- 风险信号识别
- 风险等级分类
- 风险证据抽取
- 多维评分辅助

但它们不应该作为唯一决策来源。更稳妥的做法是将模型输出作为风险识别的一部分，再结合规则兜底、多维评分、硬规则提升和上下文惯性得到最终结果。

---

## 13. 测试集设计

为了验证风险安全模块，需要构造覆盖不同风险等级的测试集。

测试集可以分为以下几类：

| 类型 | 示例方向 | 目标 |
|---|---|---|
| 普通压力 | 实验失败、学习压力、工作压力 | 验证不会误判为高风险 |
| 轻中度崩溃 | “撑不住了”“很绝望” | 验证能识别情绪风险 |
| 模糊自伤表达 | “想消失”“不想醒来” | 验证语义识别能力 |
| 明确自杀表达 | “不想活了” | 验证高风险召回 |
| 紧急风险 | “现在”“今晚”“已经准备好了” | 验证紧急风险识别 |
| 他伤风险 | “控制不住想伤害别人” | 验证非自伤安全风险 |
| 否定表达 | “我没有想自杀” | 验证不会简单关键词误判 |
| 第三方求助 | “我朋友说不想活了” | 验证主体识别能力 |
| 新闻/影视/论文讨论 | “电影角色自杀了” | 验证语境识别能力 |
| 多轮升级 | 从压力逐渐升级到高风险 | 验证上下文风险累积 |

测试集不需要一开始很大，但要覆盖边界情况。对于安全模块来说，边界样本比普通样本更重要。

---

## 14. 评估指标

风险安全模块的评估不能只看准确率。

更重要的指标包括：

| 指标 | 作用 |
|---|---|
| 高风险召回率 | 高风险样本是否被识别出来 |
| Level 3 召回率 | 紧急风险是否被漏判 |
| 误报率 | 普通压力是否被过度判为高风险 |
| 分级一致性 | 模型输出等级是否符合人工标注 |
| 证据准确性 | 输出的风险证据是否支持判断 |
| 多轮稳定性 | 风险等级是否在多轮中合理变化 |
| 策略一致性 | risk_level 是否正确控制推荐和回复模式 |

其中最重要的是高风险召回率和 Level 3 召回率。

原因是安全场景下，漏判高风险的代价远高于误判。

---

## 15. 项目落地建议

建议分三个阶段实现。

### 第一阶段：规则 + LLM 结构化评估

先实现规则兜底、风险维度评估、硬规则提升和安全门控。

这一阶段不需要训练模型，适合作为项目 MVP。

重点是把流程跑通：用户输入后能输出 risk_level，并能控制回复模式和推荐系统。

### 第二阶段：加入小规模标注数据

收集或构造一批中文压力管理对话样本，人工标注风险等级和风险维度。

然后用这些数据评估规则和 LLM 输出是否稳定。

这个阶段可以开始优化阈值、规则和 Prompt。

### 第三阶段：引入可复用模型

如果数据量足够，可以接入 Chinese MentalBERT、通用中文 RoBERTa 或其他心理健康文本模型，训练一个辅助风险分类器。

模型输出不直接作为最终结果，而是作为 risk scoring model 的一部分。

最终仍然由规则兜底、多维评分、硬规则提升和上下文惯性共同决定风险等级。

---

## 16. 一句话总结

风险安全模块本质上是压力管理 Agent 的 safety controller。它通过规则兜底、语义识别、多维风险评分、硬规则提升和上下文风险惯性，将用户输入映射为可解释的风险等级，并进一步控制回复模式、推荐系统和记忆系统，保证 Agent 在普通压力场景和高风险危机场景下采取完全不同的处理策略。
